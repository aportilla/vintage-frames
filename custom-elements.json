{
  "schemaVersion": "1.0.0",
  "readme": "",
  "modules": [
    {
      "kind": "javascript-module",
      "path": "src/components/vf-button-group.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "Lays out a set of `vf-button`s the way a System 7 dialog does: a tidy row\n(or column) with a consistent gap, every button sharing the width of the\nwidest, and all their *faces* on one line.\n\nTwo wrinkles make an ad-hoc flex row get this subtly wrong, and the group\nfixes both:\n\n- **Face alignment.** A `variant=\"default\"` button reserves room for its\n  ring with a RING_INSETpx margin, so in a row aligned by any edge\n  its *face* drifts from its neighbors' — you line up the ring, not the\n  button. The group zeroes that margin (via the inherited\n  `--vf-button-ring-margin`) and reserves the ring space itself as padding,\n  then centers the cross axis, so the faces — not the margin boxes — align.\n- **Shared width.** Classic dialogs sized grouped buttons to the widest, even\n  when a label was short (\"OK\" as wide as \"Cancel\"). The group does this with\n  pure CSS — equal `1fr` grid columns resolve to the widest button's\n  intrinsic width under the shrink-wrapped `inline-grid` — and stretches each\n  button's face to fill (via the inherited `--vf-button-flex`). No measuring.\n  `natural` opts out, letting each button size to its own label.\n\nThe group is layout-neutral: it shrink-wraps to its buttons, so position it\nwith the parent (e.g. `justify-self: end` for a bottom-right action row).",
          "name": "VfButtonGroup",
          "cssProperties": [
            {
              "description": "Gap between buttons (default 12px).",
              "name": "--vf-button-group-gap"
            }
          ],
          "slots": [
            {
              "description": "The `vf-button`s to arrange.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "vertical",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Stack the buttons vertically instead of in a row.",
              "attribute": "vertical",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "natural",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Let each button size to its own label instead of every button sharing the\nwidest button's width. Off by default: grouped buttons are uniform width,\nmatching classic System 7 dialogs.",
              "attribute": "natural",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "private",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "role",
              "type": {
                "text": "string"
              },
              "default": "'group'"
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "vertical",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Stack the buttons vertically instead of in a row.",
              "fieldName": "vertical"
            },
            {
              "name": "natural",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Let each button size to its own label instead of every button sharing the\nwidest button's width. Off by default: grouped buttons are uniform width,\nmatching classic System 7 dialogs.",
              "fieldName": "natural"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-button-group",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfButtonGroup",
          "declaration": {
            "name": "VfButtonGroup",
            "module": "src/components/vf-button-group.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-button-group",
          "declaration": {
            "name": "VfButtonGroup",
            "module": "src/components/vf-button-group.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-button.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "The classic System 7 push button (\"OK\", \"Cancel\", \"Install\", …).\n\nA rounded-rectangle control with a 1px black border and white face that\ninverts to white-on-black while pressed. `variant=\"default\"` draws the\nclassic bold double ring around the button, marking it as the default\naction of a dialog. `size=\"small\"` renders the compact 16px button with a\nbody-face label.\n\nThe rounded corners are not `border-radius` arcs: frame, face, and ring are\nstepped `clip-path` silhouettes traced pixel-for-pixel from the kit's button\nreference sheet (see `src/pixel-frame.ts`), so every corner renders as the\nexact 1-bit staircase with no antialiasing.\n\nKeyboard focus — an affordance System 7 didn't have, drawn in its idiom\nanyway — is a 1px dashed rule under the label text (`vfFocusUnderline`)\nrather than a ring around the control.\n\nForm-associated: place it inside a `<form>` and `type=\"submit\"` submits the\nform (contributing its `name`/`value` to the submission), `type=\"reset\"`\nresets it. Enter and Space activate it via the inner native button.",
          "name": "VfButton",
          "cssProperties": [
            {
              "description": "`vf-button` face (the default ring's inner box is 80×20)",
              "name": "--vf-button-height",
              "default": "20px"
            },
            {
              "description": "`size=\"small\"` buttons",
              "name": "--vf-control-height-small",
              "default": "16px"
            }
          ],
          "cssParts": [
            {
              "description": "The inner native `<button>` element.",
              "name": "button"
            }
          ],
          "slots": [
            {
              "description": "The button label.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "shadowRootOptions",
              "type": {
                "text": "ShadowRootInit"
              },
              "static": true,
              "default": "{ ...VfFormControl.shadowRootOptions, delegatesFocus: true, }"
            },
            {
              "kind": "field",
              "name": "variant",
              "type": {
                "text": "'default' | undefined"
              },
              "description": "When set to `'default'`, draws the classic double ring that marks the\ndefault button (activated by Return in real System 7 dialogs).",
              "attribute": "variant",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "size",
              "type": {
                "text": "'small' | undefined"
              },
              "description": "When set to `'small'`, renders the compact 16px button from the\nreference sheet's third row: same pixel-traced corners, body-face label.",
              "attribute": "size",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "type",
              "type": {
                "text": "'button' | 'submit' | 'reset'"
              },
              "default": "'button'",
              "description": "Activation behavior, mirroring native `<button type>`:\n`'submit'` submits the associated form, `'reset'` resets it and\n`'button'` (the default) does nothing beyond the `click` event.",
              "attribute": "type"
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name; submitted as `name=value` when `type=\"submit\"`.",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Value submitted under `name` when `type=\"submit\"`.",
              "attribute": "value",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "buttonEl",
              "type": {
                "text": "HTMLButtonElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "click",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Forwards a synthetic activation to the real `<button>` in the shadow\nroot. A `click()` on the host dispatches at the host and propagates *up*,\nnever down to the inner button's listener — so the native control, the\nactual activation surface, is handed the call instead. Its click then\nbubbles back out composed, exactly as a pointer's does. This is what the\nfields' implicit submission (Enter) activates as the form's default\nbutton; a disabled button swallows it natively, as it should."
            },
            {
              "kind": "field",
              "name": "handleClick",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "Whether the control is empty for `required`'s purposes. Overridden by the\ncontrols a value can be missing from — `value === ''` on the fields, the\nselect and the radio group, unchecked on the checkbox. The default never\nfails, which is what makes a bare `required` inert on the rest.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The `valueMissing` message; subclasses match their native counterpart's.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "variant",
              "type": {
                "text": "'default' | undefined"
              },
              "description": "When set to `'default'`, draws the classic double ring that marks the\ndefault button (activated by Return in real System 7 dialogs).",
              "fieldName": "variant"
            },
            {
              "name": "size",
              "type": {
                "text": "'small' | undefined"
              },
              "description": "When set to `'small'`, renders the compact 16px button from the\nreference sheet's third row: same pixel-traced corners, body-face label.",
              "fieldName": "size"
            },
            {
              "name": "type",
              "type": {
                "text": "'button' | 'submit' | 'reset'"
              },
              "default": "'button'",
              "description": "Activation behavior, mirroring native `<button type>`:\n`'submit'` submits the associated form, `'reset'` resets it and\n`'button'` (the default) does nothing beyond the `click` event.",
              "fieldName": "type"
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name; submitted as `name=value` when `type=\"submit\"`.",
              "fieldName": "name"
            },
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Value submitted under `name` when `type=\"submit\"`.",
              "fieldName": "value"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "VfFormControl",
            "module": "/src/form-control.js"
          },
          "tagName": "vf-button",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfButton",
          "declaration": {
            "name": "VfButton",
            "module": "src/components/vf-button.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-button",
          "declaration": {
            "name": "VfButton",
            "module": "src/components/vf-button.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-checkbox.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "The classic System 7 checkbox: a 13×13 white square with a 1px black\nborder whose checked state is the corner-to-corner ✕ glyph — the pixel-exact\ncross traced from the Classic Macintosh UI Kit sprite. The border \"thickens\"\nwhile pressed, exactly like the original control.\n\nForm-associated: submits `value` under `name` when checked (like a native\ncheckbox) and restores its initial checked state on form reset. Toggles on\nclick and Space.",
          "name": "VfCheckbox",
          "cssParts": [
            {
              "description": "The 13×13 checkbox square.",
              "name": "box"
            },
            {
              "description": "The label wrapper around the slot.",
              "name": "label"
            }
          ],
          "slots": [
            {
              "description": "The label, rendered to the right of the box with a 6px gap.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "checked",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the checkbox is checked.",
              "default": "false",
              "attribute": "checked",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting.",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "'on'",
              "description": "Value submitted with the form while checked.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "`required` on a checkbox means it must be checked, as on a native one.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The message a native `<input type=\"checkbox\" required>` reports.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "toggleInternals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "description": "The handle VfFormControl already attached.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "toggleDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "`disabled`, or an ancestor `<fieldset disabled>`.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Form-associated lifecycle: restores the initial checked state."
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Restored form state means the flag, not `value`: a checkbox only ever\nstores its submission string while checked, so getting one back IS\n\"checked\" (the base default would have overwritten `value` instead).",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "activate",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Click/Space on an enabled checkbox flips it.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "role",
              "type": {
                "text": "string"
              },
              "default": "'checkbox'"
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "externallyCoordinated",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "True while an ancestor coordinator owns this control's selection *and*\nits tab stop — the `vf-radio-group` case, where the group runs a roving\ntabindex and is the single source of truth for which child is checked.\nA coordinated control neither self-manages its tabindex nor optimistically\nself-checks. Always false for a standalone control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#consumerOwnsTabIndex",
              "privacy": "private",
              "type": {
                "text": "boolean | null"
              },
              "default": "null",
              "description": "Whether the *consumer* authored a `tabindex`, latched on the *first*\nconnect only.\n\nIt has to be latched: once we self-manage, `this.tabIndex = 0` writes a\n`tabindex` attribute onto the host, so re-testing `hasAttribute` on a\nlater connect reads our own write back as the consumer's and stands down\nforever — leaving a reconnected control that no longer drops out of the\ntab order when disabled. Same trap, and the same fix, as `vf-menu-item`'s\n`#ownsRole` latch.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "selfManagedTabIndex",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "True when this control currently owns its host tabindex: the consumer\ndidn't author one and no coordinator is running a roving tabindex.\nRe-derived on every connect, because externallyCoordinated can\nlegitimately change when a control is re-parented into or out of a group.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncToggleState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Mirror the control's state into ARIA and keep its tab stop honest.\nSeparate from `updated()` so a subclass that gates its own work on\nchanged keys can still call it unconditionally.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "#interact",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The one gate every activation passes through.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#handleClick",
              "privacy": "private",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#handleKeydown",
              "privacy": "private",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "events": [
            {
              "description": "When toggled by user interaction. `detail: { checked: boolean }`.",
              "name": "vf-change"
            },
            {
              "description": "Native event, dispatched from the host per user toggle (with `change`, the pair a native checkbox fires). A programmatic `checked` set fires nothing, as on a native checkbox.",
              "name": "input"
            },
            {
              "description": "Native event, dispatched from the host per user toggle so form delegation and framework bindings hear it.",
              "name": "change"
            }
          ],
          "attributes": [
            {
              "name": "checked",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the checkbox is checked.",
              "fieldName": "checked"
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting.",
              "fieldName": "name"
            },
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "'on'",
              "description": "Value submitted with the form while checked.",
              "fieldName": "value"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            },
            {
              "name": "VfToggleControl",
              "module": "/src/toggle-control.js"
            }
          ],
          "superclass": {
            "name": "VfFormControl",
            "module": "/src/form-control.js"
          },
          "tagName": "vf-checkbox",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfCheckbox",
          "declaration": {
            "name": "VfCheckbox",
            "module": "src/components/vf-checkbox.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-checkbox",
          "declaration": {
            "name": "VfCheckbox",
            "module": "src/components/vf-checkbox.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-desktop.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-desktop>` — the full-bleed classic desktop container.\n\nRenders the 50%-dither gray desktop pattern and manages the stacking order\nand `active` state of slotted `vf-window` children: a `pointerdown` or\n`focusin` (keyboard focus) anywhere inside a window brings it to the front\nand makes it the single active window. The windows' light-DOM order is kept\nin step with the stacking order (bottom-most first, at pointer-gesture\nends), so tabbing walks the stack the way the eye does and Shift+Tab is\nits exact mirror.\n\nUtility windows (`vf-window[variant=\"utility\"]`) stack in a floating tier\nabove every document-tier window, restack only among themselves, and stand\noutside the single-active invariant entirely — clicking a palette neither\ndeactivates the active document window nor greys the palette, exactly as\nSystem 7's floating windoids behaved while their application was frontmost.\n\nThe desktop is a raster with an explicit size, always: **`width` and\n`height`**, in system px, the way a WIND resource declared a window's —\nthe host box renders at the declared screen plus `2 × bezel` per axis, a\nwhole number of system pixels by construction (default 512×342, the\ncompact Mac's screen). Pure CSS sizing is not supported; the page sets\nthe numbers — directly, or via VfDesktop.fitWithin on\n`resize`/`onScaleChange` for a viewport-filling desktop — and positions\nthe sized box with its own stylesheet, keeping any sub-system-pixel\nslack on its side. `bezel` (system px) draws the black screen surround —\nthe CRT's unlit margin — around the screen, rounding its top corners\nwith the classic corner mask.\n\nCustom properties:\n- `--vf-desktop-pattern` — the background-image pattern layer (default a\n  1-bit 50% checker dither, drawn as opaque black-on-white).\n- `--vf-desktop` — base color painted *under* the pattern layer (default\n  `#808080`). The default tile is opaque, so this only becomes visible when\n  `--vf-desktop-pattern` is overridden with a tile that has transparent\n  cells (or with `none`).",
          "name": "VfDesktop",
          "cssProperties": [
            {
              "description": "base color under the desktop dither — occluded by the default (opaque) tile, so it only shows through a custom `--vf-desktop-pattern`",
              "name": "--vf-desktop",
              "default": "#808080"
            },
            {
              "description": "`vf-desktop`'s background-image layer — a 50% checker drawn as opaque black-on-white rects (override the whole pattern)",
              "name": "--vf-desktop-pattern"
            }
          ],
          "cssParts": [
            {
              "description": "The dithered screen surface — the whole-system-px raster (inset by `bezel` when one is set).",
              "name": "desktop"
            }
          ],
          "slots": [
            {
              "description": "Default slot: menu bar, windows, anything.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "width",
              "default": "512",
              "description": "Screen width in system px — the raster's own size, the way a WIND\nresource declared a window's. The host box renders at exactly\n`width + 2 × bezel` system px, always whole; a desktop is never sized\nby page CSS — the page sets these numbers (directly or via\nfitWithin) and positions the explicitly sized box with its own\nstylesheet, keeping whatever sub-system-pixel slack its layout has on\nits side of the fence. Defaults to the compact Mac's 512.",
              "attribute": "width",
              "type": {
                "text": "number"
              }
            },
            {
              "kind": "field",
              "name": "height",
              "default": "342",
              "description": "Screen height in system px; see width. Defaults to 342.",
              "attribute": "height",
              "type": {
                "text": "number"
              }
            },
            {
              "kind": "field",
              "name": "bezel",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Width of the black screen bezel, in system px (`0` = none), added onto\nthe declared screen on every side — a `width=\"502\" bezel=\"5\"` desktop\nrenders a 512-system-px host box. The compact Mac's CRT showed an\nunlit black margin between the desktop's raster and the case; `bezel`\ndraws it around the screen and puts the classic screen-corner mask on\nthe screen's two *top* corners — only the top pair was rounded in the\nframebuffer. Flow, window coordinates and the drag clip all belong to\nthe screen, so windows crop at its edge. Inside a bezeled desktop a\nmenu bar needs no `rounded` of its own — the desktop's mask lands on\nthe same pixels.",
              "attribute": "bezel"
            },
            {
              "kind": "method",
              "name": "fitWithin",
              "return": {
                "type": {
                  "text": "{ width: number; height: number }"
                }
              },
              "parameters": [
                {
                  "name": "maxWidth",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "maxHeight",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Size the screen to the largest whole-system-px raster whose host box —\nbezel included — fits a CSS-px bound, and return what was set. The\npage's half of the sizing contract: it owns the viewport, so it\nmeasures the box and hands it here on `resize` and `onScaleChange`\n(zoom and density moves change how many CSS px a system px costs),\nand the sub-system-pixel remainder stays on the page, behind the\nbezel. The epsilon forgives float error in the division so an\nexact-fit bound never drops a whole system pixel."
            },
            {
              "kind": "field",
              "name": "_windows",
              "type": {
                "text": "HTMLElement[]"
              },
              "privacy": "private",
              "description": "Slotted `vf-window` children (direct children only)."
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "_zCounter",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "default": "0",
              "description": "Monotonic z-index counter for window stacking."
            },
            {
              "kind": "field",
              "name": "_awaitingUpgrade",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether a one-shot post-upgrade re-normalization is already pending."
            },
            {
              "kind": "field",
              "name": "_pointerGesture",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether a pointer gesture that began on the desktop is still in flight."
            },
            {
              "kind": "field",
              "name": "_restoringFocus",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether _syncDomOrder is putting focus back after a node move."
            },
            {
              "kind": "field",
              "name": "_gestureEnd",
              "privacy": "private",
              "readonly": true,
              "default": "new DocumentListenersController(this, () => [ [document, 'pointerup', this._onGestureEnd, true], [document, 'pointercancel', this._onGestureEnd, true], ])",
              "description": "Ends the pointer-gesture window that defers DOM reordering. Capture-phase\non the document so a component's `stopPropagation` can't strand the flag,\nand detached on host disconnect by the controller."
            },
            {
              "kind": "method",
              "name": "_isUtility",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "win",
                  "type": {
                    "text": "HTMLElement"
                  }
                }
              ],
              "description": "Whether a slotted window belongs to the floating (utility) tier. The\nattribute is the source of truth here so a not-yet-upgraded element\nstill lands in the right tier (variant reflects, so an upgraded\nproperty-set window agrees)."
            },
            {
              "kind": "method",
              "name": "bringToFront",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "win",
                  "type": {
                    "text": "HTMLElement"
                  }
                }
              ],
              "description": "Bring a slotted window to the front of its tier. A document-tier window\nalso becomes the single active window (clearing `active` on the other\ndocument windows); a utility window restacks within the floating tier\nand leaves every `active` state alone.\n\nThe light-DOM order of the slotted windows follows (_syncDomOrder):\nvisual stacking and sequential focus order come from independent channels\n(z-index vs DOM position), and letting them drift apart is how a desktop\ntabs front-to-back one way and back-to-front the other, with widgets\nreachable only travelling backwards. Deferred to the end of any in-flight\npointer gesture — moving a node clears the pointer capture a title-bar\ndrag or grow-box resize holds on it, and a background window must stay\ndraggable in the same gesture that raises it."
            },
            {
              "kind": "method",
              "name": "_restack",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "win",
                  "type": {
                    "text": "HTMLElement"
                  }
                }
              ],
              "description": "The z/active half of a raise, shared by every path."
            },
            {
              "kind": "method",
              "name": "_requestDomSync",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Sync the DOM order now, or at gesture end if a pointer is down."
            },
            {
              "kind": "method",
              "name": "_raise",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "win",
                  "type": {
                    "text": "HTMLElement"
                  }
                }
              ],
              "description": "Raise the window an event originated in, skipping the restack/activation\nchurn when it is already on top of its own tier (and, for a document\nwindow, already active): otherwise every click inside the front window\nwould bump _zCounter and re-run the whole-fleet activation loop for\nnothing.\n\nRestacks z/active only — deliberately no DOM sync. A focus-driven raise\nMUST NOT move nodes: moving the window focus just entered re-orders the\nsequence mid-traversal, and a Shift+Tab that raises each window it\nenters (pushing it forward in the DOM, back the way the traversal came)\nwould revisit it forever. The pointer path syncs at gesture end instead,\nwhich is also the next safe point after any keyboard-session staleness."
            },
            {
              "kind": "field",
              "name": "_onPointerDown",
              "privacy": "private",
              "description": "Delegated pointerdown: raise the window the event originated in. Any\npress opens the gesture window that defers DOM reordering (see\nbringToFront) — a raise can be requested mid-gesture by this\npress or by code the press runs."
            },
            {
              "kind": "field",
              "name": "_onGestureEnd",
              "privacy": "private",
              "description": "The press ended (or was cancelled) — sync the DOM order. Unconditional\nrather than only-if-raised: the sync no-ops when order already agrees,\nand running it at every gesture end is what heals the staleness a\nkeyboard-only stretch leaves behind (focus-driven raises change z but\nnever move nodes — see _raise)."
            },
            {
              "kind": "field",
              "name": "_onFocusIn",
              "privacy": "private",
              "description": "Delegated focusin: raise the window keyboard focus entered, so tabbing\ninto a background window brings it to front (and reveals its close/zoom\nwidgets) just like a pointerdown would. Ignored while `_syncDomOrder` is\nputting focus back after a node move — the restore re-fires focusin on\nan element that may sit in a *background* window, and raising that\nwindow would undo the raise that triggered the sync."
            },
            {
              "kind": "method",
              "name": "_windowFromEvent",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "HTMLElement | undefined"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "The slotted window the event originated in, if any."
            },
            {
              "kind": "method",
              "name": "_onSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Wire up newly slotted windows: seed z-indices, normalize `active`."
            },
            {
              "kind": "method",
              "name": "_normalizeAfterUpgrade",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Re-assert the single-active-window state after slotted `<vf-window>`\nelements upgrade. One-shot: the flag collapses repeat slotchanges into a\nsingle wait, and once the definition is registered `_onSlotChange` stops\ncalling this at all. If a consumer never imports vf-window the promise\nsimply never settles — those elements render nothing either way."
            },
            {
              "kind": "method",
              "name": "_setActive",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "win",
                  "type": {
                    "text": "HTMLElement"
                  }
                }
              ],
              "description": "Set `active` on `win` only, clearing it on every other *document-tier*\nwindow. Utility windows stand outside the invariant: a palette keeps\nwhatever `active` state it has (true by default, so its dither and\nwidgets stay drawn while document windows trade the highlight)."
            },
            {
              "kind": "method",
              "name": "_setWindowActive",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "win",
                  "type": {
                    "text": "HTMLElement"
                  }
                },
                {
                  "name": "value",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Set a window's `active` state. Prefers the property (the source of truth\non an upgraded `vf-window`, whose reflection then wins over any pending\ninitial-value reflection); falls back to the attribute for not-yet\nupgraded elements."
            },
            {
              "kind": "method",
              "name": "_topmost",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "HTMLElement | null"
                }
              },
              "parameters": [
                {
                  "name": "windows",
                  "type": {
                    "text": "HTMLElement[]"
                  }
                }
              ],
              "description": "The window with the highest z-index, or null when there are none."
            },
            {
              "kind": "method",
              "name": "_deepActiveElement",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "Element | null"
                }
              },
              "description": "The innermost focused element, through open shadow roots."
            },
            {
              "kind": "method",
              "name": "_syncDomOrder",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Re-order the slotted windows in the light DOM to match their z-order\n(bottom-most first), so sequential focus navigation walks the stack the\nway the eye does — and Shift+Tab is its exact mirror. The utility band\nsorts the floating tier after every document window by construction.\n\nMinimal-move: windows already in relative order are never touched (the\ncommon case — after one raise, one window moves). Non-window siblings\n(a menu bar, page content) keep their positions; only a window that must\ncross the stack moves past them. Moving a node containing the focused\nelement drops focus to `<body>`, so it is restored afterwards — behind\n`_restoringFocus`, because the restore re-fires focusin (see\n_onFocusIn) on an element that may sit in a background window.\nOnly runs between pointer gestures or programmatically, never from a\nfocus-driven raise (see _raise for why)."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "width",
              "default": "DEFAULT_SCREEN_WIDTH",
              "description": "Screen width in system px — the raster's own size, the way a WIND\nresource declared a window's. The host box renders at exactly\n`width + 2 × bezel` system px, always whole; a desktop is never sized\nby page CSS — the page sets these numbers (directly or via\nfitWithin) and positions the explicitly sized box with its own\nstylesheet, keeping whatever sub-system-pixel slack its layout has on\nits side of the fence. Defaults to the compact Mac's 512.",
              "fieldName": "width"
            },
            {
              "name": "height",
              "default": "DEFAULT_SCREEN_HEIGHT",
              "description": "Screen height in system px; see width. Defaults to 342.",
              "fieldName": "height"
            },
            {
              "name": "bezel",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Width of the black screen bezel, in system px (`0` = none), added onto\nthe declared screen on every side — a `width=\"502\" bezel=\"5\"` desktop\nrenders a 512-system-px host box. The compact Mac's CRT showed an\nunlit black margin between the desktop's raster and the case; `bezel`\ndraws it around the screen and puts the classic screen-corner mask on\nthe screen's two *top* corners — only the top pair was rounded in the\nframebuffer. Flow, window coordinates and the drag clip all belong to\nthe screen, so windows crop at its edge. Inside a bezeled desktop a\nmenu bar needs no `rounded` of its own — the desktop's mask lands on\nthe same pixels.",
              "fieldName": "bezel"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-desktop",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfDesktop",
          "declaration": {
            "name": "VfDesktop",
            "module": "src/components/vf-desktop.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-desktop",
          "declaration": {
            "name": "VfDesktop",
            "module": "src/components/vf-desktop.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-dialog.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-dialog>` — the System 7 modal dialog shell.\n\nTwo chromes, one modal lifecycle (native `<dialog>` for top-layer rendering\nand focus trapping, with a fully transparent backdrop — no dimming):\n\n- **Default:** a striped title bar with a centered title over a white body —\n  the movable-modal look. Drag the title bar to move it. `closable` adds the\n  standard close box (left of the bar) — the HIG's own figures disagree on\n  whether a movable modal carries one (Figure 5-1 says yes, Figure 6-1 and\n  the Chapter 6 text say no), so the component enables either reading rather\n  than enforcing one.\n- **`frame=\"plain\"`:** the classic dBoxProc modal-dialog frame — 1px outer\n  border, 2px gap, 2px inner band, no shadow, no title bar — and immovable,\n  like the original. A `heading` renders as a centered display-face heading\n  at the top of the body (the reference art's \"Dialog title\"); `closable` is\n  ignored, there being no bar to carry the widget.\n\nOpen it with `show()` (or set the `open` attribute/property); close with\n`close()`. Escape closes it and fires `vf-close` with\n`{ reason: 'escape' }`; the close box and programmatic closing fire\n`{ reason: 'close' }`.",
          "name": "VfDialog",
          "cssProperties": [
            {
              "description": "the windoid bar's dot-grid dither — a 2×2 tile, one black pixel at the origin (`vfDots`; override the whole pattern like `--vf-desktop-pattern`)",
              "name": "--vf-dots-pattern"
            },
            {
              "description": "window/dialog title bars",
              "name": "--vf-titlebar-height"
            }
          ],
          "cssParts": [
            {
              "description": "The outer chrome frame (striped-bar or plain).",
              "name": "frame"
            },
            {
              "description": "The striped title bar (default chrome only).",
              "name": "title-bar"
            },
            {
              "description": "The centered title patch (or the plain-frame heading).",
              "name": "title"
            },
            {
              "description": "The close widget (`closable`, default chrome only).",
              "name": "close-box"
            },
            {
              "description": "The white content area.",
              "name": "body"
            },
            {
              "description": "The scrolling region inside the body (heading + slotted content, not the footer). Inert while the content fits; over-stuffed, it scrolls under a System 7 rail and becomes a keyboard stop.",
              "name": "content"
            },
            {
              "description": "The action row wrapping the buttons.",
              "name": "footer"
            },
            {
              "description": "The button group inside the footer.",
              "name": "buttons"
            }
          ],
          "slots": [
            {
              "description": "Default slot: dialog body content.",
              "name": ""
            },
            {
              "description": "Optional action buttons. Rendered as a bottom-right `vf-button-group` (equal-width, faces aligned); the footer only takes space when the slot is populated.",
              "name": "buttons"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "_drag",
              "privacy": "private",
              "readonly": true,
              "default": "new DragController(this, { onDragStart: (event: PointerEvent): { x: number; y: number } | null => { if (event.button !== 0) return null // Ignore drags that start on the close widget (same guard as vf-window). if ( event .composedPath() .some( (node) => node instanceof HTMLElement && node.classList.contains('box') ) ) { return null } const dialog = this._dialog if (!dialog?.open) return null // Seed from the grid-pinned margins written on open; fall back to the // live rect if a drag somehow precedes the pin. let x = parseFloat(dialog.style.marginLeft) let y = parseFloat(dialog.style.marginTop) if (Number.isNaN(x) || Number.isNaN(y)) { const rect = dialog.getBoundingClientRect() x = snapToSystemPx(rect.left, this) y = snapToSystemPx(rect.top, this) } return { x, y } }, onDrag: (x: number, y: number): void => { const dialog = this._dialog if (!dialog) return // Keep a grabbable strip on-screen, as vf-window does against its // positioning parent — the dialog's is the viewport. With a declared // width nothing else stops a drag at an edge; the box used to run out of // room and squeeze itself instead of moving. // Re-snap after clamping so the clamped edge still lands on the grid. const keep = sys(24, this) const nx = Math.min(Math.max(x, keep - dialog.offsetWidth), window.innerWidth - keep) const ny = Math.min(Math.max(y, 0), Math.max(0, window.innerHeight - keep)) dialog.style.marginLeft = `${snapToSystemPx(nx, this)}px` dialog.style.marginTop = `${snapToSystemPx(ny, this)}px` }, })",
              "description": "Title-bar drag-to-move (shared with `vf-window` via DragController).\nThe dialog is a centered top-layer `<dialog>` pinned onto the device grid\non open (snapDialogToGrid); dragging rewrites those centering margins with\nthe snapped new origin. `unsnapDialog` on close clears them so the next\nopen re-centers. Inert with `frame=\"plain\"` — no bar is rendered, so no\npointer ever reaches the controller (dBoxProc dialogs don't move)."
            },
            {
              "kind": "field",
              "name": "heading",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Title text: the bar's centered patch, or the plain frame's heading.",
              "attribute": "heading"
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the dialog (`aria-label`), for a dialog with no\n`heading` — an untitled title bar has no text to be named by. Ignored when\nempty and a `heading` is set (the title patch names the dialog then);\ndefaults to `'Dialog'` when neither is given.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "closable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Show the close box (left side of the title bar). Off by default — the\nbare movable-modal bar. Ignored with `frame=\"plain\"` (no bar). Clicking\nit closes the dialog and fires `vf-close` with `{ reason: 'close' }`.",
              "attribute": "closable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "frame",
              "type": {
                "text": "'plain' | undefined"
              },
              "description": "Frame chrome. Omit for the striped title bar (movable modal); `'plain'`\nfor the immovable dBoxProc double frame with no bar (modal dialog box).",
              "attribute": "frame",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "_hasButtons",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether the `buttons` slot has assigned content (drives the footer)."
            },
            {
              "kind": "field",
              "name": "_content",
              "type": {
                "text": "HTMLElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "_scrollable",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether the body content overflows its box (drives the content stop)."
            },
            {
              "kind": "field",
              "name": "_scrollState",
              "privacy": "private",
              "readonly": true,
              "default": "new ScrollStateController( this, () => this._content, undefined, (overflow) => { this._scrollable = overflow.y } )",
              "description": "Activates the System 7 rail — and the content region's keyboard stop —\nonce the body content overflows the declared (or viewport-capped) box."
            },
            {
              "kind": "method",
              "name": "_onBodySlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Content changed under the fixed box — re-measure the overflow."
            },
            {
              "kind": "method",
              "name": "_onCloseClick",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "_onButtonsSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ]
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "protected",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the modal is open. Kept in sync with the native `<dialog>`.",
              "attribute": "open",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | undefined"
              },
              "description": "Modal width in whole system px — the art's own unit, so the box holds its\nproportion to the chrome inside it at every display density.\n\n**Declare it.** A modal owns its width; it is not a shape its content\nhappens to fall into. The platform's own default is `fit-content` measured\nagainst the space *left over* beside the margins — and those margins are\nhow snapDialogToGrid pins the box and how a movable dialog is\ndragged, so an undeclared modal squeezes itself and reflows its text as it\nmoves toward an edge. Unset, it falls back to MODAL_FALLBACK_WIDTH\nand says so once in the console.",
              "attribute": "width",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | undefined"
              },
              "description": "Modal height in whole system px.\n\n**Declare it,** with width. A modal is a fixed box in both axes —\nthe same rule `vf-window` follows, for the same reason: the classic shells\ncarried both dimensions in the resource, and a box that grows with its body\nis one whose buttons move under the pointer as its text changes. Unset, the\nbody sizes to its content — the old behavior — and it says so once in the\nconsole.\n\nContent taller than the declared box is clipped at the frame.",
              "attribute": "height",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "_dialog",
              "type": {
                "text": "HTMLDialogElement"
              },
              "privacy": "protected",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "dialogSize",
              "type": {
                "text": "Record<string, string | null>"
              },
              "privacy": "protected",
              "description": "The declared box as inline styles for the subclass's `<dialog>`:\n`style=${styleMap(this.dialogSize)}`. Width always resolves to something\ndefinite — the declaration or the fallback — because that is what keeps a\ndrag from squeezing the box.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "#warnedNoSize",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "One warning per element, on the open that first shows an unsized modal.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "method",
              "name": "#warnIfUnsized",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Say it once, when an unsized modal is first shown — a hidden one is not\nyet anybody's problem, and the name in the message is what makes it\nfindable in a page with several.\n\nBoth dimensions are required, as on `vf-window`, but they fail differently:\nan undeclared width takes a definite fallback (a squeezing box is worse\nthan a wrong-but-stable one), while an undeclared height just sizes to the\ncontent and says so.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "#closeReason",
              "privacy": "private",
              "type": {
                "text": "VfCloseReason | null"
              },
              "default": "null",
              "description": "Close reason pending for the next native `close` event.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "#invoker",
              "privacy": "private",
              "type": {
                "text": "Element | null"
              },
              "default": "null",
              "description": "The element that was focused when the modal opened. The normal close path\nrestores focus natively, but the removal path (see the class doc) skips\nthe close algorithm, and by the time `disconnectedCallback` runs, focus\nhas already fallen to `<body>` — so we keep our own record.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "method",
              "name": "show",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Open the modal (native `showModal()`), pinned onto the device grid.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "method",
              "name": "close",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Close the modal. Fires `vf-close` with `{ reason: 'close' }`.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "method",
              "name": "#syncDialog",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Reconcile the native `<dialog>` with `open`. Opening pins the UA's\nauto-centering onto the device-pixel grid (half-pixel offsets fringe the\n1-bit chrome — see scale.ts). Closing just calls `dialog.close()`, routing\nteardown through the native `close` event so _onNativeClose is the\none place margins are cleared and `vf-close` is fired.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "#repin",
              "privacy": "private",
              "description": "Re-derive the pin rather than keep the latch: clear the pinned margins\n(restoring the UA's `margin: auto` centering) and re-pin from the live,\nre-centered rect. Without this the margins latched at open time forever —\nand the open-time rect can be wrong by the next frame: slotted content\nupgrading after `showModal()` grows the box (the pin then holds a\nviewport-tall dialog at the offset its small first render centered at,\nstranding its bottom off-screen), and a browser zoom rescales every\nmetric while the old margins hold the stale origin. A dragged position is\ntraded for re-centering on these signals — recoverable, where a stranded\nmodal with no drag handle (frame=\"plain\") is not.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "field",
              "name": "#resizeObserver",
              "privacy": "private",
              "type": {
                "text": "ResizeObserver | undefined"
              },
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "method",
              "name": "#watchGeometry",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "dialog",
                  "type": {
                    "text": "HTMLDialogElement"
                  }
                }
              ],
              "description": "While open, watch the two signals that invalidate the pin: the dialog's\nown box changing (content upgrade/growth, `--vf-scale` moving under a\nzoom or density change — every metric resizes with it) and the viewport\nresizing. Undone in _onNativeClose / `disconnectedCallback`.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "method",
              "name": "#unwatchGeometry",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "method",
              "name": "_onNativeCancel",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Native `cancel` (Escape): remember the reason; `close` follows.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "kind": "method",
              "name": "_onNativeClose",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Native `close` — the single teardown funnel for every close path. Clears\nthe grid-pinned margins so the next open re-centers, syncs `open`, and\nfires `vf-close` with the reason.",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            }
          ],
          "events": [
            {
              "description": "Dialog closed. Detail `{ reason: 'escape' | 'close' }`.",
              "name": "vf-close",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "heading",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Title text: the bar's centered patch, or the plain frame's heading.",
              "fieldName": "heading"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the dialog (`aria-label`), for a dialog with no\n`heading` — an untitled title bar has no text to be named by. Ignored when\nempty and a `heading` is set (the title patch names the dialog then);\ndefaults to `'Dialog'` when neither is given.",
              "fieldName": "label"
            },
            {
              "name": "closable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Show the close box (left side of the title bar). Off by default — the\nbare movable-modal bar. Ignored with `frame=\"plain\"` (no bar). Clicking\nit closes the dialog and fires `vf-close` with `{ reason: 'close' }`.",
              "fieldName": "closable"
            },
            {
              "name": "frame",
              "type": {
                "text": "'plain' | undefined"
              },
              "description": "Frame chrome. Omit for the striped title bar (movable modal); `'plain'`\nfor the immovable dBoxProc double frame with no bar (modal dialog box).",
              "fieldName": "frame"
            },
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the modal is open. Kept in sync with the native `<dialog>`.",
              "fieldName": "open",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "name": "width",
              "type": {
                "text": "number | undefined"
              },
              "description": "Modal width in whole system px — the art's own unit, so the box holds its\nproportion to the chrome inside it at every display density.\n\n**Declare it.** A modal owns its width; it is not a shape its content\nhappens to fall into. The platform's own default is `fit-content` measured\nagainst the space *left over* beside the margins — and those margins are\nhow snapDialogToGrid pins the box and how a movable dialog is\ndragged, so an undeclared modal squeezes itself and reflows its text as it\nmoves toward an edge. Unset, it falls back to MODAL_FALLBACK_WIDTH\nand says so once in the console.",
              "fieldName": "width",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            },
            {
              "name": "height",
              "type": {
                "text": "number | undefined"
              },
              "description": "Modal height in whole system px.\n\n**Declare it,** with width. A modal is a fixed box in both axes —\nthe same rule `vf-window` follows, for the same reason: the classic shells\ncarried both dimensions in the resource, and a box that grows with its body\nis one whose buttons move under the pointer as its text changes. Unset, the\nbody sizes to its content — the old behavior — and it says so once in the\nconsole.\n\nContent taller than the declared box is clipped at the frame.",
              "fieldName": "height",
              "inheritedFrom": {
                "name": "VfModalDialog",
                "module": "src/modal-dialog.ts"
              }
            }
          ],
          "superclass": {
            "name": "VfModalDialog",
            "module": "/src/modal-dialog.js"
          },
          "tagName": "vf-dialog",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfDialog",
          "declaration": {
            "name": "VfDialog",
            "module": "src/components/vf-dialog.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-dialog",
          "declaration": {
            "name": "VfDialog",
            "module": "src/components/vf-dialog.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-fieldset.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-fieldset>` — the classic \"Install Location\" group box.\n\nA 1px black rectangle whose bold legend sits on the top border, punching a\ngap through it with a `var(--vf-surface, ...)` background patch — so it\nmatches whatever surface sits behind it.",
          "name": "VfFieldset",
          "cssProperties": [
            {
              "description": "bg behind legends/label patches; `vf-window` and `vf-dialog` both set it to white",
              "name": "--vf-surface",
              "default": "..."
            }
          ],
          "cssParts": [
            {
              "description": "The bordered box.",
              "name": "fieldset"
            },
            {
              "description": "The legend patch on the top border.",
              "name": "legend"
            }
          ],
          "slots": [
            {
              "description": "Group contents.",
              "name": ""
            },
            {
              "description": "Rich legend content; overrides the `legend` attribute.",
              "name": "legend"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "legend",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Legend text shown on the top border.",
              "attribute": "legend"
            },
            {
              "kind": "field",
              "name": "_hasSlottedLegend",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether the named `legend` slot has assigned content."
            },
            {
              "kind": "method",
              "name": "#onLegendSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ]
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "legend",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Legend text shown on the top border.",
              "fieldName": "legend"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-fieldset",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfFieldset",
          "declaration": {
            "name": "VfFieldset",
            "module": "src/components/vf-fieldset.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-fieldset",
          "declaration": {
            "name": "VfFieldset",
            "module": "src/components/vf-fieldset.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-grid.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-grid>` — a lattice of equal cells separated by 1px rules.\n\nThe reference screens are full of these: the Figure 5-6 tool palette's 3×3\nof desk accessories, a color picker's swatch table, a pattern or icon\nchooser. They are all the same drawing — fixed-size cells butted together\nwith a hairline between them — and every one of them used to be hand-rolled\npage CSS: a `display: grid` with a 1px gap over a black background, so the\nbackground showed through as the rules. That trick only draws *solid*\ninterior rules, has to restate the cell size in three places, and puts the\nkit's own artwork in the consumer's stylesheet.\n\nThis draws it instead. State the cell count and cell size in whole system px\nand slot the cells in; the component owns the track geometry and the rules:\n\n```html\n<vf-grid columns=\"3\" rows=\"3\" cell-width=\"26\" cell-height=\"26\">\n  <button>…</button>  <!-- nine of them, one per cell -->\n</vf-grid>\n```\n\n**The rules are a masked lattice, not gaps.** The grid's 1px gaps make the\nroom; a single overlay paints `--vf-black` through two tiled masks — one\ncolumn of pixels at the horizontal pitch, one row at the vertical — so the\npen switches from `solid` to `dashed` (1 on, 1 off) without the layout\nmoving a pixel. It paints *under* the cells (`z-index: -1` inside the grid's\nown stacking context, above its surface), so an item that spans two cells\ncovers the boundary it swallowed, and a cell's own ink — a pressed face, a\nfocus rule, a hard shadow — is never crossed by a line.\n\n**Cells are centered in their wells.** What a palette holds is normally\nsmaller than the well it sits in — a 16px icon in a 26px cell — so a slotted\nitem is centered on both axes rather than parked in the corner, which is\nwhere grid's own `stretch` default leaves anything carrying its own size. A\ncell that means to fill its well — a tool button that inverts black when\nselected, a tile whose border merges with the lattice — says\n`place-self: stretch` and gets the whole track. (Under `collapse` that is\nalready the default: those cells exist to meet the lines.)\n\n**The perimeter is part of the lattice**, and drawn by default: the box\ncarries the 1px the outer lines need, and one uninterrupted pitch runs from\nedge to edge. `frameless` drops it, for a grid inside something that already\ndraws that line — the desk-accessory palette is a `vf-window[flush]`, whose\nframe *is* the grid's outer border, and a doubled one would read as 2px.\n\n**`collapse` puts an item's own border on the rule.** A cell that draws its\nown 1px border — a bordered tile, a 16×16 swatch — otherwise sets that border\n*beside* the lattice line, and every boundary reads 2px thick. `collapse`\npulls each cell back one pixel on all four sides, so the two become one line:\nthe `border-collapse: collapse` of a table, in system px. Size the cells at\n*item − 2px* — `cell-width=\"14\"` for a 16px item — and each item then spans\nlattice line to lattice line, sharing one with each neighbor:\n\n```html\n<vf-grid columns=\"8\" rows=\"4\" cell-width=\"14\" cell-height=\"14\" collapse>\n```\n\nThe frame is what gives the outermost borders a line of their own to land on,\nand it ends the box exactly at the last item's outer edge; go `frameless` and\nthey paint a pixel outside the grid's box instead.\n\n`rules=\"none\"` is the pen, not the lattice: nothing is drawn, the gaps close\nup, and the cells butt directly together (there is then no perimeter and\nnothing for `collapse` to collapse onto, so it goes inert).\n\n**Semantics are the consumer's.** The grid is layout — it takes no role, no\nkeyboard behavior and no selection, so what it holds decides what it is: put\n`role=\"group\"`/`aria-label` on the host for a tool palette, `role=\"radiogroup\"`\nfor a picker, or nothing at all for a plain tiling. Slotted cells stay in the\nlight DOM with their own semantics intact.",
          "name": "VfGrid",
          "cssProperties": [
            {
              "description": "The surface behind the cells (default white).",
              "name": "--vf-surface"
            }
          ],
          "cssParts": [
            {
              "description": "The grid box itself (surface, tracks, frame padding).",
              "name": "grid"
            },
            {
              "description": "The lattice overlay painting the rules.",
              "name": "rules"
            }
          ],
          "slots": [
            {
              "description": "The cells, in order — one element per cell.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "columns",
              "type": {
                "text": "number"
              },
              "default": "1",
              "description": "Cells across the x axis.",
              "attribute": "columns"
            },
            {
              "kind": "field",
              "name": "rows",
              "type": {
                "text": "number | undefined"
              },
              "description": "Cells down the y axis. Left unset the grid takes as many rows as the\nslotted cells need; set, it reserves that many — an unfilled cell still\ngets its rules, the way a palette keeps its empty wells.",
              "attribute": "rows"
            },
            {
              "kind": "field",
              "name": "cellWidth",
              "type": {
                "text": "number"
              },
              "default": "16",
              "description": "Cell width in whole system px.",
              "attribute": "cell-width"
            },
            {
              "kind": "field",
              "name": "cellHeight",
              "type": {
                "text": "number"
              },
              "default": "16",
              "description": "Cell height in whole system px.",
              "attribute": "cell-height"
            },
            {
              "kind": "field",
              "name": "rules",
              "type": {
                "text": "VfGridRules"
              },
              "default": "'solid'",
              "description": "The pen every rule is drawn with: `solid` (default), `dashed` (1 system px\non, 1 off), or `none` — no rules at all, and the cells butt together.",
              "attribute": "rules",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "frameless",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Drop the outer border, leaving only the boundaries between cells. For a\ngrid inside something that already draws that line — a `vf-window[flush]`,\na `vf-fieldset` — where a second one would read as 2px.",
              "attribute": "frameless",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "collapse",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Collapse each cell's own 1px border onto the grid's rules instead of\nsetting it beside them (see the class doc). Size the cells at *item − 2px*;\nthe frame is what gives the outermost borders a line to land on, so a\n`frameless` grid lets them paint a pixel outside its box.",
              "attribute": "collapse",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "columns",
              "type": {
                "text": "number"
              },
              "default": "1",
              "description": "Cells across the x axis.",
              "fieldName": "columns"
            },
            {
              "name": "rows",
              "type": {
                "text": "number | undefined"
              },
              "description": "Cells down the y axis. Left unset the grid takes as many rows as the\nslotted cells need; set, it reserves that many — an unfilled cell still\ngets its rules, the way a palette keeps its empty wells.",
              "fieldName": "rows"
            },
            {
              "name": "cell-width",
              "type": {
                "text": "number"
              },
              "default": "16",
              "description": "Cell width in whole system px.",
              "fieldName": "cellWidth"
            },
            {
              "name": "cell-height",
              "type": {
                "text": "number"
              },
              "default": "16",
              "description": "Cell height in whole system px.",
              "fieldName": "cellHeight"
            },
            {
              "name": "rules",
              "type": {
                "text": "VfGridRules"
              },
              "default": "'solid'",
              "description": "The pen every rule is drawn with: `solid` (default), `dashed` (1 system px\non, 1 off), or `none` — no rules at all, and the cells butt together.",
              "fieldName": "rules"
            },
            {
              "name": "frameless",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Drop the outer border, leaving only the boundaries between cells. For a\ngrid inside something that already draws that line — a `vf-window[flush]`,\na `vf-fieldset` — where a second one would read as 2px.",
              "fieldName": "frameless"
            },
            {
              "name": "collapse",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Collapse each cell's own 1px border onto the grid's rules instead of\nsetting it beside them (see the class doc). Size the cells at *item − 2px*;\nthe frame is what gives the outermost borders a line to land on, so a\n`frameless` grid lets them paint a pixel outside its box.",
              "fieldName": "collapse"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-grid",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfGrid",
          "declaration": {
            "name": "VfGrid",
            "module": "src/components/vf-grid.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-grid",
          "declaration": {
            "name": "VfGrid",
            "module": "src/components/vf-grid.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-icon.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-icon>` — a Finder icon: pixel art in a reserved cell with its name on a\nplate below, selectable, movable and renameable in place.\n\nIt is the one composite in the kit whose parts already existed separately —\n`vf-img` draws the art, `vf-list-item` inverts on selection, `vf-window`\ndrags — and the reason to have it anyway is that the *icon* is the unit the\nFinder actually manipulates. A picture and a caption that select together,\nmove together and rename together is a control, not a layout.\n\n### The art arrives by slot\n\nTwo slots, one per resource size — `large` is the 32×32 `ICN#` and `small`\nthe 16×16 `ics#` — each holding a `vf-img` around the consumer's own `<img>`:\n\n```html\n<vf-icon label=\"Macintosh HD\" selectable movable editable>\n  <vf-img slot=\"large\"><img src=\"hd-32.png\" alt=\"\"></vf-img>\n  <vf-img slot=\"small\"><img src=\"hd-16.png\" alt=\"\"></vf-img>\n</vf-icon>\n```\n\nSlots rather than `src` properties, for the reason `vf-img` exists at all:\nthe kit ships no raster files and never builds an `<img>` on a consumer's\nbehalf, so the graphic stays a real element in the light DOM with its own\n`alt`, `srcset`, loading behavior and asset URLs. A `src` string can express\nnone of that, and it could not hold an inline `<svg>` or a `<canvas>` either.\n`vf-list-item`'s `icon` slot makes the same trade.\n\nThe cost is that both files fetch even though one paints — pay it with two\ndata URIs, or slot only the size that view uses.\n\n### The cell is reserved, the art is registered in it\n\n`size` picks which slot paints *and* the cell it paints in: 32×32 or 16×16,\nheld whether or not there is art for it. A folder is 32×20 of ink and a\ndocument 25×32, and a row of them keeps one baseline only because the cell —\nnot the ink — is the unit, which is what an icon resource always was.\n`scripts/extract-da-icons.py` bakes the same registration into the assets it\ncuts.\n\n### Everything centered lands on a whole pixel, by parity\n\nThe frame centers two things over one axis — the art cell and the name — and\na centered child sits at `(box − child) / 2`. That is a whole number exactly\nwhen the box and the child have the **same parity**. Half a system pixel is\nwhat fringes 1-bit art: the glyph stems smear across two device columns and\ngo gray, while the plate behind them stays sharp, because backgrounds are\npixel-snapped by the compositor and glyphs are not. A crisp plate under a\ngrey name is the signature of exactly this.\n\nSo the component makes the parities agree instead of correcting afterwards.\nThe cell is 32 or 16, and #measurePlate sizes the name plate to a\nwhole **even** number of system px — measured from the text, rounded up. Both\nchildren are then even, and so is every offset, at every display density and\nwhether or not a `width` is declared. Nothing is snapped, nothing leans on\nthe rasterizer, and `verify:icon` asserts both the geometry and that a field\nof names renders without a single gray pixel — with the kit's normal\nantialiasing left on, since a run on whole pixels gives it nothing to smooth.\n\nYour side of it is one rule: **a declared `width` must be even**, since the\ncomponent cannot round a number you chose. Supply art at the cell size too —\nwhich \"one image pixel is one system pixel\" already implies — because art\ndiffering from its cell by an odd number centers the same way.\n\n### Selection inverts, because the art is a 1-bit mask\n\nA System 7 icon is black ink and opaque white on a transparent surround —\nprecisely an image plus its mask — so inverting it flips ink and fill and\nleaves the surround alone, which is the whole of the classic selected\nappearance. `filter: invert(1)` reproduces it exactly for 1-bit art. Color\nart inverts too, into a photographic negative rather than the darkening\nSystem 7 gave it; that is the case to revisit if the kit ever grows a\nselected-state treatment of its own. The label plate inverts to the\n`--vf-highlight` pair, sharing one selection color with `vf-list-item`.\n\n### Open is derived, not shipped\n\nWith `open`, the art redraws as the Finder's open ghost: the outline held\nin solid black, the interior re-filled with the kit's loose 25% dither (the\nscrollbar trough's lattice), the transparent surround untouched. There is\nno second raster and no second fetch — the same alpha channel that makes\nselection an inversion makes the ghost derivable, and `src/open-art.ts`\nderives it from the slotted art by canvas compositing alone. No pixels are\never read back, so a cross-origin image that taints its canvas still works:\ntaint forbids reading, not drawing or displaying.\n\nThe ghost keeps the shape selection expects — ink and opaque white on a\ntransparent surround — so a selected open icon inverts exactly as a closed\none does, with no second treatment. The slot stays in the tree while the\nghost paints, hidden (it is where the art loads, and re-loads, from), and\nart the pipeline cannot draw — nothing slotted yet, a failed load, an\ninline `<svg>` — keeps rendering as itself rather than vanishing behind a\nstate it cannot show.\n\n### `movable`, not `draggable`\n\n`draggable` is a global HTML attribute *and* an `HTMLElement` accessor, so\ndeclaring it would both shadow a platform member and hand the element to the\nbrowser's own drag-and-drop machinery. This is the `align` trap from\n`vf-stack` in a second costume — `align`, `hidden`, `dir`, `draggable` and\n`title` all carry behavior a custom element never asked for. The kit already\nspells this parameter `movable` on `vf-window`, so the icon does too, and it\nmoves the same way: `DragController` seeds absolute positioning from the\nin-flow offset on the first drag and writes back `left`/`top` snapped onto\nthe system-pixel grid, whole art pixels the way QuickDraw moved things.\n\nDragging is a pointer gesture with no keyboard equivalent, which is the kind\nof gap the kit closes rather than inherits (SPEC §1): a focused movable icon\nalso moves under the arrow keys, one system px at a time and eight with\nShift.\n\nOpening gets the same treatment. The double-click is the pointer gesture,\nand its keyboard route is ⌘O / ⌘↓ — the System 7 Open shortcuts, with Ctrl\nstanding in for ⌘ off the Mac. Return is deliberately not one of them: the\nFinder's Return renamed, never opened, so on an editable icon it starts the\nedit and on a non-editable one it does nothing at all.\n\n### The label is a property, because it is editable\n\nThe caption is `label` rather than slotted content: renaming means the\ncomponent owns the string and hands it back on `vf-change`, and it cannot own\ntext that lives in the consumer's DOM. An empty `label` draws no plate at\nall — that *is* the \"no label\" parameter, in preference to a second attribute\nthat could disagree with it. `editable` then lets a click on the plate of an\nalready-selected icon open the rename box, as the Finder's does, with Return\ncommitting, Escape reverting, and the plate widening as you type.\n\n### A name is never abbreviated, and never folded\n\nThere is no ellipsis, no clipping and no wrapping: one line, always. System 7\nsolved the long-name problem at the *other* end — HFS capped a filename at 31\ncharacters — so the Finder could afford to always draw the name in full, and\ndid. A name wider than its cell simply overflows it, centered, the way a name\nwider than a 32-pixel icon always did.\n\nStaying on one line is also what keeps the name on the grid: a single run has\na single measured width, so the even-plate rule above is one number rather\nthan one per line, each with its own parity. `width` is therefore the cell —\nthe grid pitch — not a bound on the name.\n\n`maxlength` (31) bounds the rename field rather than the `label` property:\nthe name belongs to the consumer's model, and truncating one handed to us\nwould lose data.\n\nThe box hugs its text while you type it, not only once you are done, which is\nwhat keeps the name from moving when the edit commits: the plate is the same\nwidth either side of it, so the glyphs stay exactly where they were.\n\nA file also has to be called *something*, so a rename committed empty — or\nas nothing but spaces — is refused rather than applied, and the previous name\ncomes back, which is what System 7 did. An empty `label` is still a perfectly\ngood state to *start* in, though: a freshly made icon has no name until it is\ngiven one. Such an icon draws no plate but stays selectable, focusable and\nrenameable — Return opens a field, and an edit with nothing in it yet is the\none time the box stops hugging and reserves a cell's width, since a field you\ncannot see is one you cannot type into.",
          "name": "VfIcon",
          "cssProperties": [
            {
              "description": "Space between the art cell and the name plate",
              "name": "--vf-icon-gap"
            },
            {
              "description": "The name plate's line box",
              "name": "--vf-icon-label-height"
            }
          ],
          "cssParts": [
            {
              "description": "The icon and its label plate, stacked.",
              "name": "frame"
            },
            {
              "description": "The reserved art cell.",
              "name": "icon"
            },
            {
              "description": "The name block; its lines are centered under the art.",
              "name": "label"
            },
            {
              "description": "The inked run behind the name, which each wrapped line gets its own of (inverts when selected).",
              "name": "plate"
            },
            {
              "description": "The rename field, while editing.",
              "name": "input"
            }
          ],
          "slots": [
            {
              "description": "The 32×32 art, normally a `vf-img` around an `<img>`.",
              "name": "large"
            },
            {
              "description": "The 16×16 art, shown under `size=\"small\"`.",
              "name": "small"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The icon's name. Empty draws no plate — that is the \"no label\" setting.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "size",
              "type": {
                "text": "VfIconSize"
              },
              "default": "'large'",
              "description": "Which member of the icon family paints, and the cell it is registered in:\n`large` (32×32) or `small` (16×16).",
              "attribute": "size",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "selectable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Clicking selects. Set `selected` yourself to drive selection some other way.",
              "attribute": "selectable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "selected",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the icon is selected: the art inverts and the plate goes black.",
              "attribute": "selected",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "The icon's window is on screen, so the art paints as the Finder's open\nghost — outline held, interior re-filled with the kit's loose dither —\nderived in the client from the slotted art itself (see the class doc).\nSet it when handling `vf-open`, clear it when the window goes away.\nSelection inverts the ghost exactly as it inverts the art.",
              "attribute": "open",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "movable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Drag to move — `movable`, never `draggable`, which is a platform attribute\nand accessor (see the class doc). Arrow keys move a focused icon too.",
              "attribute": "movable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "editable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "The name can be renamed in place: click a selected plate, or press Return.",
              "attribute": "editable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | undefined"
              },
              "description": "The cell width in whole system px — the grid pitch, and it must be\n**even**: the component rounds its own plate to keep every centered offset\nwhole, but it cannot round a number you chose (see the class doc).\n\nIt is not a bound on the name. Left off, the box is as wide as the wider of\nthe art cell and the name; set, a longer name overflows it, centered.\nDeclaring one is what keeps a field of icons on a single pitch, and what\nkeeps a text-sized box off a fractional origin (contract rule 3).",
              "attribute": "width"
            },
            {
              "kind": "field",
              "name": "maxlength",
              "type": {
                "text": "number"
              },
              "default": "31",
              "description": "The most characters the rename field will accept — 31, the HFS filename\nlimit every System 7 name was cut to, which is also why the Finder could\nafford to always draw the whole name. Going past it fires\n`vf-name-too-long` with what was tried, so a host can say so rather than\nlet characters vanish.\n\nIt bounds *typing* only: a `label` set from your own data is displayed as\ngiven and fires nothing, since the name belongs to your model and silently\ntruncating it would lose data.",
              "attribute": "maxlength"
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "focusRule",
              "privacy": "private",
              "readonly": true,
              "default": "new FocusRuleController(this)",
              "description": "Whether to wear the dashed focus rule; see src/focus-modality.ts."
            },
            {
              "kind": "field",
              "name": "_editing",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "True while the rename field is open."
            },
            {
              "kind": "field",
              "name": "_draft",
              "type": {
                "text": "string"
              },
              "privacy": "private",
              "default": "''",
              "description": "The name as typed. The hidden plate renders it, so the box tracks it."
            },
            {
              "kind": "field",
              "name": "_plateWidth",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "default": "0",
              "description": "The plate's width in whole, EVEN system px — 0 until the text is measured.\nSee #measurePlate; this is what keeps every name on the grid."
            },
            {
              "kind": "field",
              "name": "_ghost",
              "type": {
                "text": "HTMLCanvasElement | null"
              },
              "privacy": "private",
              "default": "null",
              "description": "The derived open ghost, or null while there is nothing to derive from."
            },
            {
              "kind": "field",
              "name": "#art",
              "privacy": "private",
              "type": {
                "text": "HTMLImageElement | HTMLCanvasElement | null"
              },
              "default": "null",
              "description": "The art element the ghost derives from, so a rehome moves the listener."
            },
            {
              "kind": "field",
              "name": "#ghostKey",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "What the current ghost was derived from, so a no-op refresh is free."
            },
            {
              "kind": "field",
              "name": "#committed",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The name editing started from, for Escape to put back."
            },
            {
              "kind": "field",
              "name": "#outside",
              "privacy": "private",
              "readonly": true,
              "default": "new DocumentListenersController(this, () => [ [document, 'pointerdown', this.#onOutsidePointerDown, true], ])",
              "description": "Clicking elsewhere deselects, the way it does on a real desktop. Attached\nonly while this icon is both selectable and selected, so an unselected\nfield of icons costs nothing. Capture phase, so a handler that stops the\npress still deselects — and so the icon being clicked *into* selects after\nthis runs, which is what makes plain clicks single-select with no container\nmanaging the set. A container that wants multi-select suppresses it by\nowning `selected` and leaving `selectable` off."
            },
            {
              "kind": "field",
              "name": "#drag",
              "privacy": "private",
              "readonly": true,
              "default": "new DragController(this, { onDragStart: (event: PointerEvent): { x: number; y: number } | null => { if (!this.movable || event.button !== 0 || this._editing) return null return this.#seedPosition() }, onDrag: (x: number, y: number): void => this.#moveTo(x, y), })",
              "description": "Drag-to-move, on the same delegate shape as `vf-window`: seed absolute\npositioning from the in-flow offset on the first drag, then write back the\norigin the controller has already snapped onto the system-pixel grid."
            },
            {
              "kind": "method",
              "name": "#measurePlate",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Size the plate to a whole, EVEN number of system pixels.\n\nThis is the whole alignment argument, so it is worth stating plainly. The\nframe centers two things over one axis — the art cell and the name — and a\ncentered child's offset is `(box − child) / 2`. That is a whole number\nexactly when the box and the child have the SAME PARITY. The cell is 32 or\n16 and a declared `width` is required even, so if the plate is even too,\nevery offset in the component is whole by construction: no correction, no\nsnapping, nothing for the compositor to round.\n\nThe text is measured rather than the plate, with a Range over its own\ncontents — the plate already carries the width this computed last time, so\nreading the element back would just return it. The measurement is in\nsystem px and so is scale-invariant: the same name is the same number of\nart pixels wide at every display density, which is why nothing here\nre-runs when the scale changes.\n\nRounding UP to even (never down) means the plate never clips the run it\nwas measured from; the leftover is at most one system pixel of extra plate\nafter the text, since the embedded faces advance in whole pixels and only\na fallback face measures fractionally."
            },
            {
              "kind": "field",
              "name": "#onArtSlotChange",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onArtSettled",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "#trackArt",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Follow the active slot's art element and keep the ghost derived from it.\nThe art is normally an `<img>` inside a slotted `vf-img`, but a bare\n`<img>` or a `<canvas>` (slotted directly or wrapped) derives the same.\nThe `load` listener lives on an element inside this host's own light DOM,\nso it cannot outlive us (the `vf-img` reasoning), and it re-fires on a\n`src` swap — the re-derivation a swapped icon needs."
            },
            {
              "kind": "method",
              "name": "#refreshGhost",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Derive (or drop) the open ghost. Keyed on the art's current source so the\nusual refresh — a slotchange or load that changed nothing — costs a\nstring compare; a `<canvas>` source has no such fingerprint and\nre-derives each time, which at icon sizes is a handful of composited\ndraws."
            },
            {
              "kind": "method",
              "name": "startEditing",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Open the rename field. The whole name starts selected, as the Finder's did\n— the common rename is a replacement, not an edit."
            },
            {
              "kind": "method",
              "name": "commitEditing",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Keep the typed name and close the field — unless there is no name left, in\nwhich case the edit is refused and the old one comes back.\n\nA file has to be called something: System 7 would not let you commit an\nempty name, and this does the same, treating a run of nothing but spaces\nas empty too (the plate would read as blank either way). The refusal is\nreported as `vf-name-rejected` for the same reason `vf-name-too-long` is —\nso a host can say why instead of leaving the name to silently snap back."
            },
            {
              "kind": "method",
              "name": "cancelEditing",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Put the previous name back and close the field."
            },
            {
              "kind": "field",
              "name": "#interactive",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "description": "Focusable exactly when there is something to do with it.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#syncTabIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#setSelected",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "next",
                  "type": {
                    "text": "boolean"
                  }
                }
              ]
            },
            {
              "kind": "field",
              "name": "#onPointerDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onDoubleClick",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onOutsidePointerDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onBeforeInput",
              "privacy": "private",
              "description": "Report a name the limit will not take, before the field refuses it.\n\n`beforeinput` rather than `input`, because by the time `input` fires the\nattempt is gone: with `maxlength` on the field a blocked keystroke fires\n`beforeinput` and then NO `input` at all, and an overshooting paste\nreaches `input` already trimmed to fit. Only the \"about to change\" event\nstill knows what was tried, which is the thing an integrator wants to put\nin an alert — as System 7 did, rather than silently dropping characters."
            },
            {
              "kind": "field",
              "name": "#onInput",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "#reportTooLong",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "attempted",
                  "type": {
                    "text": "string"
                  }
                }
              ]
            },
            {
              "kind": "field",
              "name": "#onInputKeyDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onKeyDown",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "#seedPosition",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "{ x: number; y: number }"
                }
              },
              "description": "The origin future moves add to, in the host's own `left`/`top` space.\nSeeds absolute positioning from the in-flow offset the first time, then\nre-reads the COMPUTED coordinates each time after — an author's `left: 10%`\nis a perfectly good way to place an icon, and reading `style.left` back\nwould parse it as a bare number and jump. (Same delegate as `vf-window`.)"
            },
            {
              "kind": "method",
              "name": "#moveTo",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "x",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "y",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Write a new origin, kept inside the positioning parent and re-snapped onto\nthe system-pixel grid after the clamp, so the clamped edge is a whole art\npixel too. Unlike `vf-window`, which only keeps a grabbable strip on\nscreen, an icon is small enough to hold whole — losing half of one to an\nedge reads as a bug rather than as a window pushed aside."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "events": [
            {
              "description": "Selection changed by user interaction. `detail: { selected: boolean }`.",
              "name": "vf-select"
            },
            {
              "description": "The name was committed. `detail: { label: string, previous: string }`.",
              "name": "vf-change"
            },
            {
              "description": "The icon was opened — double-clicked, or ⌘O / ⌘↓ from the keyboard (Ctrl off the Mac), the System 7 shortcuts. Return renames instead, as the Finder's did. `detail: {}`.",
              "name": "vf-open"
            },
            {
              "description": "A rename was typed or pasted past `maxlength`, and the field refused the excess. `detail: { attempted, accepted, limit }` — enough to raise the alert System 7 raised rather than drop the characters silently. Not fired for a `label` set from your own code, which is never truncated in the first place.",
              "name": "vf-name-too-long"
            },
            {
              "description": "A rename was committed with no name in it, so the edit was dropped and the old name put back. `detail: { attempted, kept, reason: 'empty' }`. A `vf-change` is *not* fired alongside it — nothing changed.",
              "name": "vf-name-rejected"
            }
          ],
          "attributes": [
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The icon's name. Empty draws no plate — that is the \"no label\" setting.",
              "fieldName": "label"
            },
            {
              "name": "size",
              "type": {
                "text": "VfIconSize"
              },
              "default": "'large'",
              "description": "Which member of the icon family paints, and the cell it is registered in:\n`large` (32×32) or `small` (16×16).",
              "fieldName": "size"
            },
            {
              "name": "selectable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Clicking selects. Set `selected` yourself to drive selection some other way.",
              "fieldName": "selectable"
            },
            {
              "name": "selected",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the icon is selected: the art inverts and the plate goes black.",
              "fieldName": "selected"
            },
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "The icon's window is on screen, so the art paints as the Finder's open\nghost — outline held, interior re-filled with the kit's loose dither —\nderived in the client from the slotted art itself (see the class doc).\nSet it when handling `vf-open`, clear it when the window goes away.\nSelection inverts the ghost exactly as it inverts the art.",
              "fieldName": "open"
            },
            {
              "name": "movable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Drag to move — `movable`, never `draggable`, which is a platform attribute\nand accessor (see the class doc). Arrow keys move a focused icon too.",
              "fieldName": "movable"
            },
            {
              "name": "editable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "The name can be renamed in place: click a selected plate, or press Return.",
              "fieldName": "editable"
            },
            {
              "name": "width",
              "type": {
                "text": "number | undefined"
              },
              "description": "The cell width in whole system px — the grid pitch, and it must be\n**even**: the component rounds its own plate to keep every centered offset\nwhole, but it cannot round a number you chose (see the class doc).\n\nIt is not a bound on the name. Left off, the box is as wide as the wider of\nthe art cell and the name; set, a longer name overflows it, centered.\nDeclaring one is what keeps a field of icons on a single pitch, and what\nkeeps a text-sized box off a fractional origin (contract rule 3).",
              "fieldName": "width"
            },
            {
              "name": "maxlength",
              "type": {
                "text": "number"
              },
              "default": "31",
              "description": "The most characters the rename field will accept — 31, the HFS filename\nlimit every System 7 name was cut to, which is also why the Finder could\nafford to always draw the whole name. Going past it fires\n`vf-name-too-long` with what was tried, so a host can say so rather than\nlet characters vanish.\n\nIt bounds *typing* only: a `label` set from your own data is displayed as\ngiven and fires nothing, since the name belongs to your model and silently\ntruncating it would lose data.",
              "fieldName": "maxlength"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-icon",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfIcon",
          "declaration": {
            "name": "VfIcon",
            "module": "src/components/vf-icon.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-icon",
          "declaration": {
            "name": "VfIcon",
            "module": "src/components/vf-icon.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-img.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-img>` — a raster image on the kit's pixel grid.\n\nThe reference art is full of small pixel graphics that are *pictures*, not\nglyphs: the Apple menu's logo, desk-accessory and document icons, the\nabout-box machine portrait. This component displays one — a slotted native\n`<img>` — at the kit's own magnification, treating **one image pixel as one\nsystem pixel**:\n\n- the image is sized to `naturalWidth × naturalHeight` system px, multiplied\n  by `--vf-scale` in `calc()` like every other metric, so a source pixel\n  covers exactly `scale × dpr` device pixels (a whole number, per layout\n  contract rule 1);\n- `image-rendering: pixelated` keeps the resampling nearest-neighbor, and on\n  a whole-device-pixel box that is bit-exact magnification — no smoothing,\n  no half-covered edge rows;\n- its own GridSnapController, so the image's origin holds the device\n  grid wherever the page puts it (a fractional origin would slide the whole\n  box across device pixels and shear every source pixel's coverage).\n\nThe graphic stays a real `<img>` in the consumer's light DOM — it keeps\nnative loading, `alt` text (use `alt=\"\"` for a decorative icon) and the\nconsumer's own asset URLs; the kit ships no raster files. `width`/`height`\n(in system px, whole numbers) state the box up front so the page doesn't\nshift when the file lands — until a size is known the box is 0×0, never a\nflash of the image at some other scale. They also override the natural size\ndeliberately: a whole multiple (`width=\"32\"` on 16-px art) magnifies on the\nsame grid.",
          "name": "VfImg",
          "cssParts": [
            {
              "description": "The sized box the image fills.",
              "name": "frame"
            }
          ],
          "slots": [
            {
              "description": "The image: a single `<img>` element.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | undefined"
              },
              "description": "The displayed width in system px — a whole number. Defaults to the slotted\nimage's `naturalWidth` (one image px = one system px); state it explicitly\nto reserve the box before the file loads, or a whole multiple to magnify.",
              "attribute": "width"
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | undefined"
              },
              "description": "The displayed height in system px; see width.",
              "attribute": "height"
            },
            {
              "kind": "field",
              "name": "_naturalWidth",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "default": "0",
              "description": "The slotted image's intrinsic size, 0 until it has one."
            },
            {
              "kind": "field",
              "name": "_naturalHeight",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "default": "0"
            },
            {
              "kind": "field",
              "name": "_failed",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "True when the slotted image has settled with no natural size (a failed\nload); see #measure."
            },
            {
              "kind": "field",
              "name": "#img",
              "privacy": "private",
              "type": {
                "text": "HTMLImageElement | null"
              },
              "default": "null",
              "description": "The image whose load we're watching, so a rehome moves the listener."
            },
            {
              "kind": "field",
              "name": "#onSlotChange",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onImgSettled",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "#measure",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Natural size if the image has one; 0×0 (an empty box) otherwise."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "width",
              "type": {
                "text": "number | undefined"
              },
              "description": "The displayed width in system px — a whole number. Defaults to the slotted\nimage's `naturalWidth` (one image px = one system px); state it explicitly\nto reserve the box before the file loads, or a whole multiple to magnify.",
              "fieldName": "width"
            },
            {
              "name": "height",
              "type": {
                "text": "number | undefined"
              },
              "description": "The displayed height in system px; see width.",
              "fieldName": "height"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-img",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfImg",
          "declaration": {
            "name": "VfImg",
            "module": "src/components/vf-img.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-img",
          "declaration": {
            "name": "VfImg",
            "module": "src/components/vf-img.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-label.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-label>` — a static System 7 caption.\n\nThe \"Name:\" beside a field, the \"Mode\" heading over a group of radios, a\nnumeric readout beside a slider. Text the page was previously setting by hand\nin the display face — this is that, as a component, on the kit's grid:\n\n- the **Chicago-style chrome face** by default (dialog captions are chrome),\n  with `face=\"body\"` to switch to Geneva — which is also the kit's fine\n  print, as it was System 7's: a dialog's small captions are Geneva 9 at\n  its own strike size;\n- a **whole-system-pixel line box at the face's native pitch**\n  (`--vf-label-line-height`: 16px — Chicago 12's own line, which is also the\n  em; `face=\"body\"` drops to 12px, Geneva 9's), so a column of captions\n  accumulates whole offsets instead of pushing what follows off the\n  device-pixel grid the way a ratio `line-height` does (README, layout\n  contract rule 2);\n- its own GridSnapController, so the bitmap stems stay on the grid\n  wherever the page puts it;\n- a **declared `width`** (VfSized) — the shared width of a caption\n  column, so a run of label-and-field rows lands every field on one x.\n  Left to its text a caption measures whatever its glyphs measure (the\n  showcase's Apple menu title came to 32.641 system px) and anything sized\n  from it inherits the fraction; a declared width is whole, and so is the\n  row built on it (layout contract rule 3). A caption wider than its column\n  overflows rather than reflowing the row: the number is the column, and a\n  caption that doesn't fit is a number to raise.\n\n`for` points at a control by id, as a native `<label>` does — clicking the\ncaption focuses that control, and the caption text becomes its accessible\nname (see for for how, and for what it deliberately doesn't do).\n\nChrome text, so the host is not selectable (SPEC §1); prose belongs in\n`<vf-paragraph>`, which is.",
          "name": "VfLabel",
          "cssProperties": [
            {
              "description": "The caption's own line box, in system px. Unset (the default) the box follows the face tokens below; set, it overrides both faces for labels alone. Keep any override a whole *even* number — a ratio puts every following line off the grid, and an odd value halves into the em's half-leading and puts the baseline on a half pixel.",
              "name": "--vf-label-line-height"
            },
            {
              "description": "The body face's native line (default `12px`, Geneva 9's) — the face-level knob a body-face retheme states alongside `--vf-font-family` / `--vf-font-size`.",
              "name": "--vf-line-height"
            },
            {
              "description": "The display face's native line (default `16px`, Chicago 12's) — the display retheme's third number.",
              "name": "--vf-line-height-display"
            }
          ],
          "cssParts": [
            {
              "description": "The inner text box.",
              "name": "label"
            }
          ],
          "slots": [
            {
              "description": "The caption text.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "for",
              "type": {
                "text": "string | undefined"
              },
              "description": "The id of the control this caption names, resolved in the label's own tree\nscope (its document or shadow root), like a native `<label for>`.\n\nTwo things follow from it. **Clicking the caption focuses the control** —\na focus shortcut, not an activation: the kit's toggles carry their own\nlabels, so forwarding a click to one would double up on the label they\nalready have. And **the caption text becomes the control's accessible\nname**, by whichever route reaches it:\n\n- a `vf-*` control's focusable element lives in its shadow root, where an\n  `aria-label` on the host cannot reach it — which is why each of them\n  exposes a `label` property that lands on the inner control. That property\n  is what gets filled in, and only when the consumer left it empty.\n- anything else (a native `<input>`, an element with a role) is in this\n  label's own tree scope, so an `aria-labelledby` id reference works; it is\n  set only when the target has no name of its own — and a name computed\n  from the target's *content* counts: a `vf-checkbox` with slotted text, a\n  `vf-button`, a native `<button>` are already named by what they show, and\n  the caption declines rather than stamping over it.\n\nThe one target neither route reaches is a control whose focusable element\nis shadow-internal but which exposes no `label` property because it names\nfrom content — `vf-button` is the kit's case. With visible text it needs\nno caption; an icon-only one should carry its name on the art (`alt` on\nthe slotted `<img>`), which name-from-content picks up the same way.\n\nEither way the label puts back what it found when it is removed, the id\nchanges, or the caption text does.\n\nLeft `undefined` rather than `''` so an unset `for` reflects no attribute\nat all: `vf-label[for]` is a selector a page will reasonably write, and an\nempty `for=\"\"` on every plain caption would make it match all of them.",
              "attribute": "for",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "face",
              "type": {
                "text": "'display' | 'body' | undefined"
              },
              "description": "Which embedded face to set the caption in — `'display'` (the Chicago-style\nchrome face, the default) or `'body'` (Geneva). Applied by\n`vfStaticText`; declared here so it types and reflects.",
              "attribute": "face",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "dim",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Greys the caption to `--vf-disabled`. System 7 dims the label, not the\ncontrol (SPEC §1), so this is what a caption beside a disabled control\nwears — the control keeps its solid black box.",
              "attribute": "dim",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "#named",
              "privacy": "private",
              "type": {
                "text": "{ target: Nameable; via: 'property' | 'aria'; value: string } | null"
              },
              "default": "null",
              "description": "The name we handed the target, so we can take back exactly that."
            },
            {
              "kind": "field",
              "name": "#textObserver",
              "privacy": "private",
              "type": {
                "text": "MutationObserver | undefined"
              },
              "description": "Re-derives the pushed name when the caption's text mutates *in place*.\n`slotchange` only fires when the assigned nodes change — and frameworks\n(Lit's ChildPart, React) update text by writing `.data` on the existing\nText node, which assigns nothing. Without this, a re-rendered caption\nchanged on screen while the control kept the old accessible name forever.\nWatching the target's own property back is safe: `#link` writes to the\n*target*, never into this label's subtree, so the observer can't loop."
            },
            {
              "kind": "method",
              "name": "#target",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "Nameable | null"
                }
              },
              "description": "Resolve `for` in this label's own tree scope, as `<label for>` does."
            },
            {
              "kind": "field",
              "name": "#handleSlotChange",
              "privacy": "private",
              "description": "The caption text is the accessible name, so it has to be re-derived\nwhenever the light DOM changes — including the very first time, since a\ncomponent defined before the page parses is upgraded with no children yet\nand `connectedCallback` sees an empty caption."
            },
            {
              "kind": "field",
              "name": "#relink",
              "privacy": "private",
              "description": "Re-run the wiring: mid-parse retry (see #link) and in-place text edits\n(see #textObserver) both land here."
            },
            {
              "kind": "method",
              "name": "#link",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Give the target its name; a no-op if it already has one."
            },
            {
              "kind": "method",
              "name": "#unlink",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Take back the name — but only if it is still the one we set."
            },
            {
              "kind": "field",
              "name": "#handleClick",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "attribute": "width",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "attribute": "height",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "for",
              "type": {
                "text": "string | undefined"
              },
              "description": "The id of the control this caption names, resolved in the label's own tree\nscope (its document or shadow root), like a native `<label for>`.\n\nTwo things follow from it. **Clicking the caption focuses the control** —\na focus shortcut, not an activation: the kit's toggles carry their own\nlabels, so forwarding a click to one would double up on the label they\nalready have. And **the caption text becomes the control's accessible\nname**, by whichever route reaches it:\n\n- a `vf-*` control's focusable element lives in its shadow root, where an\n  `aria-label` on the host cannot reach it — which is why each of them\n  exposes a `label` property that lands on the inner control. That property\n  is what gets filled in, and only when the consumer left it empty.\n- anything else (a native `<input>`, an element with a role) is in this\n  label's own tree scope, so an `aria-labelledby` id reference works; it is\n  set only when the target has no name of its own — and a name computed\n  from the target's *content* counts: a `vf-checkbox` with slotted text, a\n  `vf-button`, a native `<button>` are already named by what they show, and\n  the caption declines rather than stamping over it.\n\nThe one target neither route reaches is a control whose focusable element\nis shadow-internal but which exposes no `label` property because it names\nfrom content — `vf-button` is the kit's case. With visible text it needs\nno caption; an icon-only one should carry its name on the art (`alt` on\nthe slotted `<img>`), which name-from-content picks up the same way.\n\nEither way the label puts back what it found when it is removed, the id\nchanges, or the caption text does.\n\nLeft `undefined` rather than `''` so an unset `for` reflects no attribute\nat all: `vf-label[for]` is a selector a page will reasonably write, and an\nempty `for=\"\"` on every plain caption would make it match all of them.",
              "fieldName": "for"
            },
            {
              "name": "face",
              "type": {
                "text": "'display' | 'body' | undefined"
              },
              "description": "Which embedded face to set the caption in — `'display'` (the Chicago-style\nchrome face, the default) or `'body'` (Geneva). Applied by\n`vfStaticText`; declared here so it types and reflects.",
              "fieldName": "face"
            },
            {
              "name": "dim",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Greys the caption to `--vf-disabled`. System 7 dims the label, not the\ncontrol (SPEC §1), so this is what a caption beside a disabled control\nwears — the control keeps its solid black box.",
              "fieldName": "dim"
            },
            {
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "fieldName": "width",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "fieldName": "height",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfSized",
              "module": "/src/size.js"
            },
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-label",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfLabel",
          "declaration": {
            "name": "VfLabel",
            "module": "src/components/vf-label.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-label",
          "declaration": {
            "name": "VfLabel",
            "module": "src/components/vf-label.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-list-item.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-list-item>` — a row inside a `<vf-list>` list box.\n\nA 20px-tall single-line row; when selected the entire row inverts\n(white-on-black), the classic System 7 selection style. Selection and\nkeyboard focus are managed by the parent `<vf-list>`.",
          "name": "VfListItem",
          "cssParts": [
            {
              "description": "The text span beside the icon gutter (ellipsizes).",
              "name": "text"
            }
          ],
          "slots": [
            {
              "description": "The row's text/content.",
              "name": ""
            },
            {
              "description": "A leading graphic — usually a `vf-img` holding a 16×16 System 7 small icon, but any element rides here. The row lays it out as the icon gutter: flex-centered vertically (keep the difference between the row height and the icon height even, so the centering offset stays a whole pixel — 16 in a 20px row is a whole 2px), with the reference art's 4px gap to the text. Contributes no text, so first-letter type-ahead still reads the row's words. On a selected row the graphic rides the inverted bar as-is — System 7 left color icons unfiltered on the highlight.",
              "name": "icon"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The value this row contributes to the list's `value`/`values`.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "selected",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the row is selected (inverted). Managed by `<vf-list>`.",
              "attribute": "selected",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the row: dimmed text, not selectable or focusable.",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "listDisabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "True while the containing `<vf-list>` is disabled. Managed by the list —\nnot intended to be set by consumers. Kept distinct from `disabled` so\nre-enabling the list doesn't clear rows that are disabled in their own\nright. (The dimming already arrives by inheritance: the list host sets\n`color: var(--vf-disabled)` and rows inherit it.) Mirrors\n`vf-radio.groupDisabled`."
            }
          ],
          "attributes": [
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The value this row contributes to the list's `value`/`values`.",
              "fieldName": "value"
            },
            {
              "name": "selected",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the row is selected (inverted). Managed by `<vf-list>`.",
              "fieldName": "selected"
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the row: dimmed text, not selectable or focusable.",
              "fieldName": "disabled"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-list-item",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfListItem",
          "declaration": {
            "name": "VfListItem",
            "module": "src/components/vf-list-item.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-list-item",
          "declaration": {
            "name": "VfListItem",
            "module": "src/components/vf-list-item.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-list.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-list>` — the classic System 7 list box.\n\nA white, black-bordered scrolling box of `<vf-list-item>` rows with\nSystem 7-styled scrollbars (dither track, boxed arrow buttons). The vertical\nrail is a permanent placeholder — an empty white channel when the rows fit,\nfilling in with dither/thumb/arrows only on overflow (driven by\nScrollStateController). Selection inverts rows. Supports single and\nmultiple selection, roving tabindex, arrow-key navigation, and classic Finder\nfirst-letter type-ahead.\n\nMax height defaults to 200px; override with `--vf-list-max-height`.",
          "name": "VfList",
          "cssProperties": [
            {
              "description": "`vf-list` max height before its rail takes over (the host adds the 2px frame)",
              "name": "--vf-list-max-height",
              "default": "200px"
            },
            {
              "description": "scrollbar thumb/elevator (white)",
              "name": "--vf-scrollbar-thumb"
            },
            {
              "description": "scrollbar trough — **Firefox fallback only**; the WebKit path draws the dot-dither tile instead, and this is its flat 25%-black average",
              "name": "--vf-scrollbar-track"
            }
          ],
          "cssParts": [
            {
              "description": "The scrolling viewport around the slotted items.",
              "name": "list"
            }
          ],
          "slots": [
            {
              "description": "`vf-list-item` elements.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "viewport",
              "type": {
                "text": "HTMLElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "content",
              "type": {
                "text": "HTMLElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "scrollState",
              "privacy": "private",
              "readonly": true,
              "default": "new ScrollStateController( this, () => this.viewport, () => this.content, (overflow) => { this._scrollable = overflow.x || overflow.y } )",
              "description": "Reserves the vertical rail and toggles it active on overflow."
            },
            {
              "kind": "field",
              "name": "_scrollable",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether the rows actually overflow the box (either axis)."
            },
            {
              "kind": "field",
              "name": "multiple",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Allows multiple selection (Shift extends, Cmd/Ctrl toggles).",
              "attribute": "multiple",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Value of the (first) selected item. Settable.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "values",
              "type": {
                "text": "string[]"
              },
              "default": "[]",
              "description": "Values of all selected items (multiple mode). Settable."
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the whole list: dimmed, no interaction.",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the list, applied as `aria-label` on the host (which\ncarries `role=\"listbox\"`). Without it a caption-less list is announced\nanonymously. A consumer-supplied `aria-label`/`aria-labelledby` attribute\nis left alone.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "_items",
              "type": {
                "text": "VfListItem[]"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#activeIndex",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "default": "-1",
              "description": "Index of the item that owns the roving tabindex / keyboard cursor."
            },
            {
              "kind": "field",
              "name": "#anchorIndex",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "default": "-1",
              "description": "Index the next Shift+click extends from."
            },
            {
              "kind": "field",
              "name": "#typeAhead",
              "privacy": "private",
              "readonly": true,
              "default": "new TypeAheadBuffer()",
              "description": "First-letter type-ahead over the rows; see src/type-ahead.ts."
            },
            {
              "kind": "field",
              "name": "#appliedValues",
              "privacy": "private",
              "type": {
                "text": "string[]"
              },
              "default": "[]",
              "description": "The selection last pushed onto the rows; see #applyIfStale."
            },
            {
              "kind": "method",
              "name": "focus",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "options",
                  "optional": true,
                  "type": {
                    "text": "FocusOptions"
                  }
                }
              ],
              "description": "Moves keyboard focus to the row holding the roving tab stop — or, while\nthe list is disabled, to the scrolling viewport (the disabled list's own\nTab stop; the rows are all at -1 then). Overridden because the platform's\n`focus()` is a silent no-op on this host — no `delegatesFocus`, no host\ntabindex — so `vf-label for` (which calls `target.focus()`) never reached\nthe list."
            },
            {
              "kind": "method",
              "name": "#applyIfStale",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "vals",
                  "type": {
                    "text": "string[]"
                  }
                }
              ],
              "description": "Push `vals` onto the rows unless they already carry exactly that\nselection.\n\nEvery interactive path applies the selection synchronously and *then*\nwrites `value`/`values`, which re-enters `updated()` above — so without\nthis gate each click, arrow key and type-ahead jump ran a second,\nbyte-identical pass over every row. External writes still land: they reach\nhere with a selection the rows don't have yet."
            },
            {
              "kind": "method",
              "name": "#onSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#applySelection",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "vals",
                  "type": {
                    "text": "string[]"
                  }
                },
                {
                  "name": "notify",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Makes exactly `vals` the selected values, syncs `value`/`values`, and\n(optionally) fires `vf-change` if the selection actually changed."
            },
            {
              "kind": "method",
              "name": "#syncItems",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Pushes list state down to the rows: the roving tabindex (the active — or\nfirst selected/enabled — row gets 0) and the list-level disabled flag, so a\ndisabled listbox doesn't present enabled-looking options to a screen reader.\n`listDisabled` stays separate from each row's own `disabled`, so\nre-enabling the list doesn't un-disable individually disabled rows.\nMirrors `vf-radio-group.syncRadios()`."
            },
            {
              "kind": "field",
              "name": "#onClick",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onKeydown",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "#cursorTo",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Moves the keyboard cursor (roving tab stop + focus) to `index` without\ntouching the selection — the multiple-mode focus move (Ctrl+Arrow, plain\nHome/End, type-ahead), where Space then acts on the row it reached."
            },
            {
              "kind": "method",
              "name": "#extendTo",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "from",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Selects exactly the contiguous run from the anchor to `index` — adopting\n`from` as the anchor when none is set — and moves the cursor there. The\nshape every Shift extension shares: Shift+Arrow, Shift(+Ctrl)+Home/End,\nShift+Space."
            },
            {
              "kind": "method",
              "name": "#step",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number"
                }
              },
              "parameters": [
                {
                  "name": "from",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "dir",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ],
              "description": "Next enabled index from `from` in `dir`, without wrapping."
            },
            {
              "kind": "method",
              "name": "#moveTo",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Selects (replacing) and focuses the item at `index`."
            },
            {
              "kind": "method",
              "name": "#focusTo",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Focuses and scrolls to the item at `index` without selecting it.\n\n`block: 'nearest'` is the conventional minimal-scroll listbox pattern and is\nkept deliberately, with one known consequence: scrollIntoView walks the\nwhole scrollable-ancestor chain, so a list sitting near a viewport edge can\nnudge the page as well as its own `.list` viewport. Reviewed and accepted —\nreplacing it with hand-computed `scrollTop` math against the internal\nviewport would trade a well-understood primitive for bespoke geometry."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "events": [
            {
              "description": "When the user changes the selection. `detail: { value: string, values: string[] }`.",
              "name": "vf-change"
            }
          ],
          "attributes": [
            {
              "name": "multiple",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Allows multiple selection (Shift extends, Cmd/Ctrl toggles).",
              "fieldName": "multiple"
            },
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Value of the (first) selected item. Settable.",
              "fieldName": "value"
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the whole list: dimmed, no interaction.",
              "fieldName": "disabled"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the list, applied as `aria-label` on the host (which\ncarries `role=\"listbox\"`). Without it a caption-less list is announced\nanonymously. A consumer-supplied `aria-label`/`aria-labelledby` attribute\nis left alone.",
              "fieldName": "label"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-list",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfList",
          "declaration": {
            "name": "VfList",
            "module": "src/components/vf-list.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-list",
          "declaration": {
            "name": "VfList",
            "module": "src/components/vf-list.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-menu-bar.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-menu-bar>` — the System 7 menu bar: white strip, 1px black bottom\nrule, slotted `<vf-menu>` children laid out from the left. `rounded` adds\nthe classic screen-corner mask to the top corners.\n\nCoordinates its menus: pressing a label opens that menu (and inverts the\nlabel); while any menu is open, hovering another label switches to it;\nEscape, an outside click, or item selection closes. ArrowLeft/ArrowRight\nmove between menus while one is open; ArrowDown/ArrowUp move focus through\nthe open menu's items, Home/End jump to its first/last, and typed letters\nrun the shared Finder first-letter type-ahead (src/type-ahead.ts).\n\nThe bar also owns the **press-drag-release** gesture (see src/menu-press.ts)\n— press a title, slide onto a command, release over it — because one press\nmay travel across several of its menus. `vf-select` drives its popup by the\nsame mechanic.",
          "name": "VfMenuBar",
          "cssProperties": [
            {
              "description": "`vf-menu-bar`",
              "name": "--vf-menubar-height",
              "default": "20px"
            }
          ],
          "cssParts": [
            {
              "description": "The horizontal layout container.",
              "name": "bar"
            }
          ],
          "slots": [
            {
              "description": "`vf-menu` elements.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the bar, applied as `aria-label` on the host (which\ncarries `role=\"menubar\"`). Without it the menubar computes with an empty\nname. A consumer-supplied `aria-label`/`aria-labelledby` attribute is\nleft alone.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "rounded",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Rounds the bar's top corners the way System 7's were: not a curve of the\nbar itself but the black stair-step mask the compact Mac's ROM painted\nover the CRT's rounded corners, which the menu bar wore by sitting flush\nwith the screen top. Drawn as the traced 5-px staircase in `--vf-black`\nink over the bar's own corners — over any backdrop, as the hardware\nmask was.",
              "attribute": "rounded",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "_menus",
              "type": {
                "text": "VfMenu[]"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#openMenu",
              "privacy": "private",
              "type": {
                "text": "VfMenu | null"
              },
              "default": "null",
              "description": "The currently-open slotted menu, if any."
            },
            {
              "kind": "field",
              "name": "#ownsRole",
              "privacy": "private",
              "type": {
                "text": "boolean | undefined"
              },
              "description": "Whether this component owns the host `role`. Decided on the FIRST connect\nonly (vf-menu-item's latch): our own `role=\"menubar\"` write persists on\nthe element, so re-testing `hasAttribute('role')` on a reconnect would\nread that write back as consumer-supplied and freeze it — while testing\nnothing would overwrite a consumer's own `role=\"toolbar\"` on upgrade."
            },
            {
              "kind": "field",
              "name": "#typeAhead",
              "privacy": "private",
              "readonly": true,
              "default": "new TypeAheadBuffer()",
              "description": "First-letter type-ahead over the open menu's items; see src/type-ahead.ts."
            },
            {
              "kind": "field",
              "name": "#rovingMenu",
              "privacy": "private",
              "type": {
                "text": "VfMenu | null"
              },
              "default": "null",
              "description": "The menu that currently holds the bar's single Tab stop (roving\ntabindex). Falls back to the first menu when unset."
            },
            {
              "kind": "field",
              "name": "#docListeners",
              "privacy": "private",
              "readonly": true,
              "default": "new DocumentListenersController(this, () => [ [document, 'pointerdown', this.#onDocPointerDown, true], [document, 'keydown', this.#onDocKeydown, true], ])",
              "description": "Outside dismissal + menu/item keyboard nav while a menu is open."
            },
            {
              "kind": "field",
              "name": "#press",
              "privacy": "private",
              "readonly": true,
              "default": "new MenuPressController(this, { menus: () => this._menus, open: (menu) => this.#openMenuAt(menu), close: () => this.#closeAll(), })",
              "description": "The System 7 press-drag-release gesture across the whole bar. Presses on a\ntitle or a dropped row both bubble up here composed, and the callbacks are\nthe bar's own open/close rules — so the gesture changes *when* a menu\nopens, never *how*."
            },
            {
              "kind": "method",
              "name": "focus",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "options",
                  "optional": true,
                  "type": {
                    "text": "FocusOptions"
                  }
                }
              ],
              "description": "Moves keyboard focus to the bar's single Tab stop — the open menu's label,\nelse the roving one's, else the first. Overridden because the platform's\n`focus()` was a silent no-op on this host (no `delegatesFocus`, no\ntabindex), so `vf-label for` and consumer scripts couldn't reach the bar."
            },
            {
              "kind": "field",
              "name": "#onHostFocusOut",
              "privacy": "private",
              "description": "Focus left the bar entirely while a menu was open: close it, so an open\npanel can't outlive the focus that operates it and go on capturing every\narrow key on the page. Modeled on `VfSelect.handleHostFocusOut`; the\nrelatedTarget is retargeted to light-DOM scope, so slotted menus (and\ntheir panels' items) read as contained."
            },
            {
              "kind": "method",
              "name": "#onSlotChange",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#syncMenus",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Push a roving tabindex down to the slotted menus so the whole bar is a\nsingle Tab stop: the active menu's label gets tabindex 0, the rest -1.\nThe active menu is the open one, else the roving one, else the first.\nMirrors `vf-radio-group.syncRadios()`."
            },
            {
              "kind": "method",
              "name": "#setRovingMenu",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "menu",
                  "type": {
                    "text": "VfMenu"
                  }
                }
              ],
              "description": "Moves the roving Tab stop to `menu` and re-syncs tabindices."
            },
            {
              "kind": "field",
              "name": "#onToggleRequest",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onMenuHover",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onCloseRequest",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "#openMenuAt",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "menu",
                  "type": {
                    "text": "VfMenu"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#closeAll",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "field",
              "name": "#onDocPointerDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onDocKeydown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onBarKeydown",
              "privacy": "private",
              "description": "Roving-focus navigation while the bar has focus but no menu is open.\nArrowLeft/Right (and Home/End) move the single Tab stop between labels\nand follow it with DOM focus. While a menu IS open, ArrowLeft/Right is\nhandled by `#onDocKeydown`/`#switchMenu` instead, so bail out here."
            },
            {
              "kind": "method",
              "name": "#switchMenu",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "direction",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ],
              "description": "Opens the previous/next menu in the bar, wrapping at the ends."
            },
            {
              "kind": "method",
              "name": "#moveItemFocus",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "direction",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ],
              "description": "Moves keyboard focus through the open menu's enabled items, wrapping."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the bar, applied as `aria-label` on the host (which\ncarries `role=\"menubar\"`). Without it the menubar computes with an empty\nname. A consumer-supplied `aria-label`/`aria-labelledby` attribute is\nleft alone.",
              "fieldName": "label"
            },
            {
              "name": "rounded",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Rounds the bar's top corners the way System 7's were: not a curve of the\nbar itself but the black stair-step mask the compact Mac's ROM painted\nover the CRT's rounded corners, which the menu bar wore by sitting flush\nwith the screen top. Drawn as the traced 5-px staircase in `--vf-black`\nink over the bar's own corners — over any backdrop, as the hardware\nmask was.",
              "fieldName": "rounded"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-menu-bar",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfMenuBar",
          "declaration": {
            "name": "VfMenuBar",
            "module": "src/components/vf-menu-bar.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-menu-bar",
          "declaration": {
            "name": "VfMenuBar",
            "module": "src/components/vf-menu-bar.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-menu-item.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-menu-item>` — a single command inside a `<vf-menu>` panel.\n\nRenders the classic System 7 menu row: optional ✓ check in the\n`--vf-select-gutter` left column (16px, shared with vf-select/vf-option),\nlabel, and the keyboard shortcut left-aligned in a right-anchored column,\nso every ⌘ lands at the same x. On activation the item performs the classic\n3-blink inversion (~250ms), then dispatches `vf-menu-select` and asks its\nancestors to close the menu.",
          "name": "VfMenuItem",
          "cssProperties": [
            {
              "description": "`vf-menu-item` row pitch (`Menus.png`; kept separate from `--vf-popup-height` so re-theming the popup pill doesn't move pulldown rows)",
              "name": "--vf-menu-row-height",
              "default": "16px"
            },
            {
              "description": "`vf-menu-item` shortcut slot, right-anchored with the text left-aligned in it so every ⌘ lands at the same x (`Menus.png`) — the MDEF reserve, ⌘'s 11px advance + the face's widest letter (M/W, 12px); widen it to line up longer shortcuts (\"⌘⇧S\")",
              "name": "--vf-menu-shortcut-column",
              "default": "23px"
            },
            {
              "description": "checkmark column: `vf-select` left inset / `vf-option` + `vf-menu-item` ✓ column (shared so the value doesn't shift on open)",
              "name": "--vf-select-gutter",
              "default": "16px"
            }
          ],
          "cssParts": [
            {
              "description": "The row container.",
              "name": "item"
            },
            {
              "description": "The ✓ checkmark glyph (rendered when `checked`).",
              "name": "check"
            },
            {
              "description": "The label wrapper around the default slot.",
              "name": "label"
            },
            {
              "description": "The shortcut text, left-aligned in the shared column.",
              "name": "shortcut"
            }
          ],
          "slots": [
            {
              "description": "The item label.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the item: dimmed text, no highlight, no activation.",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "checked",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Shows the classic ✓ checkmark in the left gutter.",
              "attribute": "checked",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "active",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Transient highlight — the full-row inversion the press-drag gesture paints\non the row under the pointer (`:hover` can't: under touch the pointer is\ncaptured by the title the press started on). Managed by the menu, mirroring\n`vf-option[active]`; not part of the authoring API.",
              "attribute": "active",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "checkable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Declares the item a *checkable* toggle up front, so it carries\n`role=\"menuitemcheckbox\"` and `aria-checked=\"false\"` from the first render\nrather than only once it has been checked. Set this on a toggle that starts\n**off** — otherwise it announces as a plain command until the first flip\n(the `checked` attribute can't express \"checkable but off\": for a boolean\nattribute, presence *is* true).",
              "attribute": "checkable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "shortcut",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Keyboard shortcut text, e.g. `\"⌘H\"`, drawn left-aligned in the panel's\nshared shortcut column. Display only in the\nvisual sense — the span is `aria-hidden` so the glyphs never concatenate\ninto the item's accessible name (\"Print… place of interest sign P\") —\nwhile the host mirrors it as `aria-keyshortcuts` (\"Meta+H\"), so AT\nannounces it *as* a shortcut. A consumer's own `aria-keyshortcuts` wins.",
              "attribute": "shortcut"
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string | undefined"
              },
              "description": "Value reported in the `vf-menu-select` event detail. Defaults to the item's\ntrimmed text content when unset.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "_blinkPhase",
              "type": {
                "text": "'on' | 'off' | null"
              },
              "privacy": "private",
              "default": "null",
              "description": "`'on' | 'off'` while the selection blink runs, otherwise `null`."
            },
            {
              "kind": "field",
              "name": "#blinkHandle",
              "privacy": "private",
              "type": {
                "text": "BlinkHandle | undefined"
              }
            },
            {
              "kind": "field",
              "name": "#blinking",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false"
            },
            {
              "kind": "field",
              "name": "#everChecked",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Latches true once the item has ever been `checked`, so an item that only\never sets `checked` — without declaring checkable — is still\npromoted to a toggle. Folded together with the public prop by\n#isCheckable."
            },
            {
              "kind": "field",
              "name": "#ownsRole",
              "privacy": "private",
              "type": {
                "text": "boolean | undefined"
              },
              "description": "Whether this component owns the host `role`. Decided on the FIRST connect\nonly: our own `role` write persists on the element, so re-testing\n`hasAttribute('role')` on a reconnect would read that write back as\nconsumer-supplied and freeze the role wherever it happened to be."
            },
            {
              "kind": "field",
              "name": "#ownsKeyshortcuts",
              "privacy": "private",
              "type": {
                "text": "boolean | undefined"
              },
              "description": "Same first-connect latch, for the host `aria-keyshortcuts` mirror."
            },
            {
              "kind": "field",
              "name": "#swallowClick",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Swallows the one `click` the browser synthesises after a pointer press: the\nmenu's press gesture (src/menu-press.ts) already resolved it, and under\n`prefers-reduced-motion` — where activation completes synchronously and the\nblink guard is back down by the time the click lands — the row would\notherwise fire `vf-menu-select` twice. Set only inside a `vf-menu`, where\nthat gesture is running; a row used outside one keeps plain click\nactivation. A `click` with no preceding pointerdown (keyboard /\nassistive tech) always activates — which is exactly why the click listener\nlives on the HOST (see the constructor): a synthetic `element.click()`\ntargets the node carrying the role, and events dispatched at the host\npropagate up, never down into its own shadow tree. Cleared by the\ngesture's trailing click wherever it lands (see releaseAfterGesture) — a\npress-drag-release dispatches it at an ancestor, above this element."
            },
            {
              "kind": "field",
              "name": "#isCheckable",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "description": "True when the item should announce as a toggle, not a plain command.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#syncKeyshortcuts",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Mirrors shortcut onto the host as `aria-keyshortcuts`, normalised\nfrom the Mac display glyphs to the ARIA grammar (\"⌘⇧S\" → \"Meta+Shift+S\").\nSkipped when the consumer supplied their own value."
            },
            {
              "kind": "method",
              "name": "#syncRole",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Writes the ARIA role and `aria-checked` for the current state. A checkable\nitem announces its on/off state (`aria-checked` is only valid on the\ncheckbox role); a plain command stays `role=\"menuitem\"`. Skipped entirely\nwhen the consumer supplied their own role."
            },
            {
              "kind": "method",
              "name": "#onPointerDown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#onClick",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#onKeydown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "activate",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Runs the classic 3-blink inversion, then dispatches `vf-menu-select` and an\ninternal `vf-menu-close-request` so ancestor menu/menu-bar close. Public\nbecause the menu's press gesture activates the row a drag was released\nover, which the row's own `click` never sees (a press that started on the\ntitle dispatches its click above both of them). No-op while disabled or\nalready blinking."
            },
            {
              "kind": "method",
              "name": "#cancelBlink",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Aborts an in-flight selection blink *without* dispatching — the item is\nbeing disabled or torn down, so the pending `vf-menu-select` is dropped."
            },
            {
              "kind": "method",
              "name": "#dispatchSelect",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            }
          ],
          "events": [
            {
              "description": "After the blink completes. `detail: { value, item }`. Named for the menu rather than plain `vf-select`, which would collide with the `<vf-select>` popup on any delegated ancestor listener (that component commits with `vf-change`).",
              "name": "vf-menu-select"
            }
          ],
          "attributes": [
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the item: dimmed text, no highlight, no activation.",
              "fieldName": "disabled"
            },
            {
              "name": "checked",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Shows the classic ✓ checkmark in the left gutter.",
              "fieldName": "checked"
            },
            {
              "name": "active",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Transient highlight — the full-row inversion the press-drag gesture paints\non the row under the pointer (`:hover` can't: under touch the pointer is\ncaptured by the title the press started on). Managed by the menu, mirroring\n`vf-option[active]`; not part of the authoring API.",
              "fieldName": "active"
            },
            {
              "name": "checkable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Declares the item a *checkable* toggle up front, so it carries\n`role=\"menuitemcheckbox\"` and `aria-checked=\"false\"` from the first render\nrather than only once it has been checked. Set this on a toggle that starts\n**off** — otherwise it announces as a plain command until the first flip\n(the `checked` attribute can't express \"checkable but off\": for a boolean\nattribute, presence *is* true).",
              "fieldName": "checkable"
            },
            {
              "name": "shortcut",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Keyboard shortcut text, e.g. `\"⌘H\"`, drawn left-aligned in the panel's\nshared shortcut column. Display only in the\nvisual sense — the span is `aria-hidden` so the glyphs never concatenate\ninto the item's accessible name (\"Print… place of interest sign P\") —\nwhile the host mirrors it as `aria-keyshortcuts` (\"Meta+H\"), so AT\nannounces it *as* a shortcut. A consumer's own `aria-keyshortcuts` wins.",
              "fieldName": "shortcut"
            },
            {
              "name": "value",
              "type": {
                "text": "string | undefined"
              },
              "description": "Value reported in the `vf-menu-select` event detail. Defaults to the item's\ntrimmed text content when unset.",
              "fieldName": "value"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-menu-item",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfMenuItem",
          "declaration": {
            "name": "VfMenuItem",
            "module": "src/components/vf-menu-item.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-menu-item",
          "declaration": {
            "name": "VfMenuItem",
            "module": "src/components/vf-menu-item.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-menu.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-menu>` — a pull-down menu: a bar label plus a dropped panel of\n`<vf-menu-item>` / `<vf-separator>` children.\n\nInside a `<vf-menu-bar>` the bar coordinates open state (only one menu open,\nhover-switching, outside-click/Escape dismissal) and owns the pointer\ngesture, which may travel between its menus. Used standalone, the menu\ntoggles itself on label click and manages its own dismissal, its own press\ngesture and item keyboard navigation (ArrowUp/ArrowDown, Home/End, and the\nshared first-letter type-ahead — src/type-ahead.ts) while open.\n\nPointer — the two styles `vf-select` supports, on the same terms (see\nsrc/menu-press.ts): the System 7 press-drag-release (press the title, slide\nonto a command, release over it) and a modern quick tap that leaves the menu\ndropped for a second click.",
          "name": "VfMenu",
          "cssProperties": [
            {
              "description": "`vf-menu-bar`",
              "name": "--vf-menubar-height",
              "default": "20px"
            },
            {
              "description": "`vf-menu-item` row pitch; the panel also spends one of these on every slotted `vf-separator`, the full row the MDEF gave a divider (rule 8px into it — see the panel CSS)",
              "name": "--vf-menu-row-height",
              "default": "16px"
            }
          ],
          "cssParts": [
            {
              "description": "The menu title in the bar (inverts while open).",
              "name": "label"
            },
            {
              "description": "The dropped `.vf-panel` containing the items.",
              "name": "panel"
            }
          ],
          "slots": [
            {
              "description": "Menu contents: `vf-menu-item` and `vf-separator` elements.",
              "name": ""
            },
            {
              "description": "Replaces the `label` text in the bar — e.g. a `vf-img` apple icon for the Apple menu. Keep the `label` attribute set too: it stays the menu's accessible name (the bar item's `aria-label` and the panel's) when the visible title is an image.",
              "name": "label"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "#focusRule",
              "privacy": "private",
              "readonly": true,
              "default": "new FocusRuleController(this)",
              "description": "Whether the bar title wears the kit's dashed focus rule — keyboard focus\nonly, which this control can't read off `:focus-visible` (see the CSS)."
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The menu title shown in the bar, and the menu's accessible name. Slotted\n`label` content replaces it visually (see the `label` slot) but this text\nkeeps naming the menu for AT.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the panel is dropped. Reflected. Managed by the parent\n`vf-menu-bar` when present, otherwise by the menu itself.",
              "attribute": "open",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "barTabIndex",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "The bar label's tabindex. A parent `vf-menu-bar` owns a roving tabindex\nacross its menus and sets this to 0 on the active menu, -1 on the rest,\nso the whole bar is a single Tab stop. Defaults to 0 so a standalone\nmenu is Tab-focusable on its own."
            },
            {
              "kind": "field",
              "name": "_labelEl",
              "type": {
                "text": "HTMLElement"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "_assignedItems",
              "type": {
                "text": "VfMenuItem[]"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#press",
              "privacy": "private",
              "readonly": true,
              "default": "new MenuPressController(this, { menus: () => [this], open: (menu) => { menu.open = true }, close: () => { this.open = false }, })",
              "description": "The System 7 press-drag-release gesture, for a menu standing on its own.\nInside a `vf-menu-bar` the bar owns it instead — a press may travel between\nits menus, so the coordinator has to be the one tracking it."
            },
            {
              "kind": "field",
              "name": "items",
              "type": {
                "text": "VfMenuItem[]"
              },
              "description": "The enabled `vf-menu-item` children, in document order.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "allItems",
              "type": {
                "text": "VfMenuItem[]"
              },
              "description": "Every slotted `vf-menu-item`, in document order — **disabled rows\nincluded**, unlike items. The press gesture hit-tests against\nthese: releasing over a disabled row has to cancel, not fall through to\nwhatever the panel covers.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "labelRect",
              "type": {
                "text": "DOMRect | null"
              },
              "description": "Viewport rect of the bar title, or `null` before the first render. The\npress gesture hit-tests by coordinates rather than by event target (see\nsrc/menu-press.ts), so it wants the box, not the element.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#inBar",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "description": "Whether a `vf-menu-bar` is coordinating this menu.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#ownsRole",
              "privacy": "private",
              "type": {
                "text": "boolean | undefined"
              },
              "description": "Whether this component owns the host `role`. Decided on the FIRST connect\nonly (vf-menu-item's latch): our own write persists on the element, so\nre-testing `hasAttribute('role')` on a reconnect would read it back as\nconsumer-supplied and freeze it."
            },
            {
              "kind": "field",
              "name": "#typeAhead",
              "privacy": "private",
              "readonly": true,
              "default": "new TypeAheadBuffer()",
              "description": "First-letter type-ahead while open standalone; see src/type-ahead.ts."
            },
            {
              "kind": "field",
              "name": "#swallowClick",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Swallows the one `click` the browser synthesises after a pointer press, so\nthe gesture that just dropped the menu isn't immediately toggled shut by\nits own trailing click. A `click` with no preceding pointerdown (keyboard /\nassistive-tech activation) still reaches #onLabelClick. The same\nguard `vf-select` uses, for the same reason."
            },
            {
              "kind": "field",
              "name": "#onCloseRequest",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onDocPointerDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onDocKeydown",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "#moveItemFocus",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "direction",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ],
              "description": "Moves keyboard focus through the enabled items, wrapping at the ends."
            },
            {
              "kind": "field",
              "name": "#docListeners",
              "privacy": "private",
              "readonly": true,
              "default": "new DocumentListenersController(this, () => [ [document, 'pointerdown', this.#onDocPointerDown, true], [document, 'keydown', this.#onDocKeydown, true], ])",
              "description": "Outside dismissal + item keyboard nav while open standalone."
            },
            {
              "kind": "method",
              "name": "#syncRole",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "In a bar the host is `role=\"none\"`: the `menubar`'s items are the shadow\n`.label`s (`menuitem`), and while browsers do compute that ownership\nthrough a role-less generic, declaring it keeps the chain explicit.\nStandalone, the host stays role-less — the label is a `button` there.\nSkipped when the consumer supplied their own role."
            },
            {
              "kind": "field",
              "name": "#onHostFocusOut",
              "privacy": "private",
              "description": "Focus left the menu entirely while open standalone: close, so an open\npanel can't outlive the focus that operates it and go on capturing the\ndocument's keyboard. Modeled on `VfSelect.handleHostFocusOut`. In a bar,\nthe bar's own focusout listener coordinates instead."
            },
            {
              "kind": "field",
              "name": "#onHostPointerDown",
              "privacy": "private",
              "description": "A press anywhere in the menu — the title, or a row inside the dropped panel\n(both bubble up composed). Inside a bar the bar's own listener drives the\ngesture instead, since it may travel between menus."
            },
            {
              "kind": "method",
              "name": "focus",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "options",
                  "optional": true,
                  "type": {
                    "text": "FocusOptions"
                  }
                }
              ],
              "description": "Moves keyboard focus to the menu's bar label — the host's one focusable\npoint. Overridden because the platform's `focus()` is a silent no-op here:\nno `delegatesFocus`, no host tabindex, so the standard method did nothing\nand `vf-label for` (which calls `target.focus()`) couldn't reach the menu."
            },
            {
              "kind": "method",
              "name": "focusLabel",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "deprecated": "Use `focus()` — the standard method now does this. (Spelled\nwithout a link: a `{@link}` inside a deprecated tag makes the cem\nanalyzer attach the parsed AST node to the manifest, which then fails\nto serialize — \"Converting circular structure to JSON\".)"
            },
            {
              "kind": "method",
              "name": "#requestToggle",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Requests a toggle. A parent `vf-menu-bar` cancels the internal\n`vf-menu-toggle-request` event and coordinates; otherwise the menu\ntoggles itself.\n\nStandalone, no event is emitted at all: the cancelable request exists for\nthe bar's benefit, and letting it bubble on to the page meant a\nconsumer's delegated handler calling `preventDefault()` on an\nunrecognised event silently kept the menu from ever opening."
            },
            {
              "kind": "method",
              "name": "#onLabelPointerDown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Arms #swallowClick: the pointer gesture owns this press. Released\nwhen the gesture's trailing click lands — a press-drag-release onto a row\ndispatches that click at the common ancestor, above this label, so the\nclearing in #onLabelClick alone would leave the latch stuck and\nsilently swallow the next assistive-tech click on the title."
            },
            {
              "kind": "method",
              "name": "#onLabelClick",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "`click` handler for *synthesised* activation — keyboard/assistive-tech\nclicks that arrive with no preceding pointerdown. A real mouse/touch click\nis swallowed here because the press gesture already resolved it."
            },
            {
              "kind": "method",
              "name": "#onLabelEnter",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "#onLabelKeydown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#focusFirstItem",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "Promise<void>"
                }
              },
              "description": "Focuses the first enabled item once the panel is visible. The reflected\n`open` attribute (which un-hides the panel) only lands on the host in the\nnext Lit update, so focusing synchronously would silently no-op."
            }
          ],
          "attributes": [
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The menu title shown in the bar, and the menu's accessible name. Slotted\n`label` content replaces it visually (see the `label` slot) but this text\nkeeps naming the menu for AT.",
              "fieldName": "label"
            },
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the panel is dropped. Reflected. Managed by the parent\n`vf-menu-bar` when present, otherwise by the menu itself.",
              "fieldName": "open"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-menu",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfMenu",
          "declaration": {
            "name": "VfMenu",
            "module": "src/components/vf-menu.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-menu",
          "declaration": {
            "name": "VfMenu",
            "module": "src/components/vf-menu.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-number-field.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-number-field>` — a System 7 numeric entry field paired with the classic\n\"little arrows\" stepper.\n\nA form-associated wrapper around a native `<input>` (white well, 1px black\nborder) plus the pixel-exact little-arrows control from the Classic Macintosh\nUI Kit sprite. Clicking (or press-and-holding, with autorepeat) an arrow\nsteps the value by `step`, clamped to `min`/`max`; the held arrow fills solid\nblack, matching the kit's hollow→filled press convention. Keyboard focus\ndraws the kit's dashed rule under the well alone — the stepper is beside the\ntext, not part of it. The field is a `spinbutton`: ArrowUp/ArrowDown step, Home/End\njump to min/max. The shared field skin lives in `vfField`; the value/form\nscaffolding in VfTextControlBase.",
          "name": "VfNumberField",
          "cssProperties": [
            {
              "description": "text fields — `vf-text-field`, `vf-text-area`, the `vf-number-field` well",
              "name": "--vf-control-height",
              "default": "22px"
            },
            {
              "description": "width of `vf-number-field`'s input, in its own text (an em, not a system px length — it sizes to the digits)",
              "name": "--vf-number-field-width",
              "default": "4em"
            },
            {
              "description": "placeholder text in the editable fields — kept off `--vf-disabled`: a placeholder sits in an *enabled* well and holds AA contrast, where the disabled gray is exempt",
              "name": "--vf-field-placeholder",
              "default": "#767676"
            }
          ],
          "cssParts": [
            {
              "description": "The inner native `<input>`.",
              "name": "input"
            },
            {
              "description": "The little-arrows control.",
              "name": "stepper"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "min",
              "type": {
                "text": "number | undefined"
              },
              "description": "Minimum allowed value (inclusive). Omit for no lower bound.",
              "attribute": "min"
            },
            {
              "kind": "field",
              "name": "max",
              "type": {
                "text": "number | undefined"
              },
              "description": "Maximum allowed value (inclusive). Omit for no upper bound.",
              "attribute": "max"
            },
            {
              "kind": "field",
              "name": "step",
              "type": {
                "text": "number"
              },
              "default": "1",
              "description": "Increment applied per step / arrow press. Also sets the value's precision.",
              "attribute": "step"
            },
            {
              "kind": "field",
              "name": "pressed",
              "type": {
                "text": "'up' | 'down' | null"
              },
              "privacy": "private",
              "default": "null",
              "description": "Which arrow is currently held (drives the solid pressed glyph)."
            },
            {
              "kind": "field",
              "name": "input",
              "type": {
                "text": "HTMLInputElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#delayTimer",
              "privacy": "private",
              "type": {
                "text": "number | undefined"
              }
            },
            {
              "kind": "field",
              "name": "#repeatTimer",
              "privacy": "private",
              "type": {
                "text": "number | undefined"
              }
            },
            {
              "kind": "method",
              "name": "#parse",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number"
                }
              }
            },
            {
              "kind": "method",
              "name": "#clamp",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number"
                }
              },
              "parameters": [
                {
                  "name": "n",
                  "type": {
                    "text": "number"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#commit",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "n",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Clamp + round to the step's precision, store, and announce a change — but\nonly when the value actually changes, so autorepeat held against a bound\n(or a keyboard step already at min/max) doesn't fire a redundant\n`vf-change` on every 60ms tick."
            },
            {
              "kind": "method",
              "name": "#stepBy",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "dir",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#emit",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "type",
                  "type": {
                    "text": "'vf-input' | 'vf-change'"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#startRepeat",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "dir",
                  "type": {
                    "text": "1 | -1"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#stopRepeat",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "field",
              "name": "#onArrowDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onArrowUp",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onKeydown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onInput",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onChange",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "shadowRootOptions",
              "type": {
                "text": "ShadowRootInit"
              },
              "static": true,
              "default": "{ ...VfFormControl.shadowRootOptions, delegatesFocus: true, }",
              "description": "Focus delegates into the inner native control.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Current value. Synced on every keystroke and submitted with forms.",
              "attribute": "value",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Placeholder text shown when the field is empty.",
              "attribute": "placeholder",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "readonly",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Makes the field read-only (focusable, not editable).",
              "attribute": "readonly",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "attribute": "name",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, applied as `aria-label` on the inner native control (the\nelement that receives focus and is announced by screen readers — a host\nattribute cannot reach it on its own). Left empty, the name falls back to\nwhatever the host carries — `aria-labelledby`, `aria-label` or an\nassociated `<label for>` — via VfFormControl.hostLabel, so the\nplatform's own labeling vocabulary works on these controls too.",
              "attribute": "label",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "A field is empty for `required`'s purposes when its value is `''`.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "forwardedAttributes",
              "type": {
                "text": "readonly string[]"
              },
              "privacy": "protected",
              "static": true,
              "readonly": true,
              "default": "[ 'autocomplete', 'inputmode', 'enterkeyhint', 'maxlength', 'spellcheck', 'autocapitalize', ]",
              "description": "Host attributes forwarded verbatim onto the inner native control — the\ninput-behavior vocabulary the platform only honors on the element that\nactually takes the input. `<vf-text-field autocomplete=\"email\">` used to\nput the token on a custom element the browser ignores for autofill while\nthe shadow `<input>` never received it; same story for the mobile-keyboard\npair (`inputmode`, `enterkeyhint`) and the rest. These are deliberately\n*not* reactive properties: four of them are global attributes with IDL\naccessors already on `HTMLElement` (`spellcheck`, `autocapitalize`,\n`inputMode`, `enterKeyHint`), and a Lit `@property` would shadow the\nplatform member — the kit's own `align`/`draggable` trap. Instead the\nattributes are observed (see `observedAttributes`) and read at render\ntime via forwardedAttr. Subclasses may extend the list\n(`vf-text-field` adds `pattern`) or override a default in their template\n(`vf-number-field` keeps `inputmode=\"decimal\"` unless told otherwise).",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "forwardedAttr",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "string | typeof nothing"
                }
              },
              "parameters": [
                {
                  "name": "name",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "The forwarded value of a host attribute for the inner control's template\nbinding, or `nothing` (remove the attribute) while the host doesn't\ncarry it.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "protected",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "protected",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "focusRule",
              "privacy": "protected",
              "readonly": true,
              "default": "new FocusRuleController(this)",
              "description": "Whether the well wears the kit's dashed focus rule — keyboard focus only.\nSee FocusRuleController for why a field cannot get this from\n`:focus-visible` the way most of the kit does. Typing does NOT reveal the\nrule after a click: the click already showed where the insertion point\nwent, which is the whole reason a field marks keyboard focus and not every\nfocus, so the field never calls the controller's `reveal()`.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "wellClass",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "Classes for the `.vf-field-well` wrapper every field renders around its\nnative control: the shared skin hook (`vfField`), the grid-snap target, and\nthe focus rule's own gate. Assembled here so the three fields state the\nwrapper once and can't drift on what turns the rule on.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Restores the initial value when the associated form resets.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "isSubmitEnter",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ],
              "description": "True for a plain Enter press (no modifiers, not IME-composing) — the key\nthat triggers a form's implicit submission. The native control is\nshadow-encapsulated (null form owner), so single-line fields call\n`internals.form?.requestSubmit()` themselves; this centralises the guard.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "requestImplicitSubmit",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Emulates the form's implicit submission for a plain Enter press. HTML\ndefines it as *activating the form's default button* — the first submit\nbutton in tree order — so the submission carries that button's submitter\nidentity and name/value. A bare `requestSubmit()` carries neither\n(`event.submitter === null`), which made Enter and a click on the same\nform produce different payloads. `click()` is the activation: a native\nbutton becomes the real submitter, and a `vf-button` forwards it to its\ninner button, which runs the same transient-proxy path a pointer does. A\ndisabled default button means no submission at all, as in HTML; a form\nwith no submit button falls back to the bare call, also as in HTML.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "emitValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "type",
                  "type": {
                    "text": "'vf-input' | 'vf-change'"
                  }
                },
                {
                  "name": "detail",
                  "default": "{ value: this.value }",
                  "type": {
                    "text": "Record<string, unknown>"
                  }
                }
              ],
              "description": "Dispatch a bubbling, composed value event (SPEC §4). Text fields use the\ndefault `{ value }` detail; `vf-number-field` passes an enriched detail that\nalso carries `valueAsNumber`.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "handleInput",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Mirror the native control's value into `value` and announce a keystroke.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "handleChange",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Mirror the native control's value into `value` and announce a commit.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The `valueMissing` message; subclasses match their native counterpart's.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "events": [
            {
              "description": "On every keystroke. `detail: { value, valueAsNumber }`.",
              "name": "vf-input"
            },
            {
              "description": "On commit or step. `detail: { value, valueAsNumber }`.",
              "name": "vf-change"
            },
            {
              "description": "The native event: the inner input's own on a keystroke (composed, crosses the boundary itself), dispatched from the host on a step — a native number input's spinner fires it per click too.",
              "name": "input"
            },
            {
              "description": "The native commit event, dispatched from the host on a typed commit and on every step, as a native spinner does. The input-behavior attributes — `autocomplete`, `inputmode`, `enterkeyhint`, `maxlength`, `spellcheck`, `autocapitalize` — are forwarded from the host onto the inner input; unset, `inputmode` stays the numeric keypad's `decimal` and `autocomplete` stays `off`.",
              "name": "change"
            }
          ],
          "attributes": [
            {
              "name": "min",
              "type": {
                "text": "number | undefined"
              },
              "description": "Minimum allowed value (inclusive). Omit for no lower bound.",
              "fieldName": "min"
            },
            {
              "name": "max",
              "type": {
                "text": "number | undefined"
              },
              "description": "Maximum allowed value (inclusive). Omit for no upper bound.",
              "fieldName": "max"
            },
            {
              "name": "step",
              "type": {
                "text": "number"
              },
              "default": "1",
              "description": "Increment applied per step / arrow press. Also sets the value's precision.",
              "fieldName": "step"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Current value. Synced on every keystroke and submitted with forms.",
              "fieldName": "value",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Placeholder text shown when the field is empty.",
              "fieldName": "placeholder",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "readonly",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Makes the field read-only (focusable, not editable).",
              "fieldName": "readonly",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "fieldName": "name",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, applied as `aria-label` on the inner native control (the\nelement that receives focus and is announced by screen readers — a host\nattribute cannot reach it on its own). Left empty, the name falls back to\nwhatever the host carries — `aria-labelledby`, `aria-label` or an\nassociated `<label for>` — via VfFormControl.hostLabel, so the\nplatform's own labeling vocabulary works on these controls too.",
              "fieldName": "label",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "VfTextControlBase",
            "module": "/src/text-control.js"
          },
          "tagName": "vf-number-field",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfNumberField",
          "declaration": {
            "name": "VfNumberField",
            "module": "src/components/vf-number-field.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-number-field",
          "declaration": {
            "name": "VfNumberField",
            "module": "src/components/vf-number-field.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-option.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-option>` — a single choice inside a `<vf-select>` popup menu.\n\nA light-DOM child of `<vf-select>` (slotted into the popup panel). Renders\nits slotted label at menu-item metrics (16px row — the pill's content height,\nso a selected option overlays the closed pill exactly; the left checkmark gutter is\n`--vf-select-gutter`, shared with the closed control's left inset so the value\ndoesn't shift on open). The parent select manages `selected` and the transient\n`active` highlight, and slots this element into its popup panel.\n\nThe host carries `role=\"option\"` with `aria-selected`/`aria-disabled`.",
          "name": "VfOption",
          "cssProperties": [
            {
              "description": "`vf-select` pill (border box; its 1px hard shadow makes the sheet's 157×19 ink box)",
              "name": "--vf-popup-height",
              "default": "18px"
            },
            {
              "description": "checkmark column: `vf-select` left inset / `vf-option` + `vf-menu-item` ✓ column (shared so the value doesn't shift on open)",
              "name": "--vf-select-gutter",
              "default": "16px"
            }
          ],
          "cssParts": [
            {
              "description": "The ✓ checkmark shown in the left gutter when selected.",
              "name": "check"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Submitted/compared value of this option. Falls back to the trimmed text\ncontent when empty (like a native `<option>`).",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the option: gray text, not selectable.",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "selected",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether this option is the select's current value. Managed by the parent.",
              "attribute": "selected",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "active",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Transient highlight (hover / keyboard cursor) — full-row inversion.\nManaged by the parent `<vf-select>`; not part of the authoring API.",
              "attribute": "active",
              "reflects": true
            }
          ],
          "attributes": [
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Submitted/compared value of this option. Falls back to the trimmed text\ncontent when empty (like a native `<option>`).",
              "fieldName": "value"
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the option: gray text, not selectable.",
              "fieldName": "disabled"
            },
            {
              "name": "selected",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether this option is the select's current value. Managed by the parent.",
              "fieldName": "selected"
            },
            {
              "name": "active",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Transient highlight (hover / keyboard cursor) — full-row inversion.\nManaged by the parent `<vf-select>`; not part of the authoring API.",
              "fieldName": "active"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-option",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfOption",
          "declaration": {
            "name": "VfOption",
            "module": "src/components/vf-option.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-option",
          "declaration": {
            "name": "VfOption",
            "module": "src/components/vf-option.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-paragraph.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-paragraph>` — a paragraph of copy on the kit's body face and grid.\n\nThe installer's welcome text, a dialog's explanation, an article's prose:\nanything a page would otherwise set with a `<p>` and hope the leading landed\nsomewhere sensible. What it adds over that `<p>`:\n\n- the **Geneva body face** by default (`face=\"display\"` switches to\n  the Chicago-style chrome face);\n- a **whole-system-pixel line box at the face's native pitch** —\n  `--vf-paragraph-line-height`: 12px for the body face (Geneva 9's own\n  strike line) and 16px under `face=\"display\"` (Chicago 12's — ascent 12 +\n  descent 3 + leading 1, the pitch a real dialog wrapped its copy on).\n  Whole pixels are the point of the component: line boxes are the single\n  biggest source of off-grid layout, because a ratio resolves to whatever\n  it resolves to (`1.65 × 17px = 28.05px`) and every line of prose nudges\n  everything after it further off the device-pixel grid, smearing 1-bit\n  borders and bitmap glyph stems (README, layout contract rule 2). A\n  whole-pixel line box accumulates whole offsets;\n- its own GridSnapController, so it holds its own origin once the\n  page opts in with `applyGridSnap()`;\n- a **declared box** when the layout wants one — `width`/`height` in whole\n  system px (VfSized). In flow a paragraph takes its container's\n  width, which is usually right; a *placed* one (`top`/`left`) shrink-wraps\n  its longest line instead — a fractional glyph-run width, wrapped wherever\n  the parent's edge happens to fall — so a DITL-style layout states the\n  measure the copy wraps to, whole and on the grid.\n\nThe shadow root renders a real `<p>`, so the copy keeps paragraph semantics\nfor assistive tech, and — unlike the kit's chrome — the text is selectable.\nThere is no margin: per SPEC §2 a component adds nothing outside its own box,\nso paragraph spacing is the page's (a `gap` on the column, or a margin on the\nhost — kept a whole number of pixels, like everything else in the contract).",
          "name": "VfParagraph",
          "cssProperties": [
            {
              "description": "This paragraph kind's own line box, in system px. Unset (the default) the box follows the face tokens below; set, it overrides both faces for paragraphs alone. Keep any override a whole *even* number — a ratio is what puts a page off the grid in the first place, and half the difference to the 16px em is half-leading, so an odd value lands the baseline on a half pixel.",
              "name": "--vf-paragraph-line-height"
            },
            {
              "description": "The body face's native line (default `12px`, Geneva 9's) — the face-level knob: retheming the body face to another strike states this alongside `--vf-font-family` / `--vf-font-size`.",
              "name": "--vf-line-height"
            },
            {
              "description": "The display face's native line (default `16px`, Chicago 12's) — the display retheme's third number.",
              "name": "--vf-line-height-display"
            }
          ],
          "cssParts": [
            {
              "description": "The inner `<p>`.",
              "name": "paragraph"
            }
          ],
          "slots": [
            {
              "description": "The paragraph copy.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "face",
              "type": {
                "text": "'display' | 'body' | undefined"
              },
              "description": "Which embedded face to set the copy in — `'body'` (Geneva, the\ndefault) or `'display'` (the Chicago-style chrome face, for copy that is\nreally dialog chrome). Applied by `vfStaticText`; declared here so it types\nand reflects.",
              "attribute": "face",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "dim",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Greys the copy to `--vf-disabled` — System 7's dimmed static text.",
              "attribute": "dim",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "attribute": "width",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "attribute": "height",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "face",
              "type": {
                "text": "'display' | 'body' | undefined"
              },
              "description": "Which embedded face to set the copy in — `'body'` (Geneva, the\ndefault) or `'display'` (the Chicago-style chrome face, for copy that is\nreally dialog chrome). Applied by `vfStaticText`; declared here so it types\nand reflects.",
              "fieldName": "face"
            },
            {
              "name": "dim",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Greys the copy to `--vf-disabled` — System 7's dimmed static text.",
              "fieldName": "dim"
            },
            {
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "fieldName": "width",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "fieldName": "height",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfSized",
              "module": "/src/size.js"
            },
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-paragraph",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfParagraph",
          "declaration": {
            "name": "VfParagraph",
            "module": "src/components/vf-paragraph.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-paragraph",
          "declaration": {
            "name": "VfParagraph",
            "module": "src/components/vf-paragraph.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-progress-bar.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-progress-bar>` — the System 7 progress indicator.\n\nA 14px white track (`--vf-progress-track`) with a 1px black border.\nDeterminate mode fills from the left in solid black\n(`--vf-progress-fill`) with a 1px black leading edge. Indeterminate\nmode shows chunky, stepped diagonal black/white barber stripes (the classic\n\"busy\" bar) animated with `steps()` timing so movement is deliberately\nsteppy, not smooth.\n\nExposes `role=\"progressbar\"` with `aria-valuemin/max` and, when\ndeterminate, `aria-valuenow`.",
          "name": "VfProgressBar",
          "cssProperties": [
            {
              "description": "determinate progress fill (solid black)",
              "name": "--vf-progress-fill",
              "default": "#000000"
            },
            {
              "description": "the indeterminate barber-stripe tile, drawn as rects so the staircase stays whole system px at any scale (override the whole pattern)",
              "name": "--vf-progress-stripes"
            },
            {
              "description": "progress track (white)",
              "name": "--vf-progress-track",
              "default": "#ffffff"
            }
          ],
          "cssParts": [
            {
              "description": "The outer bordered track.",
              "name": "track"
            },
            {
              "description": "The determinate fill or the indeterminate stripe layer.",
              "name": "fill"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Current progress, from 0 to `max`. Clamped for display and ARIA.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "max",
              "type": {
                "text": "number"
              },
              "default": "100",
              "description": "Maximum value (default 100).",
              "attribute": "max"
            },
            {
              "kind": "field",
              "name": "indeterminate",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Barber-pole \"busy\" mode; ignores `value` and omits `aria-valuenow`.",
              "attribute": "indeterminate",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the bar, applied as `aria-label` on the host (which\ncarries `role=\"progressbar\"`). Especially useful in `indeterminate` mode,\nwhere there is no `aria-valuenow` to describe the bar. A consumer-supplied\n`aria-label`/`aria-labelledby` attribute is left alone.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "track",
              "type": {
                "text": "HTMLElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "trackSize",
              "privacy": "private",
              "readonly": true,
              "default": "new TrackWidthController(this, () => this.track)",
              "description": "Track the fill area's width so the determinate fill can snap to it."
            },
            {
              "kind": "field",
              "name": "effectiveMax",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "description": "Effective maximum: a non-positive `max` is treated as the default 100.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "clampedValue",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "description": "`value` clamped to `[0, effectiveMax]`.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "value",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Current progress, from 0 to `max`. Clamped for display and ARIA.",
              "fieldName": "value"
            },
            {
              "name": "max",
              "type": {
                "text": "number"
              },
              "default": "100",
              "description": "Maximum value (default 100).",
              "fieldName": "max"
            },
            {
              "name": "indeterminate",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Barber-pole \"busy\" mode; ignores `value` and omits `aria-valuenow`.",
              "fieldName": "indeterminate"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the bar, applied as `aria-label` on the host (which\ncarries `role=\"progressbar\"`). Especially useful in `indeterminate` mode,\nwhere there is no `aria-valuenow` to describe the bar. A consumer-supplied\n`aria-label`/`aria-labelledby` attribute is left alone.",
              "fieldName": "label"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-progress-bar",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfProgressBar",
          "declaration": {
            "name": "VfProgressBar",
            "module": "src/components/vf-progress-bar.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-progress-bar",
          "declaration": {
            "name": "VfProgressBar",
            "module": "src/components/vf-progress-bar.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-radio-group.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "Groups `vf-radio` children, owning selection, keyboard navigation and the\nform value. Renders no chrome of its own; directly slotted radios stack\nvertically with a 6px gap (override with your own layout if needed —\narbitrary markup containing radios also works).\n\nForm-associated: submits `value` under `name`, restores the initial value\non form reset. Keeps children in sync — the child whose `value` matches\nthe group's `value` is checked, all others unchecked.\n\nKeyboard (classic Mac behavior): the group is one tab stop (roving\ntabindex on the selected radio); ArrowUp/ArrowLeft and\nArrowDown/ArrowRight move the selection AND select it, wrapping around\nand skipping disabled radios.",
          "name": "VfRadioGroup",
          "slots": [
            {
              "description": "`vf-radio` elements, or arbitrary markup containing them.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The value of the selected radio. An empty string means no selection\n(and nothing is submitted with the form).",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting.",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the group, exposed as the `radiogroup`'s `aria-label`.\nWithout it a caption-less group is announced anonymously.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "No radio checked — the state `required` calls a missing value.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The message a native required radio group reports.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "mutationObserver",
              "type": {
                "text": "MutationObserver | null"
              },
              "privacy": "private",
              "default": "null",
              "description": "Re-syncs the group when its radios change structurally or by value —\ncovers radios added/removed inside arbitrary wrapper markup (no top-level\nslotchange fires) and a child radio's value/disabled changing at runtime."
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Form-associated lifecycle: restores the initial value."
            },
            {
              "kind": "field",
              "name": "radios",
              "type": {
                "text": "VfRadio[]"
              },
              "privacy": "private",
              "description": "All descendant radios belonging to this group (nested groups excluded).",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "syncKey",
              "type": {
                "text": "string"
              },
              "privacy": "private",
              "description": "Identity of the state syncRadios pushes down, and the value it\nlast pushed. Everything the children receive derives from these two, so\ncomparing them tells `updated()` whether a sync would be a no-op.\n\nIt intentionally doesn't track the children themselves: a structural or\nvalue/disabled change reaches `syncRadios()` directly (via the mutation\nobserver or slotchange), never through this gate.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "syncedKey",
              "type": {
                "text": "string | null"
              },
              "privacy": "private",
              "default": "null"
            },
            {
              "kind": "method",
              "name": "syncRadios",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Push group state down to the children: checked flags, group-disabled\ndimming, and the roving tabindex (the checked radio is the tab stop;\nwith no selection, the first enabled radio is)."
            },
            {
              "kind": "method",
              "name": "focus",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "options",
                  "optional": true,
                  "type": {
                    "text": "FocusOptions"
                  }
                }
              ],
              "description": "Moves keyboard focus to the group's roving tab stop — the checked radio,\nelse the first enabled one. Overridden because the platform's `focus()`\nis a silent no-op on this host (no `delegatesFocus`, no host tabindex),\nso `vf-label for` (which calls `target.focus()`) never reached the group."
            },
            {
              "kind": "method",
              "name": "selectRadio",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "radio",
                  "type": {
                    "text": "VfRadio"
                  }
                },
                {
                  "name": "focus",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Select a radio, sync everything, and fire `vf-change` if value changed.\n\nThe sync stays synchronous — a `vf-change` listener must see the children\nalready carrying the new selection, not the state of a pending update —\nand `updated()` then skips its own pass via the sync key."
            },
            {
              "kind": "field",
              "name": "handleRadioChange",
              "privacy": "private",
              "description": "A child radio was clicked or Space-selected: adopt it."
            },
            {
              "kind": "field",
              "name": "handleKeydown",
              "privacy": "private",
              "description": "Arrow keys move the selection AND select (classic Mac behavior)."
            },
            {
              "kind": "method",
              "name": "adoptPreChecked",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "With no value of its own, adopt a pre-checked radio's value — and treat\nit as the form-reset default."
            },
            {
              "kind": "field",
              "name": "handleSlotChange",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "role",
              "type": {
                "text": "string"
              },
              "default": "'radiogroup'"
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "events": [
            {
              "description": "When the selection changes via user interaction. `detail: { value: string }`.",
              "name": "vf-change"
            },
            {
              "description": "Native event, dispatched from the group (the form-associated surface) per user pick. A programmatic `value` set fires nothing.",
              "name": "input"
            },
            {
              "description": "Native event, dispatched from the group per user pick so form delegation and framework bindings hear it.",
              "name": "change"
            }
          ],
          "attributes": [
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The value of the selected radio. An empty string means no selection\n(and nothing is submitted with the form).",
              "fieldName": "value"
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting.",
              "fieldName": "name"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the group, exposed as the `radiogroup`'s `aria-label`.\nWithout it a caption-less group is announced anonymously.",
              "fieldName": "label"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "VfFormControl",
            "module": "/src/form-control.js"
          },
          "tagName": "vf-radio-group",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfRadioGroup",
          "declaration": {
            "name": "VfRadioGroup",
            "module": "src/components/vf-radio-group.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-radio-group",
          "declaration": {
            "name": "VfRadioGroup",
            "module": "src/components/vf-radio-group.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-radio.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "A single System 7 radio button: a 13×13 white circle with the pixel-exact\n1-bit ring and centered dot traced from the Classic Macintosh UI Kit sprite\n(replacing the anti-aliased `border-radius` rendering). The ring thickens\nwhile pressed, exactly like the original control.\n\nRadios are meant to live inside a `vf-radio-group`, which owns selection\nstate, the form value and the roving tabindex. A `vf-radio` is NOT itself\nform-associated. Clicking (or pressing Space on) an unselected radio\ndispatches `vf-change`; the containing group listens, selects it and\nunselects its siblings.",
          "name": "VfRadio",
          "cssParts": [
            {
              "description": "The 13×13 radio circle.",
              "name": "circle"
            },
            {
              "description": "The label wrapper around the slot.",
              "name": "label"
            }
          ],
          "slots": [
            {
              "description": "The label, rendered to the right of the circle with a 6px gap.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "checked",
              "type": {
                "text": "boolean"
              },
              "description": "Whether this radio is selected. Managed by the containing group.",
              "default": "false",
              "attribute": "checked",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables this single radio: the label dims to gray; circle and dot stay black.",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The value the containing `vf-radio-group` reports when selected.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "groupDisabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "True while the containing `vf-radio-group` is disabled. Managed by the\ngroup — not intended to be set by consumers."
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "private",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "toggleInternals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "description": "A radio attaches its own internals: it is deliberately NOT form-associated\n(the enclosing `vf-radio-group` is the form surface), so there is no\n`VfFormControl` handle to inherit.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "toggleDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "This radio's own `disabled`, or its group's.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "externallyCoordinated",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "Inside a `vf-radio-group` the group is the single source of truth: it runs\nthe roving tabindex and flips `checked` on the whole set. Standalone, this\nradio owns both — otherwise a bare radio would be keyboard-dead (its Space\nhandler unreachable) and would never visibly toggle.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "activate",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Click/Space on an enabled, unselected radio selects it.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "role",
              "type": {
                "text": "string"
              },
              "default": "'radio'"
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#consumerOwnsTabIndex",
              "privacy": "private",
              "type": {
                "text": "boolean | null"
              },
              "default": "null",
              "description": "Whether the *consumer* authored a `tabindex`, latched on the *first*\nconnect only.\n\nIt has to be latched: once we self-manage, `this.tabIndex = 0` writes a\n`tabindex` attribute onto the host, so re-testing `hasAttribute` on a\nlater connect reads our own write back as the consumer's and stands down\nforever — leaving a reconnected control that no longer drops out of the\ntab order when disabled. Same trap, and the same fix, as `vf-menu-item`'s\n`#ownsRole` latch.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "selfManagedTabIndex",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "True when this control currently owns its host tabindex: the consumer\ndidn't author one and no coordinator is running a roving tabindex.\nRe-derived on every connect, because externallyCoordinated can\nlegitimately change when a control is re-parented into or out of a group.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncToggleState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Mirror the control's state into ARIA and keep its tab stop honest.\nSeparate from `updated()` so a subclass that gates its own work on\nchanged keys can still call it unconditionally.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "#interact",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The one gate every activation passes through.",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#handleClick",
              "privacy": "private",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#handleKeydown",
              "privacy": "private",
              "inheritedFrom": {
                "name": "VfToggleControl",
                "module": "src/toggle-control.ts"
              }
            }
          ],
          "events": [
            {
              "description": "When selected by user interaction. `detail: { value: string }`.",
              "name": "vf-change"
            }
          ],
          "attributes": [
            {
              "name": "checked",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether this radio is selected. Managed by the containing group.",
              "fieldName": "checked"
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables this single radio: the label dims to gray; circle and dot stay black.",
              "fieldName": "disabled"
            },
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The value the containing `vf-radio-group` reports when selected.",
              "fieldName": "value"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            },
            {
              "name": "VfToggleControl",
              "module": "/src/toggle-control.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-radio",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfRadio",
          "declaration": {
            "name": "VfRadio",
            "module": "src/components/vf-radio.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-radio",
          "declaration": {
            "name": "VfRadio",
            "module": "src/components/vf-radio.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-scroll-area.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-scroll-area>` — a container whose scrollbars look like System 7.\n\nWhite box with a 1px black frame and an inner scrolling viewport. The\nWebKit scrollbars are the shared `vfScrollbars` recipe: the classic 16px bar\nwhose outer line is the frame (painted as an overlay above the borderless\nviewport — see the recipe comment for the Safari reason), a loose 1-bit\ndot-dither trough, a white boxed thumb, and boxed arrow buttons that nest\ncleanly under the frame. Firefox falls back to `scrollbar-color`.\n\nEach reserved scroll rail is a permanent placeholder: an empty white channel\nsits in the gutter even when the content fits, filling in with the\ndither/thumb/arrows only once that axis overflows (System 7 behavior, driven\nby ScrollStateController). Which rails are reserved is set by\naxis; when both are reserved the bottom-right corner joins them.\n\nSize the host (width/height) from the outside; the viewport fills it.",
          "name": "VfScrollArea",
          "cssProperties": [
            {
              "description": "scrollbar thumb/elevator (white)",
              "name": "--vf-scrollbar-thumb"
            },
            {
              "description": "scrollbar trough — **Firefox fallback only**; the WebKit path draws the dot-dither tile instead, and this is its flat 25%-black average",
              "name": "--vf-scrollbar-track"
            }
          ],
          "cssParts": [
            {
              "description": "The inner scrolling container.",
              "name": "viewport"
            }
          ],
          "slots": [
            {
              "description": "Scrollable content.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "axis",
              "type": {
                "text": "'vertical' | 'horizontal' | 'both'"
              },
              "default": "'vertical'",
              "description": "Which scroll rails to reserve as permanent placeholders: `vertical`\n(default), `horizontal`, or `both`. Each reserved rail shows an empty white\nchannel until its axis overflows; the unreserved axis scrolls on demand.",
              "attribute": "axis",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the scrolling viewport, applied as its `aria-label`\n(an `aria-label` on the host would not reach into the shadow DOM). The\nviewport is keyboard-focusable while its content overflows, so without a\nname it is announced only as an anonymous scrollable group; setting\n`label` also promotes it to a named `role=\"region\"` landmark. While\n`label` is empty the viewport is a plain `group` when scrollable (an\nunnamed region is inert) and role-less when not.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "viewport",
              "type": {
                "text": "HTMLElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "content",
              "type": {
                "text": "HTMLElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "scrollState",
              "privacy": "private",
              "readonly": true,
              "default": "new ScrollStateController( this, () => this.viewport, () => this.content, (overflow) => { this._scrollable = overflow.x || overflow.y } )",
              "description": "Reports overflow per axis so the reserved rail(s) activate on overflow."
            },
            {
              "kind": "field",
              "name": "_scrollable",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether the content actually overflows the viewport (either axis)."
            },
            {
              "kind": "method",
              "name": "focus",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "options",
                  "optional": true,
                  "type": {
                    "text": "FocusOptions"
                  }
                }
              ],
              "description": "Moves keyboard focus to the scrolling viewport — the focusable element\nlives inside the shadow root, where the platform's `focus()` can't reach\n(no `delegatesFocus`, no host tabindex), so `vf-label for` and consumer\nscripts silently no-opped on the host."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "axis",
              "type": {
                "text": "'vertical' | 'horizontal' | 'both'"
              },
              "default": "'vertical'",
              "description": "Which scroll rails to reserve as permanent placeholders: `vertical`\n(default), `horizontal`, or `both`. Each reserved rail shows an empty white\nchannel until its axis overflows; the unreserved axis scrolls on demand.",
              "fieldName": "axis"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the scrolling viewport, applied as its `aria-label`\n(an `aria-label` on the host would not reach into the shadow DOM). The\nviewport is keyboard-focusable while its content overflows, so without a\nname it is announced only as an anonymous scrollable group; setting\n`label` also promotes it to a named `role=\"region\"` landmark. While\n`label` is empty the viewport is a plain `group` when scrollable (an\nunnamed region is inert) and role-less when not.",
              "fieldName": "label"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-scroll-area",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfScrollArea",
          "declaration": {
            "name": "VfScrollArea",
            "module": "src/components/vf-scroll-area.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-scroll-area",
          "declaration": {
            "name": "VfScrollArea",
            "module": "src/components/vf-scroll-area.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-select.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-select>` — the classic System 7 popup menu control (\"Macintosh HD ▼\").\n\nChildren are `<vf-option>` elements in the default slot. The closed control\nis a white box with a 1px black border, the small 1px hard shadow, the\nselected option's label on the left and a solid black ▼ triangle on the\nright. The open panel uses the shared `.vf-panel` recipe and is positioned\n`position: fixed` (computed from `getBoundingClientRect()`) so it escapes\nclipping containers; when possible the currently-selected item opens\ndirectly over the control, like the real popup menu.\n\nPointer — two interaction styles are supported and disambiguated by the\ngesture itself, resolved at the first pointer release:\n  - System 7 press-drag-release: press the pill (the list appears under the\n    pointer), drag onto an item, release over it to pick — one continuous\n    press. Releasing over the current item or off the list closes with no\n    change.\n  - Modern click-to-open: a quick in-place click (no drag, released within\n    PRESS_HOLD_MS) leaves the list open; a second, independent click\n    then picks an item.\nThe two share one opening trigger (pointerdown) and diverge only on how the\npress ends — whether the pointer travelled to another item, and (for an\nin-place release) whether it was a quick tap or a held press. The pull-down\nmenus drive themselves the same way, on the same threshold; that half lives\nin src/menu-press.ts, because one menu press may travel across a whole bar.\n\nKeyboard: Space/Enter/ArrowDown open; while open ArrowUp/ArrowDown move the\nhighlight, Home/End jump, Enter/Space select, Escape cancels. Selecting an\nitem plays the classic inversion blink (~250 ms) before closing. Keyboard\nfocus is marked with the kit's 1px dashed rule under the closed pill\n(`vfFocusUnderline`) rather than a ring around it — keyboard only, which\nhere means the page's input modality rather than `:focus-visible` (the pill\ndrives its own focus, and Blink reads that as visible either way).\n\nForm-associated: submits `value` under `name`.",
          "name": "VfSelect",
          "cssProperties": [
            {
              "description": "`vf-select` pill (border box; its 1px hard shadow makes the sheet's 157×19 ink box)",
              "name": "--vf-popup-height",
              "default": "18px"
            },
            {
              "description": "scrollbar thumb/elevator (white)",
              "name": "--vf-scrollbar-thumb"
            },
            {
              "description": "scrollbar trough — **Firefox fallback only**; the WebKit path draws the dot-dither tile instead, and this is its flat 25%-black average",
              "name": "--vf-scrollbar-track"
            },
            {
              "description": "checkmark column: `vf-select` left inset / `vf-option` + `vf-menu-item` ✓ column (shared so the value doesn't shift on open)",
              "name": "--vf-select-gutter",
              "default": "16px"
            }
          ],
          "cssParts": [
            {
              "description": "The closed popup control box.",
              "name": "control"
            },
            {
              "description": "The selected-option label inside the control.",
              "name": "label"
            },
            {
              "description": "The black ▼ triangle.",
              "name": "arrow"
            },
            {
              "description": "The popup panel (listbox).",
              "name": "panel"
            }
          ],
          "slots": [
            {
              "description": "`<vf-option>` elements.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "ITEM_HEIGHT",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "16",
              "description": "Fallback height of one option row — the pill's *content* height\n(`--vf-popup-height` 18px minus its two 1px borders). Used to overlay the\nselected row's white cell exactly on the closed pill. `positionPanel`\nprefers the row's *rendered* height so a re-themed `--vf-popup-height`\nkeeps the overlay aligned; this is the no-options fallback and must match\n`vf-option`'s default row height."
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Value of the selected option. Adopts the first enabled option if unset.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the combobox control (`aria-label`). Left empty, the\nname falls back to whatever the host carries — `aria-labelledby`,\n`aria-label` or an associated `<label for>` — via\nVfFormControl.hostLabel; with neither, the control is announced by\nits contents, i.e. the selected value.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "No option committed — the state `required` calls a missing value.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The message a native `<select required>` reports.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Whether the popup panel is open."
            },
            {
              "kind": "field",
              "name": "controlEl",
              "type": {
                "text": "HTMLDivElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "panelEl",
              "type": {
                "text": "HTMLDivElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "assignedOptions",
              "type": {
                "text": "VfOption[]"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "cachedOptions",
              "type": {
                "text": "VfOption[]"
              },
              "privacy": "private",
              "default": "[]",
              "description": "Assigned options, cached from the last slotchange. `render()` reads this\nreactive state (via optionItems) instead of the live query above,\nwhich is empty on the first paint — the source of the empty-label /\nzero-width-sizer flash."
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "focusRule",
              "privacy": "private",
              "readonly": true,
              "default": "new FocusRuleController(this)",
              "description": "Whether the closed pill wears the kit's dashed focus rule — keyboard focus\nonly, which this control can't read off `:focus-visible` (see the CSS)."
            },
            {
              "kind": "field",
              "name": "panelListeners",
              "privacy": "private",
              "readonly": true,
              "default": "new DocumentListenersController(this, () => [ [document, 'pointerdown', this.handleDocumentPointerDown, true], [window, 'scroll', this.handleWindowScroll, true], [window, 'resize', this.handleWindowResize], ])",
              "description": "While the panel is open: outside dismissal, and closing when a scroll or\nresize would strand the fixed-position panel away from the pill."
            },
            {
              "kind": "field",
              "name": "pressListeners",
              "privacy": "private",
              "readonly": true,
              "default": "new DocumentListenersController(this, () => [ [document, 'pointermove', this.handlePressPointerMove, true], [document, 'pointerup', this.handlePressPointerUp, true], [document, 'pointercancel', this.handlePressCancel, true], ])",
              "description": "While a press gesture is in flight: track it to its release anywhere."
            },
            {
              "kind": "field",
              "name": "activeIndex",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "default": "-1",
              "description": "Index of the highlighted option while the panel is open."
            },
            {
              "kind": "field",
              "name": "pressing",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Pointer press-gesture state. A press starts on `pointerdown` and is\nresolved on the matching `pointerup`/`pointercancel`; see the class doc."
            },
            {
              "kind": "field",
              "name": "pressOpenedPanel",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "True when *this* press is the one that opened the panel."
            },
            {
              "kind": "field",
              "name": "pressMoved",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Set once the pointer leaves the option it pressed on (a drag-pick)."
            },
            {
              "kind": "field",
              "name": "pressStartOption",
              "type": {
                "text": "VfOption | null"
              },
              "privacy": "private",
              "default": "null",
              "description": "The option the press started over — origin for pressMoved."
            },
            {
              "kind": "field",
              "name": "pressDownTime",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "default": "0",
              "description": "`event.timeStamp` of the opening pointerdown (for the hold threshold)."
            },
            {
              "kind": "field",
              "name": "pressDownX",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "default": "0"
            },
            {
              "kind": "field",
              "name": "pressDownY",
              "type": {
                "text": "number"
              },
              "privacy": "private",
              "default": "0"
            },
            {
              "kind": "field",
              "name": "swallowClick",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "Swallows the one `click` the browser synthesises after a pointer press\n(pointerdown+pointerup), so the pointer handlers — not\nhandleHostClick — own mouse/touch input. A `click` with no\npreceding pointerdown (keyboard / assistive-tech activation) still reaches\nthe click handler."
            },
            {
              "kind": "field",
              "name": "blinking",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "True while the classic selection blink is playing (input is ignored)."
            },
            {
              "kind": "field",
              "name": "blinkHandle",
              "type": {
                "text": "BlinkHandle | undefined"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "typeAhead",
              "privacy": "private",
              "readonly": true,
              "default": "new TypeAheadBuffer()",
              "description": "First-letter type-ahead over the open list; see src/type-ahead.ts."
            },
            {
              "kind": "method",
              "name": "focus",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "options",
                  "optional": true,
                  "type": {
                    "text": "FocusOptions"
                  }
                }
              ],
              "description": "Focuses the popup control."
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: also re-syncs the form value immediately (the\ngated `updated()` doesn't run on a `formDisabled` change) and closes an open\npanel when an ancestor `<fieldset disabled>` disables us.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Restores the initial value when the associated form resets."
            },
            {
              "kind": "field",
              "name": "optionItems",
              "type": {
                "text": "VfOption[]"
              },
              "privacy": "private",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "optionValue",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "string"
                }
              },
              "parameters": [
                {
                  "name": "option",
                  "type": {
                    "text": "VfOption"
                  }
                }
              ],
              "description": "Resolved value of an option (falls back to its text, like `<option>`)."
            },
            {
              "kind": "method",
              "name": "applySelection",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Marks the option matching `value` as selected."
            },
            {
              "kind": "field",
              "name": "handleSlotChange",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "openPanel",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "Promise<void>"
                }
              }
            },
            {
              "kind": "method",
              "name": "positionPanel",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "selectedIndex",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Positions the fixed-position panel so the selected item sits directly\nover the closed control (classic popup behavior), clamped to the\nviewport with a 4px margin."
            },
            {
              "kind": "method",
              "name": "closePanel",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "refocusControl",
                  "type": {
                    "text": "boolean"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "setActive",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "focusOption",
                  "default": "true"
                }
              ]
            },
            {
              "kind": "method",
              "name": "clearActive",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "moveActive",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "delta",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Moves the highlight by `delta`, skipping disabled options. No wrap."
            },
            {
              "kind": "method",
              "name": "firstEnabledIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number"
                }
              }
            },
            {
              "kind": "method",
              "name": "lastEnabledIndex",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number"
                }
              }
            },
            {
              "kind": "method",
              "name": "selectOption",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "option",
                  "type": {
                    "text": "VfOption"
                  }
                }
              ],
              "description": "Plays the classic selection blink, then commits and closes."
            },
            {
              "kind": "method",
              "name": "cancelBlink",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "commit",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "option",
                  "type": {
                    "text": "VfOption"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "optionFromEvent",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "VfOption | undefined"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ]
            },
            {
              "kind": "field",
              "name": "handleHostClick",
              "privacy": "private",
              "description": "`click` handler for *synthesised* activation — keyboard/assistive-tech\nclicks that arrive with no preceding pointerdown. Real mouse/touch clicks\nare swallowed here (their swallowClick flag was set on pointerdown)\nbecause the pointer handlers already resolved the gesture."
            },
            {
              "kind": "field",
              "name": "handleHostPointerDown",
              "privacy": "private",
              "description": "Starts a press: opens the list if closed, then tracks the press to its\nrelease. Both interaction styles begin here; the gesture is classified in\nhandlePressPointerUp."
            },
            {
              "kind": "field",
              "name": "handlePressPointerMove",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "handlePressPointerUp",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "handlePressCancel",
              "privacy": "private"
            },
            {
              "kind": "method",
              "name": "endPress",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "optionAtPoint",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "VfOption | null"
                }
              },
              "parameters": [
                {
                  "name": "x",
                  "type": {
                    "text": "number"
                  }
                },
                {
                  "name": "y",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "The option whose row currently contains the viewport point, if any."
            },
            {
              "kind": "method",
              "name": "trackHighlight",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "option",
                  "type": {
                    "text": "VfOption | null"
                  }
                }
              ],
              "description": "Live highlight during a press-drag; never moves DOM focus off the control."
            },
            {
              "kind": "method",
              "name": "resolveRelease",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "option",
                  "type": {
                    "text": "VfOption | null"
                  }
                }
              ],
              "description": "Resolves a press that ended as a pick/dismiss (not a click-to-open): commit\na different, enabled option (with the blink); otherwise close with no change\n— released on the current item, a disabled item, or off the list (the\nclassic \"release outside\" cancel)."
            },
            {
              "kind": "field",
              "name": "handleHostKeyDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "handleHostPointerOver",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "handleHostFocusOut",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "handleDocumentPointerDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "handleWindowScroll",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "handleWindowResize",
              "privacy": "private",
              "description": "Close on viewport resize: the fixed panel was placed from the control's\nrect when it opened and would otherwise drift out of alignment."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "events": [
            {
              "description": "After a selection commits. `detail: { value: string }`.",
              "name": "vf-change"
            },
            {
              "description": "Native event, dispatched from the host per committed pick (with `change`, the pair a native `<select>` fires). A programmatic `value` set fires nothing.",
              "name": "input"
            },
            {
              "description": "Native event, dispatched from the host per committed pick so form delegation and framework bindings hear it.",
              "name": "change"
            }
          ],
          "attributes": [
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Value of the selected option. Adopts the first enabled option if unset.",
              "fieldName": "value"
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "fieldName": "name"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the combobox control (`aria-label`). Left empty, the\nname falls back to whatever the host carries — `aria-labelledby`,\n`aria-label` or an associated `<label for>` — via\nVfFormControl.hostLabel; with neither, the control is announced by\nits contents, i.e. the selected value.",
              "fieldName": "label"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "VfFormControl",
            "module": "/src/form-control.js"
          },
          "tagName": "vf-select",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfSelect",
          "declaration": {
            "name": "VfSelect",
            "module": "src/components/vf-select.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-select",
          "declaration": {
            "name": "VfSelect",
            "module": "src/components/vf-select.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-separator.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-separator>` — a 1px System 7 rule.\n\nHorizontal by default (1px tall, full width); set the `vertical` attribute\nfor a 1px-wide vertical rule (give it a height, or let a flex parent\nstretch it).\n\nContainers may restyle it via custom properties:\n- `--vf-separator-color` — line color (default `var(--vf-black, #000)`).\n- `--vf-separator-style` — line style, e.g. `dotted` (default `solid`).\n  `vf-menu` sets these so slotted separators render as the classic dimmed\n  dotted menu rule (see Menus.png).",
          "name": "VfSeparator",
          "cssProperties": [
            {
              "description": "`vf-separator` rule color — `vf-menu` sets it to `--vf-disabled` for the dimmed menu rule",
              "name": "--vf-separator-color"
            },
            {
              "description": "`vf-separator` rule style — `vf-menu` sets `dotted` (see `Menus.png`)",
              "name": "--vf-separator-style",
              "default": "solid"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)"
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "vertical",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Render as a vertical rule (1px wide) instead of horizontal.",
              "attribute": "vertical",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "vertical",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Render as a vertical rule (1px wide) instead of horizontal.",
              "fieldName": "vertical"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-separator",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfSeparator",
          "declaration": {
            "name": "VfSeparator",
            "module": "src/components/vf-separator.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-separator",
          "declaration": {
            "name": "VfSeparator",
            "module": "src/components/vf-separator.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-slider.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-slider>` — a horizontal 1-bit slider.\n\nNot a historically shipped System 7 control: it reverse-adapts the later\nMac OS slider into the library's solid-black, pixel-crisp 1-bit style. A\nrounded capsule rail fills solid black from the left up to a shield-shaped\ndrag handle (the SLIDER_THUMB sprite) and runs hollow after it. The\nrail is regenerated as a whole-pixel SVG on every resize so it stays crisp at\nany width, and the thumb snaps to integer pixels so the sprite never blurs.\n\nForm-associated: submits `value` under `name` and restores it on form reset.\nDrag or click the track to set the value; focus it and use the arrow keys\n(Home/End jump to min/max, PageUp/PageDown move in bigger steps).",
          "name": "VfSlider",
          "cssParts": [
            {
              "description": "The full-width rail row (the pointer target).",
              "name": "track"
            },
            {
              "description": "The `<svg>` capsule (fill + hollow).",
              "name": "rail"
            },
            {
              "description": "The shield-shaped drag handle.",
              "name": "thumb"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Current value, clamped to `[min, max]` and snapped to `step`.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "min",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Minimum value (inclusive).",
              "attribute": "min"
            },
            {
              "kind": "field",
              "name": "max",
              "type": {
                "text": "number"
              },
              "default": "100",
              "description": "Maximum value (inclusive).",
              "attribute": "max"
            },
            {
              "kind": "field",
              "name": "step",
              "type": {
                "text": "number"
              },
              "default": "1",
              "description": "Increment the value snaps to (also sets its display precision).",
              "attribute": "step"
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, exposed as the control's `aria-label`.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "track",
              "type": {
                "text": "HTMLElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "trackSize",
              "privacy": "private",
              "readonly": true,
              "default": "new TrackWidthController(this, () => this.track)",
              "description": "Measured content width of the rail, in px — the rail SVG is regenerated\nfrom it so every edge stays on the device grid."
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "selfManagedTabIndex",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "True when this component owns the host `tabindex`."
            },
            {
              "kind": "field",
              "name": "#focusRule",
              "privacy": "private",
              "readonly": true,
              "default": "new FocusRuleController(this)",
              "description": "Whether the rail wears the kit's dashed focus rule — keyboard focus only,\nwhich this control can't read off `:focus-visible` (see the CSS)."
            },
            {
              "kind": "field",
              "name": "#dragStartValue",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Value captured at pointer-down, to decide whether a drag emitted a change."
            },
            {
              "kind": "field",
              "name": "#dragging",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "True between pointer-down and its release — drives the drag lifecycle so a\ncommit does not depend on pointer capture having been established."
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Restores the initial value when the associated form resets."
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Restored form state parses back to the numeric `value` (the stored state\nis `String(clampedValue)`; the base default would assign the string).",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#range",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#step",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "description": "Effective step: a non-positive `step` snaps continuously (treated as 1).",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#clamp",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number"
                }
              },
              "parameters": [
                {
                  "name": "n",
                  "type": {
                    "text": "number"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#snap",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number"
                }
              },
              "parameters": [
                {
                  "name": "n",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Snap a raw value to the step grid, clamp it, and fix float precision."
            },
            {
              "kind": "field",
              "name": "#clampedValue",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "description": "`value` clamped to `[min, max]` for display/ARIA (unsnapped).",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#fraction",
              "privacy": "private",
              "type": {
                "text": "number"
              },
              "description": "Fraction 0–1 of the current value across the range.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "#update",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "next",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Set the value and emit `vf-input` if it actually changed."
            },
            {
              "kind": "method",
              "name": "#emit",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "type",
                  "type": {
                    "text": "'vf-input' | 'vf-change'"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "#valueFromClientX",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "number"
                }
              },
              "parameters": [
                {
                  "name": "clientX",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Map a client X coordinate to a snapped value. The usable travel is the rail\nwidth inset by half a thumb at each end, so the pointer tracks the thumb's\ncentre while its edges stay within the rail."
            },
            {
              "kind": "field",
              "name": "#onPointerDown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onPointerMove",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#onPointerUp",
              "privacy": "private",
              "description": "End a drag on pointerup / pointercancel / lostpointercapture, and commit\na change if the value moved. Idempotent (releasing capture re-enters here)."
            },
            {
              "kind": "field",
              "name": "#onKeydown",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "role",
              "type": {
                "text": "string"
              },
              "default": "'slider'"
            },
            {
              "kind": "field",
              "name": "ariaOrientation",
              "type": {
                "text": "string"
              },
              "default": "'horizontal'"
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "Whether the control is empty for `required`'s purposes. Overridden by the\ncontrols a value can be missing from — `value === ''` on the fields, the\nselect and the radio group, unchecked on the checkbox. The default never\nfails, which is what makes a bare `required` inert on the rest.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The `valueMissing` message; subclasses match their native counterpart's.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "events": [
            {
              "description": "On every drag move or key change. `detail: { value: number }`.",
              "name": "vf-input"
            },
            {
              "description": "On commit (pointer release, or key change). `detail: { value: number }`.",
              "name": "vf-change"
            },
            {
              "description": "Native event, dispatched from the host on every user value move — a native range input's cadence. A programmatic `value` set fires nothing.",
              "name": "input"
            },
            {
              "description": "Native event, dispatched from the host on commit (release, or a key change) so form delegation and framework bindings hear it.",
              "name": "change"
            }
          ],
          "attributes": [
            {
              "name": "value",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Current value, clamped to `[min, max]` and snapped to `step`.",
              "fieldName": "value"
            },
            {
              "name": "min",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "Minimum value (inclusive).",
              "fieldName": "min"
            },
            {
              "name": "max",
              "type": {
                "text": "number"
              },
              "default": "100",
              "description": "Maximum value (inclusive).",
              "fieldName": "max"
            },
            {
              "name": "step",
              "type": {
                "text": "number"
              },
              "default": "1",
              "description": "Increment the value snaps to (also sets its display precision).",
              "fieldName": "step"
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "fieldName": "name"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, exposed as the control's `aria-label`.",
              "fieldName": "label"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "VfFormControl",
            "module": "/src/form-control.js"
          },
          "tagName": "vf-slider",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfSlider",
          "declaration": {
            "name": "VfSlider",
            "module": "src/components/vf-slider.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-slider",
          "declaration": {
            "name": "VfSlider",
            "module": "src/components/vf-slider.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-stack.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-stack>` — arrange things inside a window, in system pixels.\n\nA window body is the one place the kit stopped short: every control inside it\nis authored in system px, but the *spaces between* them were the consumer's\nproblem, written by hand as `calc(var(--vf-scale, 1) * 12px)`. This is that\ncalculation, as a component — a flexbox whose `gap`, `pad`, `width` and\n`height` are declared in whole system px and converted internally:\n\n```html\n<vf-stack gap=\"12\">                            <!-- a column -->\n  <vf-stack fill-width direction=\"row\" gap=\"8\">   <!-- a labeled field -->\n    <vf-label width=\"80\" for=\"name\">Name:</vf-label>\n    <vf-text-field id=\"name\" fill-width></vf-text-field>\n  </vf-stack>\n  <vf-stack fill-width place=\"end\">                <!-- an action row -->\n    <vf-button-group>\n      <vf-button>Cancel</vf-button>\n      <vf-button variant=\"default\">Save</vf-button>\n    </vf-button-group>\n  </vf-stack>\n</vf-stack>\n```\n\n**Why this can't just be page CSS.** Scaling is default-on and *per\ncomponent*: `ScaleController` sets `--vf-scale` on the component's own host,\nnot on the document. So `var(--vf-scale, 1)` in a consumer's stylesheet\nresolves only where the rule's element happens to sit inside a `vf-*`\nancestor and inherit it — true inside a window body, false for a plain\n`<div>` holding two buttons on an ordinary page, where the fallback `1` wins\nand the gap renders at 8px around 3×-sized buttons with no warning. A page\nthat hasn't called `applyScale()` has no way to write \"8 system px\" at all.\nA component always can, because it *is* the scope — which is also why this\none carries a ScaleController of its own: without it a lone stack\nwould resolve its own gap against that same fallback while its children each\nself-scaled around it.\n\n**Whole system px is the only expressible value**, so the gap half of the\nlayout contract (README rule 2) stops being a rule to remember. Declaring\n`width`/`height` covers the size half of rule 3 as well.\n\n**The geometry is governed by the content.** A column is as wide as its\nwidest child and a row as tall as its tallest; children keep the size they\ndrew themselves at (`flex: 0 0 auto` — no growing, no shrinking). System 7\nboxes are the size they are: a push button is as wide as its label, a popup\nmenu hugs its widest option, and a window is a fixed box whose overflow is\nclipped at the frame, not a layout that squeezes its controls to fit. The\nstack distributes; it does not resize. That is why it is `inline-flex` rather\nthan block-level — a layout box that silently claimed its parent's whole\nwidth would be inventing a size nobody declared.\n\n**`fill-width` / `fill-height` are how a child asks for more**, as bare\nattributes on consumer DOM the way `nosnap` opts an element out of snapping:\n\n```html\n<vf-text-field fill-width></vf-text-field>\n```\n\nEach names the *outcome*, not the axis, so the markup means the same thing\nwherever it lands; the stack does the flexbox translation, which is the whole\nreason to have a component. One rule to learn, about geometry rather than\nvocabulary: **the cross axis always has a size, the main axis only has slack\nif you declared one.** So `fill-width` always works in a column (the width is\nthe widest child's) and needs a declared `width` in a row; `fill-height` is\nexactly the other way round. A fill with nothing to take is inert, not an\nerror. Two children filling along the main axis end up *equal* — the zeroed\nflex basis is what lets them divide the slack rather than keep their natural\nsizes — and a child that declares its own size shouldn't also ask to fill it.\n\nA stack reads the same two attributes about *itself*, for the parents that\naren't stacks: a window body, a fieldset, a scroll well, a grid cell. That is\nwhere a panel's width enters the tree, and from there `fill-width` hands it\ndown a level at a time.\n\n**It paints nothing and means nothing.** No border, no background, no role,\nno keyboard behavior — what it holds decides what it is, as with\nVfGrid. It carries no `GridSnapController` either: with nothing of\nits own on screen there is no ink to hold on the grid, and the slotted `vf-*`\nchildren each correct their own origin.\n\nIt is also the kit's one **typographically transparent** component: `vfBase`'s\nchrome face, ratio line box, color and non-selectability are all reset to\n`inherit` on the host. Wrapping content in a layout box must not change how\nthat content reads — inside a window it goes on inheriting the window's face\nexactly as before, and on a plain page it leaves the page's typography (and\nits whole-pixel line boxes) alone.\n\nWhat it deliberately does **not** do: equalize its children's widths. A row of\nbuttons still belongs in a `vf-button-group`, which sizes them all to the\nwidest and aligns their *faces* rather than the `variant=\"default\"` ring\nboxes a plain flex row would line up.",
          "name": "VfStack",
          "slots": [
            {
              "description": "The children to arrange. `fill-width` / `fill-height` on any of them.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "direction",
              "type": {
                "text": "VfStackDirection"
              },
              "default": "'column'",
              "description": "Which axis the children run along — `column` (the default) or `row`.\n\nA stack is vertical unless it says otherwise, the way the word is used\neverywhere else; a row is the one that reads as a deliberate choice in the\nmarkup.",
              "attribute": "direction",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "gap",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "The space between children, in whole system px. `0` by default: a layout\nbox that spaced things out on its own would be exactly the kind of\nsurprise SPEC §2 rules out when it says a component renders nothing\nunexpected outside its box.",
              "attribute": "gap"
            },
            {
              "kind": "field",
              "name": "pad",
              "type": {
                "text": "string | number | undefined"
              },
              "description": "Padding inside the stack, in whole system px — one to four values in the\nusual CSS shorthand order (`pad=\"12\"`, `pad=\"10 12\"`, `pad=\"14 12 10\"`).\n\nLeave it off inside a `vf-window` body, which already carries its own 12px\ninset; a stack that means to own that inset goes inside `<vf-window flush>`.",
              "attribute": "pad"
            },
            {
              "kind": "field",
              "name": "place",
              "type": {
                "text": "VfStackPlace | undefined"
              },
              "description": "Where the children sit across the stack — `start`, `center` or `end`.\nUnset resolves per direction: `start` down a column, `center` across a row.\n\nNamed `place` rather than `align` for a reason worth keeping in the source:\n`align` is a legacy HTML presentation attribute, and Blink maps it to\n`text-align` on any element, so the cross-axis switch used to re-align every\nrun of copy inside the stack (see the `text-align` reset above).\n\nNote the one thing centering cannot do: land on a whole pixel by itself. A\n16px caption centered in a row set by the 25-system-px `vf-number-field`\nsits at 4.5 system px, and no container can round that — it would have to\nread each child's height. `applyGridSnap()` keeps the caption's own ink\ncrisp regardless (it corrects the origin inside the child's shadow root);\n`place=\"start\"` is the deterministic escape.",
              "attribute": "place",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "attribute": "width",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "attribute": "height",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "direction",
              "type": {
                "text": "VfStackDirection"
              },
              "default": "'column'",
              "description": "Which axis the children run along — `column` (the default) or `row`.\n\nA stack is vertical unless it says otherwise, the way the word is used\neverywhere else; a row is the one that reads as a deliberate choice in the\nmarkup.",
              "fieldName": "direction"
            },
            {
              "name": "gap",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "The space between children, in whole system px. `0` by default: a layout\nbox that spaced things out on its own would be exactly the kind of\nsurprise SPEC §2 rules out when it says a component renders nothing\nunexpected outside its box.",
              "fieldName": "gap"
            },
            {
              "name": "pad",
              "type": {
                "text": "string | number | undefined"
              },
              "description": "Padding inside the stack, in whole system px — one to four values in the\nusual CSS shorthand order (`pad=\"12\"`, `pad=\"10 12\"`, `pad=\"14 12 10\"`).\n\nLeave it off inside a `vf-window` body, which already carries its own 12px\ninset; a stack that means to own that inset goes inside `<vf-window flush>`.",
              "fieldName": "pad"
            },
            {
              "name": "place",
              "type": {
                "text": "VfStackPlace | undefined"
              },
              "description": "Where the children sit across the stack — `start`, `center` or `end`.\nUnset resolves per direction: `start` down a column, `center` across a row.\n\nNamed `place` rather than `align` for a reason worth keeping in the source:\n`align` is a legacy HTML presentation attribute, and Blink maps it to\n`text-align` on any element, so the cross-axis switch used to re-align every\nrun of copy inside the stack (see the `text-align` reset above).\n\nNote the one thing centering cannot do: land on a whole pixel by itself. A\n16px caption centered in a row set by the 25-system-px `vf-number-field`\nsits at 4.5 system px, and no container can round that — it would have to\nread each child's height. `applyGridSnap()` keeps the caption's own ink\ncrisp regardless (it corrects the origin inside the child's shadow root);\n`place=\"start\"` is the deterministic escape.",
              "fieldName": "place"
            },
            {
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "fieldName": "width",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "fieldName": "height",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfSized",
              "module": "/src/size.js"
            },
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-stack",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfStack",
          "declaration": {
            "name": "VfStack",
            "module": "src/components/vf-stack.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-stack",
          "declaration": {
            "name": "VfStack",
            "module": "src/components/vf-stack.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-swatch.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-swatch>` — a color-swatch button: the color well of a palette cell.\n\nA solid rectangle with a 1px black border and a 1px white inset ring, filled\nedge-to-edge with a solid color. Given no `color` at all it shows the\ntransparency checker instead — the \"this cell holds nothing\" grid every\npaint program draws behind an empty fill.\n\n`width`/`height` state the border box in whole system pixels (the fill is\nwhat remains inside the border and inset), multiplied by `--vf-scale` like\nevery other metric, so the swatch holds the device-pixel grid wherever a\npalette puts it.\n\n`shadow` opts into the kit's hard drop shadow — the shared\n`--vf-shadow-offset` token, the same depth cue as windows, menus and alerts,\npainted outside the box like theirs. It is off by default because the\nswatch's usual home is a table of them (a `vf-grid`, a picker row), where\nevery cell shadowing its neighbour reads as noise rather than depth; a lone\nwell standing in for a current color — the one a dialog asks you to click —\nis the case that wants it.\n\nIt is \"basically a button\": a native `<button>` inside, so `click` retargets\nto the host, Enter/Space activate it, and pressing inverts the white inset\nto black — the inset counterpart of vf-button's face inversion. Keyboard\nfocus is marked with the kit's 1px dashed rule under the box\n(`vfFocusUnderline`) rather than a ring around it. It is form-associated\nfor the *disabled* contract alone — an ancestor `<fieldset disabled>` must\nreach a palette the way it reaches every other control, and only the\nform-associated lifecycle delivers that across the shadow boundary — but it\nsubmits nothing (a palette cell picks, it doesn't submit; no `name`, no\nvalue, no `FormData` entry). `disabled` only stops interaction, dimming\nnothing — the kit dims *labels* when disabled, and a swatch's only label is\nits fill, which must keep reading as its color.",
          "name": "VfSwatch",
          "cssProperties": [
            {
              "description": "`vf-swatch`'s no-color transparency checker — a 4×4 tile of 2×2 white/`#c0c0c0` checks (override the whole pattern like `--vf-desktop-pattern`)",
              "name": "--vf-swatch-checker"
            }
          ],
          "cssParts": [
            {
              "description": "The inner native `<button>` (border, inset and, with `shadow`, the drop shadow).",
              "name": "button"
            },
            {
              "description": "The color area inside the inset.",
              "name": "fill"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "shadowRootOptions",
              "type": {
                "text": "ShadowRootInit"
              },
              "static": true,
              "default": "{ ...VfFormControl.shadowRootOptions, delegatesFocus: true, }"
            },
            {
              "kind": "field",
              "name": "color",
              "type": {
                "text": "string | undefined"
              },
              "description": "The fill — a CSS color, typically the hex the palette calls for. Unset,\nthe swatch shows the transparency checker; a translucent value (an\n8-digit hex) layers over that same checker, so partial opacity reads as\npartial. An unparseable value falls back to the checker too.",
              "attribute": "color"
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number"
              },
              "default": "24",
              "description": "The swatch's border box width in whole system px.",
              "attribute": "width"
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number"
              },
              "default": "18",
              "description": "The swatch's border box height in whole system px.",
              "attribute": "height"
            },
            {
              "kind": "field",
              "name": "shadow",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Cast the kit's hard drop shadow (`--vf-shadow-offset`). Off by default:\nthe plain black-bordered well is what a table of swatches wants, and the\ndepth cue is for the lone well that stands in for a current color.",
              "attribute": "shadow",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the inner button — a swatch has no text of its own.\nLeft empty, the name falls back to whatever the host carries\n(`aria-labelledby`, `aria-label`, an associated `<label for>` — see\nVfFormControl.hostLabel), then to the `color` value (or\n\"transparent\") as a last resort, so a palette is never nameless — but a\nhex literal announced as \"number f f six six zero zero\" is a fallback to\nname past, not a name. `vf-label`'s `for` wiring writes this property,\nlike every `vf-*` control's.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding. Nothing dims (see class doc).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "Whether the control is empty for `required`'s purposes. Overridden by the\ncontrols a value can be missing from — `value === ''` on the fields, the\nselect and the radio group, unchecked on the checkbox. The default never\nfails, which is what makes a bare `required` inert on the rest.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The `valueMissing` message; subclasses match their native counterpart's.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "color",
              "type": {
                "text": "string | undefined"
              },
              "description": "The fill — a CSS color, typically the hex the palette calls for. Unset,\nthe swatch shows the transparency checker; a translucent value (an\n8-digit hex) layers over that same checker, so partial opacity reads as\npartial. An unparseable value falls back to the checker too.",
              "fieldName": "color"
            },
            {
              "name": "width",
              "type": {
                "text": "number"
              },
              "default": "24",
              "description": "The swatch's border box width in whole system px.",
              "fieldName": "width"
            },
            {
              "name": "height",
              "type": {
                "text": "number"
              },
              "default": "18",
              "description": "The swatch's border box height in whole system px.",
              "fieldName": "height"
            },
            {
              "name": "shadow",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Cast the kit's hard drop shadow (`--vf-shadow-offset`). Off by default:\nthe plain black-bordered well is what a table of swatches wants, and the\ndepth cue is for the lone well that stands in for a current color.",
              "fieldName": "shadow"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name for the inner button — a swatch has no text of its own.\nLeft empty, the name falls back to whatever the host carries\n(`aria-labelledby`, `aria-label`, an associated `<label for>` — see\nVfFormControl.hostLabel), then to the `color` value (or\n\"transparent\") as a last resort, so a palette is never nameless — but a\nhex literal announced as \"number f f six six zero zero\" is a fallback to\nname past, not a name. `vf-label`'s `for` wiring writes this property,\nlike every `vf-*` control's.",
              "fieldName": "label"
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding. Nothing dims (see class doc).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "VfFormControl",
            "module": "/src/form-control.js"
          },
          "tagName": "vf-swatch",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfSwatch",
          "declaration": {
            "name": "VfSwatch",
            "module": "src/components/vf-swatch.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-swatch",
          "declaration": {
            "name": "VfSwatch",
            "module": "src/components/vf-swatch.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-text-area.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-text-area>` — a System 7 multi-line text entry field.\n\nIdentical styling to `<vf-text-field>` but wrapping a native `<textarea>`.\nNo resize grip (`resize: none`) — System 7 fields don't resize. Wrapped\ntext sits on Chicago 12's native 16px line (the single-line well keeps its\n20px box — that is control geometry, not typesetting). The shared field\nskin lives in `vfField`; the value/form scaffolding in\nVfTextControlBase.\n\nThe `.vf-field-well` wrapper is the one `vfField` hangs the focus rule from,\ndoing double duty here: it is also the positioned box the `.vf-scroll-frame`\noverlay insets against, and the `vf-snap` element whose grid-snap offset the\nfield, the frame and the rule ride as one.\n\nThe vertical scroll rail is a permanent System 7 placeholder: an empty white\nchannel sits in the field even when the text fits, filling in with the\ndither/thumb/arrows only once the content overflows (driven by\nScrollStateController). The scrollbar wears the shared `vfScrollbars`\nskin — the same one used by `vf-scroll-area` and `vf-list`.",
          "name": "VfTextArea",
          "cssProperties": [
            {
              "description": "The display face's native line (default `16px`, Chicago 12's) — the pitch wrapped entry text sits on, shared with the static-text components so a display retheme moves them together.",
              "name": "--vf-line-height-display"
            },
            {
              "description": "default width of `vf-text-field` / `vf-text-area`",
              "name": "--vf-field-width",
              "default": "180px"
            },
            {
              "description": "placeholder text in the editable fields — kept off `--vf-disabled`: a placeholder sits in an *enabled* well and holds AA contrast, where the disabled gray is exempt",
              "name": "--vf-field-placeholder",
              "default": "#767676"
            },
            {
              "description": "scrollbar thumb/elevator (white)",
              "name": "--vf-scrollbar-thumb"
            },
            {
              "description": "scrollbar trough — **Firefox fallback only**; the WebKit path draws the dot-dither tile instead, and this is its flat 25%-black average",
              "name": "--vf-scrollbar-track"
            }
          ],
          "cssParts": [
            {
              "description": "The inner native `<textarea>` element.",
              "name": "textarea"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "rows",
              "type": {
                "text": "number"
              },
              "default": "4",
              "description": "Number of visible text rows (native `rows`). Default 4.",
              "attribute": "rows"
            },
            {
              "kind": "field",
              "name": "textarea",
              "type": {
                "text": "HTMLTextAreaElement | null"
              },
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "scrollState",
              "privacy": "private",
              "readonly": true,
              "default": "new ScrollStateController( this, () => this.textarea )",
              "description": "Reserves the vertical rail and toggles it active on overflow."
            },
            {
              "kind": "method",
              "name": "handleInput",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Re-measure overflow on each keystroke. A `<textarea>`'s scrollHeight grows\nas the text wraps without resizing the element's box, so the controller's\nResizeObserver never fires — this is the imperative path it documents.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "shadowRootOptions",
              "type": {
                "text": "ShadowRootInit"
              },
              "static": true,
              "default": "{ ...VfFormControl.shadowRootOptions, delegatesFocus: true, }",
              "description": "Focus delegates into the inner native control.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Current value. Synced on every keystroke and submitted with forms.",
              "attribute": "value",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Placeholder text shown when the field is empty.",
              "attribute": "placeholder",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "readonly",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Makes the field read-only (focusable, not editable).",
              "attribute": "readonly",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "attribute": "name",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, applied as `aria-label` on the inner native control (the\nelement that receives focus and is announced by screen readers — a host\nattribute cannot reach it on its own). Left empty, the name falls back to\nwhatever the host carries — `aria-labelledby`, `aria-label` or an\nassociated `<label for>` — via VfFormControl.hostLabel, so the\nplatform's own labeling vocabulary works on these controls too.",
              "attribute": "label",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "A field is empty for `required`'s purposes when its value is `''`.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "forwardedAttributes",
              "type": {
                "text": "readonly string[]"
              },
              "privacy": "protected",
              "static": true,
              "readonly": true,
              "default": "[ 'autocomplete', 'inputmode', 'enterkeyhint', 'maxlength', 'spellcheck', 'autocapitalize', ]",
              "description": "Host attributes forwarded verbatim onto the inner native control — the\ninput-behavior vocabulary the platform only honors on the element that\nactually takes the input. `<vf-text-field autocomplete=\"email\">` used to\nput the token on a custom element the browser ignores for autofill while\nthe shadow `<input>` never received it; same story for the mobile-keyboard\npair (`inputmode`, `enterkeyhint`) and the rest. These are deliberately\n*not* reactive properties: four of them are global attributes with IDL\naccessors already on `HTMLElement` (`spellcheck`, `autocapitalize`,\n`inputMode`, `enterKeyHint`), and a Lit `@property` would shadow the\nplatform member — the kit's own `align`/`draggable` trap. Instead the\nattributes are observed (see `observedAttributes`) and read at render\ntime via forwardedAttr. Subclasses may extend the list\n(`vf-text-field` adds `pattern`) or override a default in their template\n(`vf-number-field` keeps `inputmode=\"decimal\"` unless told otherwise).",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "forwardedAttr",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "string | typeof nothing"
                }
              },
              "parameters": [
                {
                  "name": "name",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "The forwarded value of a host attribute for the inner control's template\nbinding, or `nothing` (remove the attribute) while the host doesn't\ncarry it.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "protected",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "protected",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "focusRule",
              "privacy": "protected",
              "readonly": true,
              "default": "new FocusRuleController(this)",
              "description": "Whether the well wears the kit's dashed focus rule — keyboard focus only.\nSee FocusRuleController for why a field cannot get this from\n`:focus-visible` the way most of the kit does. Typing does NOT reveal the\nrule after a click: the click already showed where the insertion point\nwent, which is the whole reason a field marks keyboard focus and not every\nfocus, so the field never calls the controller's `reveal()`.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "wellClass",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "Classes for the `.vf-field-well` wrapper every field renders around its\nnative control: the shared skin hook (`vfField`), the grid-snap target, and\nthe focus rule's own gate. Assembled here so the three fields state the\nwrapper once and can't drift on what turns the rule on.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Restores the initial value when the associated form resets.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "isSubmitEnter",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ],
              "description": "True for a plain Enter press (no modifiers, not IME-composing) — the key\nthat triggers a form's implicit submission. The native control is\nshadow-encapsulated (null form owner), so single-line fields call\n`internals.form?.requestSubmit()` themselves; this centralises the guard.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "requestImplicitSubmit",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Emulates the form's implicit submission for a plain Enter press. HTML\ndefines it as *activating the form's default button* — the first submit\nbutton in tree order — so the submission carries that button's submitter\nidentity and name/value. A bare `requestSubmit()` carries neither\n(`event.submitter === null`), which made Enter and a click on the same\nform produce different payloads. `click()` is the activation: a native\nbutton becomes the real submitter, and a `vf-button` forwards it to its\ninner button, which runs the same transient-proxy path a pointer does. A\ndisabled default button means no submission at all, as in HTML; a form\nwith no submit button falls back to the bare call, also as in HTML.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "emitValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "type",
                  "type": {
                    "text": "'vf-input' | 'vf-change'"
                  }
                },
                {
                  "name": "detail",
                  "default": "{ value: this.value }",
                  "type": {
                    "text": "Record<string, unknown>"
                  }
                }
              ],
              "description": "Dispatch a bubbling, composed value event (SPEC §4). Text fields use the\ndefault `{ value }` detail; `vf-number-field` passes an enriched detail that\nalso carries `valueAsNumber`.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "handleChange",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Mirror the native control's value into `value` and announce a commit.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The `valueMissing` message; subclasses match their native counterpart's.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "events": [
            {
              "description": "On every keystroke. `detail: { value: string }`.",
              "name": "vf-input"
            },
            {
              "description": "On commit (native `change`). `detail: { value: string }`.",
              "name": "vf-change"
            },
            {
              "description": "The native keystroke event: the inner textarea's own, composed, so it crosses the shadow boundary and retargets to the host.",
              "name": "input"
            },
            {
              "description": "The native commit event, re-dispatched from the host (the inner one is `composed: false` and never leaves the shadow root). The input-behavior attributes — `autocomplete`, `inputmode`, `enterkeyhint`, `maxlength`, `spellcheck`, `autocapitalize` — are forwarded from the host onto the inner textarea, where the platform actually honors them (`pattern` is not: only an `<input>` takes it).",
              "name": "change"
            }
          ],
          "attributes": [
            {
              "name": "rows",
              "type": {
                "text": "number"
              },
              "default": "4",
              "description": "Number of visible text rows (native `rows`). Default 4.",
              "fieldName": "rows"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Current value. Synced on every keystroke and submitted with forms.",
              "fieldName": "value",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Placeholder text shown when the field is empty.",
              "fieldName": "placeholder",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "readonly",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Makes the field read-only (focusable, not editable).",
              "fieldName": "readonly",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "fieldName": "name",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, applied as `aria-label` on the inner native control (the\nelement that receives focus and is announced by screen readers — a host\nattribute cannot reach it on its own). Left empty, the name falls back to\nwhatever the host carries — `aria-labelledby`, `aria-label` or an\nassociated `<label for>` — via VfFormControl.hostLabel, so the\nplatform's own labeling vocabulary works on these controls too.",
              "fieldName": "label",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "VfTextControlBase",
            "module": "/src/text-control.js"
          },
          "tagName": "vf-text-area",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfTextArea",
          "declaration": {
            "name": "VfTextArea",
            "module": "src/components/vf-text-area.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-text-area",
          "declaration": {
            "name": "VfTextArea",
            "module": "src/components/vf-text-area.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-text-field.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-text-field>` — a System 7 single-line text entry field.\n\nA form-associated wrapper around a native `<input>`: white well, 1px solid\nblack border, no corner radius. Keyboard focus draws the kit's dashed rule\none blank row under the well; a click leaves it unmarked (no dotted outline\neither, per SPEC §1/§5). The shared field skin lives in `vfField`; the\nvalue/form scaffolding — and the focus rule's modality gate — in\nVfTextControlBase.",
          "name": "VfTextField",
          "cssProperties": [
            {
              "description": "text fields — `vf-text-field`, `vf-text-area`, the `vf-number-field` well",
              "name": "--vf-control-height",
              "default": "22px"
            },
            {
              "description": "default width of `vf-text-field` / `vf-text-area`",
              "name": "--vf-field-width",
              "default": "180px"
            },
            {
              "description": "placeholder text in the editable fields — kept off `--vf-disabled`: a placeholder sits in an *enabled* well and holds AA contrast, where the disabled gray is exempt",
              "name": "--vf-field-placeholder",
              "default": "#767676"
            }
          ],
          "cssParts": [
            {
              "description": "The inner native `<input>` element.",
              "name": "input"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "type",
              "type": {
                "text": "string"
              },
              "default": "'text'",
              "description": "Input type, passed through to the native input (e.g. `password`).",
              "attribute": "type"
            },
            {
              "kind": "field",
              "name": "forwardedAttributes",
              "type": {
                "text": "readonly string[]"
              },
              "privacy": "protected",
              "static": true,
              "readonly": true,
              "default": "[ ...VfTextControlBase.forwardedAttributes, 'pattern', ]",
              "description": "The shared forwarded set plus `pattern`, which only an `<input>` takes.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "handleKeydown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ],
              "description": "Enter in a single-line field triggers the associated form's implicit\nsubmission. The native `<input>` is shadow-encapsulated, so its form owner\nis null and the browser won't do this itself."
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "shadowRootOptions",
              "type": {
                "text": "ShadowRootInit"
              },
              "static": true,
              "default": "{ ...VfFormControl.shadowRootOptions, delegatesFocus: true, }",
              "description": "Focus delegates into the inner native control.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Current value. Synced on every keystroke and submitted with forms.",
              "attribute": "value",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Placeholder text shown when the field is empty.",
              "attribute": "placeholder",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "readonly",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Makes the field read-only (focusable, not editable).",
              "attribute": "readonly",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "attribute": "name",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, applied as `aria-label` on the inner native control (the\nelement that receives focus and is announced by screen readers — a host\nattribute cannot reach it on its own). Left empty, the name falls back to\nwhatever the host carries — `aria-labelledby`, `aria-label` or an\nassociated `<label for>` — via VfFormControl.hostLabel, so the\nplatform's own labeling vocabulary works on these controls too.",
              "attribute": "label",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "A field is empty for `required`'s purposes when its value is `''`.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "forwardedAttr",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "string | typeof nothing"
                }
              },
              "parameters": [
                {
                  "name": "name",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "The forwarded value of a host attribute for the inner control's template\nbinding, or `nothing` (remove the attribute) while the host doesn't\ncarry it.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "protected",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "protected",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "focusRule",
              "privacy": "protected",
              "readonly": true,
              "default": "new FocusRuleController(this)",
              "description": "Whether the well wears the kit's dashed focus rule — keyboard focus only.\nSee FocusRuleController for why a field cannot get this from\n`:focus-visible` the way most of the kit does. Typing does NOT reveal the\nrule after a click: the click already showed where the insertion point\nwent, which is the whole reason a field marks keyboard focus and not every\nfocus, so the field never calls the controller's `reveal()`.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "wellClass",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "Classes for the `.vf-field-well` wrapper every field renders around its\nnative control: the shared skin hook (`vfField`), the grid-snap target, and\nthe focus rule's own gate. Assembled here so the three fields state the\nwrapper once and can't drift on what turns the rule on.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Restores the initial value when the associated form resets.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "isSubmitEnter",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ],
              "description": "True for a plain Enter press (no modifiers, not IME-composing) — the key\nthat triggers a form's implicit submission. The native control is\nshadow-encapsulated (null form owner), so single-line fields call\n`internals.form?.requestSubmit()` themselves; this centralises the guard.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "requestImplicitSubmit",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Emulates the form's implicit submission for a plain Enter press. HTML\ndefines it as *activating the form's default button* — the first submit\nbutton in tree order — so the submission carries that button's submitter\nidentity and name/value. A bare `requestSubmit()` carries neither\n(`event.submitter === null`), which made Enter and a click on the same\nform produce different payloads. `click()` is the activation: a native\nbutton becomes the real submitter, and a `vf-button` forwards it to its\ninner button, which runs the same transient-proxy path a pointer does. A\ndisabled default button means no submission at all, as in HTML; a form\nwith no submit button falls back to the bare call, also as in HTML.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "emitValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "type",
                  "type": {
                    "text": "'vf-input' | 'vf-change'"
                  }
                },
                {
                  "name": "detail",
                  "default": "{ value: this.value }",
                  "type": {
                    "text": "Record<string, unknown>"
                  }
                }
              ],
              "description": "Dispatch a bubbling, composed value event (SPEC §4). Text fields use the\ndefault `{ value }` detail; `vf-number-field` passes an enriched detail that\nalso carries `valueAsNumber`.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "handleInput",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Mirror the native control's value into `value` and announce a keystroke.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "handleChange",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Mirror the native control's value into `value` and announce a commit.",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The `valueMissing` message; subclasses match their native counterpart's.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "events": [
            {
              "description": "On every keystroke. `detail: { value: string }`.",
              "name": "vf-input"
            },
            {
              "description": "On commit (native `change`). `detail: { value: string }`.",
              "name": "vf-change"
            },
            {
              "description": "The native keystroke event: the inner input's own, composed, so it crosses the shadow boundary and retargets to the host by itself.",
              "name": "input"
            },
            {
              "description": "The native commit event, re-dispatched from the host (the inner one is `composed: false` and never leaves the shadow root), so form delegation and framework bindings hear it. The input-behavior attributes — `autocomplete`, `inputmode`, `enterkeyhint`, `maxlength`, `pattern`, `spellcheck`, `autocapitalize` — are forwarded from the host onto the inner input, where the platform actually honors them.",
              "name": "change"
            }
          ],
          "attributes": [
            {
              "name": "type",
              "type": {
                "text": "string"
              },
              "default": "'text'",
              "description": "Input type, passed through to the native input (e.g. `password`).",
              "fieldName": "type"
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Current value. Synced on every keystroke and submitted with forms.",
              "fieldName": "value",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Placeholder text shown when the field is empty.",
              "fieldName": "placeholder",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "readonly",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Makes the field read-only (focusable, not editable).",
              "fieldName": "readonly",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "fieldName": "name",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, applied as `aria-label` on the inner native control (the\nelement that receives focus and is announced by screen readers — a host\nattribute cannot reach it on its own). Left empty, the name falls back to\nwhatever the host carries — `aria-labelledby`, `aria-label` or an\nassociated `<label for>` — via VfFormControl.hostLabel, so the\nplatform's own labeling vocabulary works on these controls too.",
              "fieldName": "label",
              "inheritedFrom": {
                "name": "VfTextControlBase",
                "module": "src/text-control.ts"
              }
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "VfTextControlBase",
            "module": "/src/text-control.js"
          },
          "tagName": "vf-text-field",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfTextField",
          "declaration": {
            "name": "VfTextField",
            "module": "src/components/vf-text-field.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-text-field",
          "declaration": {
            "name": "VfTextField",
            "module": "src/components/vf-text-field.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/components/vf-window.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "`<vf-window>` — the System 7 desktop-window shell.\n\nStriped title bar with optional close box (left) and zoom box (right), a\nsolid-white frame with a hard offset shadow, an optional grow box for\nresizing, optional edge scroll rails (`scrollbars`), and the slim windoid\nchrome (`variant=\"utility\"`). The HIG's window archetypes are parameter\nrecipes over this shell rather than fixed anatomies — the component enables\nHIG compliance, it doesn't enforce it (see README \"window archetypes\"):\nthe full document window is `closable zoomable movable resizable\nscrollbars=\"both\"`, a modeless dialog box is `closable movable`, a utility\nwindow is `variant=\"utility\" movable`. Place inside `<vf-desktop>` to get\nclick-to-front stacking and automatic `active` management (utility windows\nfloat above the document tier).\n\nEvery recipe also declares `width` AND `height` (VfSized), in whole\nsystem px — the art's own unit, so the window keeps its proportions to the\nchrome inside it at every display density (a CSS-px size stays put while\nthe components in it triple). A window is a fixed box in both axes, the way\nthe WIND resource carried it: left to layout it takes whatever its\ncontainer or content hands it, which is how a title bar ends up wider than\nthe screen or a dialog reflows as it moves — and a window that grows with\nits body is one the user can neither predict nor (via the grow box) own.\nContent taller than the declared box is clipped at the frame the way the\nclassic content region was; `scrollbars` lets the user reach the rest.\nUnset, the window still renders — normal block layout, as before — and\nsays so once in the console.",
          "name": "VfWindow",
          "cssProperties": [
            {
              "description": "the windoid bar's dot-grid dither — a 2×2 tile, one black pixel at the origin (`vfDots`; override the whole pattern like `--vf-desktop-pattern`)",
              "name": "--vf-dots-pattern"
            },
            {
              "description": "window/dialog title bars",
              "name": "--vf-titlebar-height"
            },
            {
              "description": "the slim `vf-window[variant=\"utility\"]` (windoid) bar — 11px interior + 1px bottom rule, traced from `Windows/utility-window.png`",
              "name": "--vf-titlebar-height-utility",
              "default": "12px"
            }
          ],
          "cssParts": [
            {
              "description": "The outer chrome frame.",
              "name": "frame"
            },
            {
              "description": "The striped (or dithered) title bar.",
              "name": "title-bar"
            },
            {
              "description": "The centered title patch (hidden on the utility bar).",
              "name": "title"
            },
            {
              "description": "The close widget (left).",
              "name": "close-box"
            },
            {
              "description": "The zoom widget (right).",
              "name": "zoom-box"
            },
            {
              "description": "The content area.",
              "name": "body"
            },
            {
              "description": "The resize widget (bottom-right, when `resizable`).",
              "name": "grow-box"
            },
            {
              "description": "The built-in scroll area's viewport (when `scrollbars`; re-exported from vf-scroll-area).",
              "name": "viewport"
            }
          ],
          "slots": [
            {
              "description": "Default slot: window body content.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "variant",
              "type": {
                "text": "'utility' | undefined"
              },
              "description": "Chrome variant. Omit for the standard 18px striped bar; `'utility'` for\nthe slim windoid bar (dot-grid dither, 7×7 widgets, no title patch — the\nheading still names the widgets). Inside a `vf-desktop`, utility windows\nfloat above the document tier and never take the single-active state.",
              "attribute": "variant",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "heading",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Title text shown centered in the title bar.",
              "attribute": "heading"
            },
            {
              "kind": "field",
              "name": "active",
              "type": {
                "text": "boolean"
              },
              "default": "true",
              "description": "Whether this is the frontmost (active) window: stripes and widgets show.\nManaged automatically by an enclosing `vf-desktop`.",
              "attribute": "active",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "closable",
              "type": {
                "text": "boolean"
              },
              "default": "true",
              "description": "Show the close box (left side of the title bar).",
              "attribute": "closable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "zoomable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Show the zoom box (right side of the title bar).",
              "attribute": "zoomable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "movable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Allow dragging the window by its title bar.",
              "attribute": "movable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "resizable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Show a grow box at the bottom-right corner for resizing.",
              "attribute": "resizable",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "flush",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Remove the default 12px body padding.",
              "attribute": "flush",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "scrollbars",
              "type": {
                "text": "'vertical' | 'horizontal' | 'both' | undefined"
              },
              "description": "Put System 7 scroll rails on the window edge — the classic document\nwindow. The body slot renders inside a built-in `vf-scroll-area` pulled\none system pixel under the frame on every side, so the rails repaint the\nborder lines and a `resizable` window's grow box lands in the corner\ncell. Values mirror `vf-scroll-area`'s `axis`; the `heading` names the\nscroll region; the viewport part is re-exported. Implies `flush` (the\nviewport carries its own padding). The slotted composition (SPEC §5\nvf-scroll-area) still works for windows that want an inset well instead.",
              "attribute": "scrollbars",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "_drag",
              "privacy": "private",
              "readonly": true,
              "default": "new DragController(this, { onDragStart: (event: PointerEvent): { x: number; y: number } | null => { if (!this.movable || event.button !== 0) return null // Ignore drags that start on the close/zoom widgets. if ( event .composedPath() .some( (node) => node instanceof HTMLElement && node.classList.contains('box') ) ) { return null } // Seed absolute positioning from the current in-flow offset the first // time the window is dragged. Every coordinate is snapped onto the // system-pixel grid (scale.ts): a fractional origin fringes all the // 1-bit art inside, and a half-CSS-px edge shifts WebKit's rails. const computed = getComputedStyle(this) if (computed.position !== 'absolute') { const left = snapToSystemPx(this.offsetLeft, this) const top = snapToSystemPx(this.offsetTop, this) this.style.position = 'absolute' this.style.left = `${left}px` this.style.top = `${top}px` this.style.margin = '0' } else { // Already absolute: re-seed the inline coordinates from the COMPUTED // ones before every drag. They are resolved pixels whatever the author // wrote — an inline `left: 1em` or `left: 10%` is a perfectly good way // to place a window, and reading `style.left` back would parse it as // the bare number and jump the window there on the first move. Once // the drag owns the coordinates they are already px, so this is a // no-op from the second drag on. this.style.left = `${snapToSystemPx(parseFloat(computed.left) || 0, this)}px` this.style.top = `${snapToSystemPx(parseFloat(computed.top) || 0, this)}px` } return { x: parseFloat(this.style.left) || 0, y: parseFloat(this.style.top) || 0, } }, onDrag: (x: number, y: number): void => { // Keep a grabbable strip on-screen: clamp the origin against the // positioning parent (the desktop, usually) so the window can't be // dragged fully past an edge and lost. Re-snap after clamping so the // clamped edge still lands on the system grid. const parent = this.offsetParent as HTMLElement | null const pw = parent?.clientWidth ?? window.innerWidth const ph = parent?.clientHeight ?? window.innerHeight const keep = sys(24, this) // px of the window that must stay reachable const nx = Math.min(Math.max(x, keep - this.offsetWidth), pw - keep) const ny = Math.min(Math.max(y, 0), Math.max(0, ph - keep)) this.style.left = `${snapToSystemPx(nx, this)}px` this.style.top = `${snapToSystemPx(ny, this)}px` }, })",
              "description": "Title-bar drag-to-move (shared with `vf-dialog` via DragController).\nThe delegate seeds absolute positioning on the first drag and writes the\nsnapped `left`/`top` back; the controller owns the pointer bookkeeping."
            },
            {
              "kind": "field",
              "name": "_resizeState",
              "type": {
                "text": "ResizeState | null"
              },
              "privacy": "private",
              "default": "null"
            },
            {
              "kind": "field",
              "name": "#warnedNoSize",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "One warning per element, not per render."
            },
            {
              "kind": "method",
              "name": "#warnIfUnsized",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Both dimensions are required: a window is a fixed box in both axes, and\neach one left out falls back to a different wrong thing.\n\nAn inline `width`/`height` is the other honest way to declare it — the grow\nbox writes exactly that — so it counts. A stylesheet rule we cannot tell\napart from normal block layout, so it still warns."
            },
            {
              "kind": "method",
              "name": "_onCloseClick",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "_onZoomClick",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "_onGrowPointerDown",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "PointerEvent"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "_onGrowPointerMove",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "PointerEvent"
                  }
                }
              ]
            },
            {
              "kind": "method",
              "name": "_onGrowPointerEnd",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "PointerEvent"
                  }
                }
              ],
              "description": "Ends a grow-box resize on pointerup / pointercancel / lostpointercapture.\nIdempotent — releasing the capture below re-enters here, and by then\n`_resizeState` is already null."
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "attribute": "width",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "attribute": "height",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "events": [
            {
              "description": "Close box clicked. Detail `{ reason: 'close' }` (shape- compatible with vf-dialog's `vf-close`). The window does NOT remove itself; the consumer decides what closing means.",
              "name": "vf-close"
            },
            {
              "description": "Zoom box clicked. Detail `{}`.",
              "name": "vf-zoom"
            }
          ],
          "attributes": [
            {
              "name": "variant",
              "type": {
                "text": "'utility' | undefined"
              },
              "description": "Chrome variant. Omit for the standard 18px striped bar; `'utility'` for\nthe slim windoid bar (dot-grid dither, 7×7 widgets, no title patch — the\nheading still names the widgets). Inside a `vf-desktop`, utility windows\nfloat above the document tier and never take the single-active state.",
              "fieldName": "variant"
            },
            {
              "name": "heading",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Title text shown centered in the title bar.",
              "fieldName": "heading"
            },
            {
              "name": "active",
              "type": {
                "text": "boolean"
              },
              "default": "true",
              "description": "Whether this is the frontmost (active) window: stripes and widgets show.\nManaged automatically by an enclosing `vf-desktop`.",
              "fieldName": "active"
            },
            {
              "name": "closable",
              "type": {
                "text": "boolean"
              },
              "default": "true",
              "description": "Show the close box (left side of the title bar).",
              "fieldName": "closable"
            },
            {
              "name": "zoomable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Show the zoom box (right side of the title bar).",
              "fieldName": "zoomable"
            },
            {
              "name": "movable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Allow dragging the window by its title bar.",
              "fieldName": "movable"
            },
            {
              "name": "resizable",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Show a grow box at the bottom-right corner for resizing.",
              "fieldName": "resizable"
            },
            {
              "name": "flush",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Remove the default 12px body padding.",
              "fieldName": "flush"
            },
            {
              "name": "scrollbars",
              "type": {
                "text": "'vertical' | 'horizontal' | 'both' | undefined"
              },
              "description": "Put System 7 scroll rails on the window edge — the classic document\nwindow. The body slot renders inside a built-in `vf-scroll-area` pulled\none system pixel under the frame on every side, so the rails repaint the\nborder lines and a `resizable` window's grow box lands in the corner\ncell. Values mirror `vf-scroll-area`'s `axis`; the `heading` names the\nscroll region; the viewport part is re-exported. Implies `flush` (the\nviewport carries its own padding). The slotted composition (SPEC §5\nvf-scroll-area) still works for windows that want an inset well instead.",
              "fieldName": "scrollbars"
            },
            {
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "fieldName": "width",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "fieldName": "height",
              "inheritedFrom": {
                "name": "VfSized",
                "module": "src/size.ts"
              }
            },
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left",
              "inheritedFrom": {
                "name": "VfPositioned",
                "module": "src/position.ts"
              }
            }
          ],
          "mixins": [
            {
              "name": "VfSized",
              "module": "/src/size.js"
            },
            {
              "name": "VfPositioned",
              "module": "/src/position.js"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "tagName": "vf-window",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfWindow",
          "declaration": {
            "name": "VfWindow",
            "module": "src/components/vf-window.ts"
          }
        },
        {
          "kind": "custom-element-definition",
          "name": "vf-window",
          "declaration": {
            "name": "VfWindow",
            "module": "src/components/vf-window.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/form-control.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "Shared base for the kit's form-associated controls (SPEC §4).\n\nOwns the boilerplate every one of them repeated: the `ElementInternals`\nhandle, the reflected `disabled` property, the `formDisabled` state fed by\n`formDisabledCallback` (an ancestor `<fieldset disabled>`), the resolved\nisDisabled getter, the form-reset default latch\n(latchFormDefault / formDefault), and — crucially — the\ndisabled-guarded syncFormValue funnel every subclass routes its\n`setFormValue` through, so a disabled control can never leak a value into\nsubmission.\n\nSubclasses supply their own value semantics, ARIA and (where needed) a\n`formResetCallback` restoring their captured default.",
          "name": "VfFormControl",
          "members": [
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals."
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass."
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control."
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape."
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it."
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description"
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting."
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`."
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults."
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override."
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place."
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear."
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit."
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not."
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure."
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "Whether the control is empty for `required`'s purposes. Overridden by the\ncontrols a value can be missing from — `value === ''` on the fields, the\nselect and the radio group, unchecked on the checkbox. The default never\nfails, which is what makes a bare `required` inert on the rest.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The `valueMissing` message; subclasses match their native counterpart's.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction."
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out."
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false"
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default."
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type."
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate."
            }
          ],
          "attributes": [
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled"
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description"
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfFormControl",
          "declaration": {
            "name": "VfFormControl",
            "module": "src/form-control.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/text-control.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "Shared base for the kit's editable text fields — `vf-text-field`,\n`vf-text-area` and `vf-number-field`. Builds on VfFormControl (form\nassociation + the disabled-value guard) and adds the value/label scaffolding\nevery field repeated: the reflected `name`, the `value`/`placeholder`/\n`readonly`/`label` props, the captured default restored on form reset, the\n`updated()` → VfFormControl.syncFormValue funnel, and the display-\nscaling controller. Pairs with the `vfField` css fragment (the white-well\nskin) in styles/base.ts.\n\nSubclasses render their own native control (`<input>`/`<textarea>` plus any\nadornments), tag it `class=\"vf-field\"`, and may enrich the emitted event\ndetail via emitValue.",
          "name": "VfTextControlBase",
          "members": [
            {
              "kind": "field",
              "name": "shadowRootOptions",
              "type": {
                "text": "ShadowRootInit"
              },
              "static": true,
              "default": "{ ...VfFormControl.shadowRootOptions, delegatesFocus: true, }",
              "description": "Focus delegates into the inner native control."
            },
            {
              "kind": "field",
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Current value. Synced on every keystroke and submitted with forms.",
              "attribute": "value"
            },
            {
              "kind": "field",
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Placeholder text shown when the field is empty.",
              "attribute": "placeholder"
            },
            {
              "kind": "field",
              "name": "readonly",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Makes the field read-only (focusable, not editable).",
              "attribute": "readonly",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "attribute": "name",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, applied as `aria-label` on the inner native control (the\nelement that receives focus and is announced by screen readers — a host\nattribute cannot reach it on its own). Left empty, the name falls back to\nwhatever the host carries — `aria-labelledby`, `aria-label` or an\nassociated `<label for>` — via VfFormControl.hostLabel, so the\nplatform's own labeling vocabulary works on these controls too.",
              "attribute": "label"
            },
            {
              "kind": "field",
              "name": "valueMissing",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "A field is empty for `required`'s purposes when its value is `''`.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "forwardedAttributes",
              "type": {
                "text": "readonly string[]"
              },
              "privacy": "protected",
              "static": true,
              "readonly": true,
              "default": "[ 'autocomplete', 'inputmode', 'enterkeyhint', 'maxlength', 'spellcheck', 'autocapitalize', ]",
              "description": "Host attributes forwarded verbatim onto the inner native control — the\ninput-behavior vocabulary the platform only honors on the element that\nactually takes the input. `<vf-text-field autocomplete=\"email\">` used to\nput the token on a custom element the browser ignores for autofill while\nthe shadow `<input>` never received it; same story for the mobile-keyboard\npair (`inputmode`, `enterkeyhint`) and the rest. These are deliberately\n*not* reactive properties: four of them are global attributes with IDL\naccessors already on `HTMLElement` (`spellcheck`, `autocapitalize`,\n`inputMode`, `enterKeyHint`), and a Lit `@property` would shadow the\nplatform member — the kit's own `align`/`draggable` trap. Instead the\nattributes are observed (see `observedAttributes`) and read at render\ntime via forwardedAttr. Subclasses may extend the list\n(`vf-text-field` adds `pattern`) or override a default in their template\n(`vf-number-field` keeps `inputmode=\"decimal\"` unless told otherwise)."
            },
            {
              "kind": "method",
              "name": "forwardedAttr",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "string | typeof nothing"
                }
              },
              "parameters": [
                {
                  "name": "name",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "The forwarded value of a host attribute for the inner control's template\nbinding, or `nothing` (remove the attribute) while the host doesn't\ncarry it."
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "protected",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "protected",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "focusRule",
              "privacy": "protected",
              "readonly": true,
              "default": "new FocusRuleController(this)",
              "description": "Whether the well wears the kit's dashed focus rule — keyboard focus only.\nSee FocusRuleController for why a field cannot get this from\n`:focus-visible` the way most of the kit does. Typing does NOT reveal the\nrule after a click: the click already showed where the insertion point\nwent, which is the whole reason a field marks keyboard focus and not every\nfocus, so the field never calls the controller's `reveal()`."
            },
            {
              "kind": "field",
              "name": "wellClass",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "Classes for the `.vf-field-well` wrapper every field renders around its\nnative control: the shared skin hook (`vfField`), the grid-snap target, and\nthe focus rule's own gate. Assembled here so the three fields state the\nwrapper once and can't drift on what turns the rule on.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Restores the initial value when the associated form resets."
            },
            {
              "kind": "method",
              "name": "isSubmitEnter",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "KeyboardEvent"
                  }
                }
              ],
              "description": "True for a plain Enter press (no modifiers, not IME-composing) — the key\nthat triggers a form's implicit submission. The native control is\nshadow-encapsulated (null form owner), so single-line fields call\n`internals.form?.requestSubmit()` themselves; this centralises the guard."
            },
            {
              "kind": "method",
              "name": "requestImplicitSubmit",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Emulates the form's implicit submission for a plain Enter press. HTML\ndefines it as *activating the form's default button* — the first submit\nbutton in tree order — so the submission carries that button's submitter\nidentity and name/value. A bare `requestSubmit()` carries neither\n(`event.submitter === null`), which made Enter and a click on the same\nform produce different payloads. `click()` is the activation: a native\nbutton becomes the real submitter, and a `vf-button` forwards it to its\ninner button, which runs the same transient-proxy path a pointer does. A\ndisabled default button means no submission at all, as in HTML; a form\nwith no submit button falls back to the bare call, also as in HTML."
            },
            {
              "kind": "method",
              "name": "emitValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "type",
                  "type": {
                    "text": "'vf-input' | 'vf-change'"
                  }
                },
                {
                  "name": "detail",
                  "default": "{ value: this.value }",
                  "type": {
                    "text": "Record<string, unknown>"
                  }
                }
              ],
              "description": "Dispatch a bubbling, composed value event (SPEC §4). Text fields use the\ndefault `{ value }` detail; `vf-number-field` passes an enriched detail that\nalso carries `valueAsNumber`."
            },
            {
              "kind": "method",
              "name": "handleInput",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Mirror the native control's value into `value` and announce a keystroke."
            },
            {
              "kind": "method",
              "name": "handleChange",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "event",
                  "type": {
                    "text": "Event"
                  }
                }
              ],
              "description": "Mirror the native control's value into `value` and announce a commit."
            },
            {
              "kind": "field",
              "name": "formAssociated",
              "type": {
                "text": "boolean"
              },
              "static": true,
              "default": "true",
              "description": "Participates in native forms via ElementInternals.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "internals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true,
              "description": "Form + ARIA internals; attached once here for every subclass.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "attribute": "disabled",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "formDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "default": "false",
              "description": "True while an ancestor `<fieldset disabled>` disables this control.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "isDisabled",
              "type": {
                "text": "boolean"
              },
              "description": "Effective disabled state: the `disabled` prop OR an ancestor fieldset.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "bridgedAriaAttributes",
              "type": {
                "text": "array"
              },
              "privacy": "private",
              "static": true,
              "readonly": true,
              "default": "[ 'aria-label', 'aria-labelledby', 'aria-describedby', ]",
              "description": "The host-level ARIA attributes the bridge below mirrors inward, observed\nso a consumer writing one after upgrade re-renders the control. They are\ndeliberately not reactive properties: each has an IDL accessor on\n`Element` already, and a Lit `@property` would shadow the platform member\n(the kit's `align`/`draggable` trap) — so they are observed by name and\nread at render time, the `forwardedAttributes` shape.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formAssociatedCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "_form",
                  "type": {
                    "text": "HTMLFormElement | null"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the association changed, so the `<label for>`\nset feeding hostLabel may have too — re-render whatever mirrors it.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "hostLabel",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The accessible name the *host* carries, for controls whose role lives on a\nshadow-internal node (the fields, `vf-select`, `vf-swatch`). On those, a\nconsumer's `aria-label`, `aria-labelledby` or `<label for>` used to be\nsilently inert — the host is a generic wrapper AccName never consults, and\na host-level IDREF cannot reach into a shadow tree — so the bridge\nresolves them to text for the control to hand to its inner focusable\nelement. The explicit `label` property still wins: templates read\n`this.label || this.hostLabel`.\n\nPrecedence is html-aam's — `aria-labelledby`, then `aria-label`, then the\nassociated `<label for>` elements (`internals.labels`). Referenced text is\nflattened at render time, so an edit to a referenced element's *text*\nlands on the control's next render rather than instantly — the one\ndivergence from a native control, recorded in SPEC §4.\n\nControls whose role sits on the host itself (the toggles, the slider, the\nradio group, the bars) never need this: the platform reads their host\nattributes directly.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "attribute": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "descriptionText",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "What renderDescription's span carries: the current validation\nmessage while the control is invalid, then the description (the property,\nor the bridged host `aria-describedby` text).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "describedBy",
              "type": {
                "text": "string | typeof nothing"
              },
              "privacy": "protected",
              "description": "`aria-describedby` value for the inner control — set only while the span\nhas something to say, so an idle control isn't announced as described by\nnothing.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "renderDescription",
              "privacy": "protected",
              "description": "The hidden span the inner control's `aria-describedby` points at.\n`hidden` keeps it out of the page; AccName still resolves `display: none`\nreference targets (the fact vf-window's utility title patch leans on), so\nthe text reaches AT without painting.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "disabled",
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser calls this when this control's\ndisabled state changes — an ancestor `<fieldset disabled>`, or the\nelement's OWN `disabled` attribute (the browser counts both).\n\nAlso mirrored as the `form-disabled` custom state: `disabled` reflects as\nan attribute a stylesheet can see, but a `<fieldset disabled>` ancestor\nlands here in otherwise-private state — `:state(form-disabled)` is the\none selector that lets consumer CSS style that case. Optional-chained for\nengines without `CustomStateSet`.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                },
                {
                  "name": "_mode",
                  "type": {
                    "text": "'restore' | 'autofill'"
                  }
                }
              ],
              "description": "Form-associated lifecycle: the browser hands back state it stored for\nthis control — a bfcache/session restore, or a browser autofill pass.\nThe kit's controls all submit through single-argument `setFormValue`, so\nthe stored state IS the last submitted string. Without this callback,\nevery native input in the form repopulates on restore while the `vf-*`\ncontrols silently keep their defaults.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "applyFormState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Maps stored form state back onto the control's own value semantics. The\ndefault covers the string-valued majority (the fields, `vf-select`,\n`vf-radio-group`); `vf-checkbox` (a checked flag) and `vf-slider` (a\nnumber) override.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncFormValue",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "string | File | FormData | null"
                  }
                }
              ],
              "description": "Submit `value` under the control's `name` — unless the control is disabled,\nin which case nothing is submitted (SPEC §4). The single guarded funnel for\nevery subclass's form value, so the disabled contract holds in one place.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "attribute": "required",
              "reflects": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#customError",
              "privacy": "private",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "The message setCustomValidity installed; `''` when clear.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "message",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Installs a custom validity message — the native channel: a non-empty\nstring makes the control invalid with exactly that message, `''` clears\nit.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validity",
              "type": {
                "text": "ValidityState"
              },
              "description": "The control's current `ValidityState`, as on a native control.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "type": {
                "text": "string"
              },
              "description": "The message of the currently failing constraint, `''` while valid.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "willValidate",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the control is a candidate for constraint validation — false\nwhile disabled or readonly, per HTML's barring rules (the browser\ncomputes this from the reflected attributes).",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "checkValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "True when the control satisfies its constraints; fires `invalid` if not.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "reportValidity",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "description": "checkValidity plus the browser's own error UI on failure.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "valueMissingMessage",
              "type": {
                "text": "string"
              },
              "privacy": "protected",
              "description": "The `valueMissing` message; subclasses match their native counterpart's.",
              "readonly": true,
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "syncValidity",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The single funnel every validity write goes through — the\nsyncFormValue shape, one place for the contract: the flags are\ncomputed from the live properties (`required` × valueMissing,\nplus any custom error) and installed with `setValidity`, so `:invalid`\nmatches on the host and `form.reportValidity()` blocks exactly as it\nwould on a native control. Runs from willUpdate, before render,\nso the same update's template reads fresh validity. Also mirrors\n`aria-required`/`aria-invalid` through internals for the controls whose\nrole sits on the host — internals lose to a consumer's own host\nattribute, which is the correct precedence direction.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefault",
              "privacy": "private",
              "type": {
                "text": "unknown"
              },
              "description": "The value formDefault hands back for the subclass's\n`formResetCallback`. `unknown` because each control latches its own value\ntype (string, number, boolean); formDefault casts it back out.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "field",
              "name": "#formDefaultLatched",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "latchFormDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "value",
                  "type": {
                    "text": "unknown"
                  }
                }
              ],
              "description": "Capture the control's form-reset default. Only the first call latches, so\nlifecycle paths that can run more than once — reconnects, repeated\nslotchanges — may call it unconditionally. WHEN to call it is each\ncontrol's own contract: most latch on first connect, `vf-select` waits\nuntil options exist, `vf-radio-group` lets a pre-checked radio adopted on\nslotchange claim an unauthored default.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "formDefault",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "T"
                }
              },
              "parameters": [
                {
                  "name": "fallback",
                  "type": {
                    "text": "T"
                  }
                }
              ],
              "description": "The latched form-reset default, or `fallback` while nothing has latched.\nThe cast is sound because a control only ever latches its own value type.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "kind": "method",
              "name": "disabledChanged",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "boolean"
                }
              },
              "parameters": [
                {
                  "name": "changed",
                  "type": {
                    "text": "PropertyValues"
                  }
                }
              ],
              "description": "True when this update changed the *resolved* disabled state — the gate a\nsubclass's `updated()` should use before re-running its form value, ARIA\nor tab-stop writes.\n\nIt exists because formDisabled is protected, so `keyof this` can't\nname it and a `PropertyValues<this>` gate silently can't test for it: a\n`changed.has('disabled')` check compiles, reads as complete, and misses\nevery ancestor `<fieldset disabled>` — which is the path that must clear a\ncontrol's submitted value. One named predicate keeps that trap in one\nplace instead of in each subclass's gate.",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "attributes": [
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Current value. Synced on every keystroke and submitted with forms.",
              "fieldName": "value"
            },
            {
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Placeholder text shown when the field is empty.",
              "fieldName": "placeholder"
            },
            {
              "name": "readonly",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Makes the field read-only (focusable, not editable).",
              "fieldName": "readonly"
            },
            {
              "name": "name",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Form field name used when submitting the associated form.",
              "fieldName": "name"
            },
            {
              "name": "label",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Accessible name, applied as `aria-label` on the inner native control (the\nelement that receives focus and is announced by screen readers — a host\nattribute cannot reach it on its own). Left empty, the name falls back to\nwhatever the host carries — `aria-labelledby`, `aria-label` or an\nassociated `<label for>` — via VfFormControl.hostLabel, so the\nplatform's own labeling vocabulary works on these controls too.",
              "fieldName": "label"
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Disables the control: it stops responding and submits no value. Each\ncontrol dims per SPEC §1 (typically the label greys while the 1-bit chrome\nstays black).",
              "fieldName": "disabled",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "description",
              "type": {
                "text": "string"
              },
              "default": "''",
              "description": "Description for the control — hint text, a format, a unit. A host-level\n`aria-describedby` cannot reach a focusable element inside a shadow root,\nso there was structurally no way to describe a field; this property is\nthat channel. It renders as a hidden span in the control's own shadow root\nwith the inner control's `aria-describedby` pointing at it — the\nshadow-internal IDREF idiom `vf-dialog`'s title patch already uses. A host-level\n`aria-describedby` is bridged into the same span when this property is\nempty, and a failing constraint's validationMessage joins it too,\nso AT hears the error where it hears the hint.",
              "fieldName": "description",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Requires a value before the associated form submits (SPEC §4): an empty\ncontrol fails constraint validation with `valueMissing`, exactly like a\nnative `required`. What \"empty\" means is each control's own\nvalueMissing; a control with no required semantics (a slider\nalways has a value, a swatch submits nothing) never fails it, the way a\nnative range input never does.",
              "fieldName": "required",
              "inheritedFrom": {
                "name": "VfFormControl",
                "module": "src/form-control.ts"
              }
            }
          ],
          "superclass": {
            "name": "VfFormControl",
            "module": "/src/form-control.js"
          }
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfTextControlBase",
          "declaration": {
            "name": "VfTextControlBase",
            "module": "src/text-control.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/toggle-control.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "The mixin's own surface: what a concrete toggle must supply, and what it\ngets back. Spelled out as a `declare class` because TypeScript cannot name\nthe anonymous class a mixin returns when emitting declarations (TS4094) —\nand that is unavoidable here rather than a consequence of our own members,\nsince LitElement's `render`/`updated`/`willUpdate`/`createRenderRoot` are\nthemselves protected. This is the shape Lit documents for typed mixins.\n\nKeep it in sync with the implementation below; it is the type half of one\ndeclaration.",
          "name": "VfToggleControlInterface",
          "members": [
            {
              "kind": "field",
              "name": "checked",
              "type": {
                "text": "boolean"
              }
            },
            {
              "kind": "field",
              "name": "toggleInternals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "toggleDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "activate",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "field",
              "name": "externallyCoordinated",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "syncToggleState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "customElement": true
        },
        {
          "kind": "mixin",
          "description": "The interaction skeleton shared by the kit's two toggle controls\n(`vf-checkbox`, `vf-radio`) — everything they did identically apart from\ntheir glyphs and their toggle-vs-select semantics:\n\n- the click / Space→activate wiring, including the `event.repeat` guard that\n  stops a *held* Space from re-firing on every ~30ms tick;\n- the single disabled guard every activation passes through, so \"a disabled\n  toggle never activates\" is guaranteed in one place (the same shape as\n  VfFormControl.syncFormValue's disabled funnel for form values);\n- `aria-checked` / `aria-disabled` mirroring in `updated()`;\n- the self-managed host tabindex, and the ownership latch that keeps it\n  honest across reconnects (see consumerOwnsTabIndex).\n\nIt is a **mixin, not a plain base class**, because the two controls sit on\ndifferent bases and must stay there: `vf-checkbox` extends\nVfFormControl (it submits a value under a name), while `vf-radio` is\ndeliberately *not* form-associated — a lone radio has no value to submit, and\nits enclosing `vf-radio-group` is the form-associated surface. Unifying them\nunder one concrete base would mean making radio form-associated, which is a\nbehavior change (a bare `<vf-radio>` would start contributing to `FormData`),\nnot a refactor.\n\nA concrete control supplies four things: checked, its\ntoggleInternals, its toggleDisabled rule, and activate.",
          "name": "VfToggleControl",
          "members": [
            {
              "kind": "field",
              "name": "checked",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the toggle is on. Declared by the concrete control so it keeps\nits own reactive-property options and its own documentation (a checkbox\nis \"checked\", a radio is \"selected by its group\")."
            },
            {
              "kind": "field",
              "name": "toggleInternals",
              "type": {
                "text": "ElementInternals"
              },
              "privacy": "protected",
              "description": "The control's `ElementInternals`. Supplied by the subclass rather than\nattached here: `attachInternals()` throws on a second call, and\n`vf-checkbox` already inherits a handle from VfFormControl while\n`vf-radio` attaches its own.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "toggleDisabled",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "The control's effective disabled state — `disabled` OR whatever else\ndisables it (an ancestor `<fieldset disabled>` for the form-associated\ncheckbox, the enclosing group for a radio).",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "activate",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Do the control's own thing in response to a click or Space: flip state,\ntake focus, dispatch `vf-change`. Only ever called on an enabled control\n— the disabled guard lives in the shared skeleton."
            },
            {
              "kind": "field",
              "name": "externallyCoordinated",
              "type": {
                "text": "boolean"
              },
              "privacy": "protected",
              "description": "True while an ancestor coordinator owns this control's selection *and*\nits tab stop — the `vf-radio-group` case, where the group runs a roving\ntabindex and is the single source of truth for which child is checked.\nA coordinated control neither self-manages its tabindex nor optimistically\nself-checks. Always false for a standalone control.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "scale",
              "privacy": "private",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "gridSnap",
              "privacy": "private",
              "readonly": true,
              "default": "new GridSnapController(this)",
              "description": "Device-pixel grid snapping (opt in with applyGridSnap()); see src/grid-snap.ts."
            },
            {
              "kind": "field",
              "name": "#consumerOwnsTabIndex",
              "privacy": "private",
              "type": {
                "text": "boolean | null"
              },
              "default": "null",
              "description": "Whether the *consumer* authored a `tabindex`, latched on the *first*\nconnect only.\n\nIt has to be latched: once we self-manage, `this.tabIndex = 0` writes a\n`tabindex` attribute onto the host, so re-testing `hasAttribute` on a\nlater connect reads our own write back as the consumer's and stands down\nforever — leaving a reconnected control that no longer drops out of the\ntab order when disabled. Same trap, and the same fix, as `vf-menu-item`'s\n`#ownsRole` latch."
            },
            {
              "kind": "field",
              "name": "selfManagedTabIndex",
              "type": {
                "text": "boolean"
              },
              "privacy": "private",
              "default": "false",
              "description": "True when this control currently owns its host tabindex: the consumer\ndidn't author one and no coordinator is running a roving tabindex.\nRe-derived on every connect, because externallyCoordinated can\nlegitimately change when a control is re-parented into or out of a group."
            },
            {
              "kind": "method",
              "name": "syncToggleState",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Mirror the control's state into ARIA and keep its tab stop honest.\nSeparate from `updated()` so a subclass that gates its own work on\nchanged keys can still call it unconditionally."
            },
            {
              "kind": "method",
              "name": "#interact",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "The one gate every activation passes through."
            },
            {
              "kind": "field",
              "name": "#handleClick",
              "privacy": "private"
            },
            {
              "kind": "field",
              "name": "#handleKeydown",
              "privacy": "private"
            }
          ],
          "parameters": [
            {
              "name": "Base",
              "type": {
                "text": "T"
              }
            }
          ]
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfToggleControlInterface",
          "declaration": {
            "name": "VfToggleControlInterface",
            "module": "src/toggle-control.ts"
          }
        },
        {
          "kind": "js",
          "name": "VfToggleControl",
          "declaration": {
            "name": "VfToggleControl",
            "module": "src/toggle-control.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/modal-dialog.ts",
      "declarations": [
        {
          "kind": "variable",
          "name": "MODAL_FALLBACK_WIDTH",
          "type": {
            "text": "number"
          },
          "default": "260",
          "description": "The width an undeclared modal falls back to, in system px — a classic\ndialog's own measure, wide enough for a line of chrome text and a button row.\nIt exists so an unsized modal still renders, and still owns a *definite*\nwidth so dragging cannot squeeze it; the console says what to declare."
        },
        {
          "kind": "variable",
          "name": "modalDialogStyles",
          "default": "css` dialog { padding: 0; margin: auto; border: none; background: transparent; overflow: visible; color: inherit; font: inherit; /* showModal()'s focusing steps fall back to focusing the dialog itself when no descendant is chosen as the focus delegate — WebKit and Firefox reach that with the kit's controls hidden inside shadow roots — and both then draw their native ring around the top-layer box. The modal frame already announces the surface, and focus marks inside it are the kit's own (1-bit, on the controls), so the UA ring is suppressed rather than restyled. */ outline: none; } /* Scoped to [open]: an unqualified display here would out-cascade the UA's dialog:not([open]) { display: none } (author origin beats UA origin) and paint every closed dialog. Flex, so the frame child can resolve against the dialog's real box even when no height is declared and the UA's dialog:modal max-height is what caps it — a percentage height resolves to auto against that (indefinite) box, which left the frame content-tall and spilling past the border with its buttons unreachable; a flex child with min-height: 0 shrinks to the capped box instead, and the shells' inner scroll region takes over from there. */ dialog[open] { display: flex; flex-direction: column; } dialog::backdrop { background: transparent; /* The backdrop is top-layer: no page cursor rule can reach it, so the kit-wide cursor token (SPEC §3) is read here — \\`::backdrop\\` inherits custom properties from its dialog in current engines; where it doesn't, the fallback is the arrow it always showed. */ cursor: var(--vf-cursor, default); } `",
          "description": "Shared native-`<dialog>` styles for the modal shells: a chromeless top-layer\ndialog (the frame is drawn by the subclass) with a fully transparent\nbackdrop — pure System 7, no dimming."
        },
        {
          "kind": "class",
          "description": "Base class for the modal shell `vf-dialog` — and for a consumer's own\nmodal (an alert box, say) authored against the kit.\n\nOwns the native `<dialog>` lifecycle every modal shares: `open` sync, `show()` /\n`close()`, the device-grid pin on open, and the single `close` funnel that\nclears the grid-pinned margins (unsnapDialog) and fires `vf-close`\nwith the reason. Because every close path — Escape, `close()`, backdrop —\nroutes through the native `close` event, an Escape-close no longer leaves\nstale margins behind, so the next open re-centers.\n\nRemoving an open modal from the DOM is a close path too. HTML's dialog\n*removing steps* take the element out of the top layer **without** running\nthe close algorithm — no `close` event, no focus restoration — which is\nexactly what the standard framework pattern of unmounting a dialog instead\nof calling `close()` does. `disconnectedCallback` routes that path through\nthe same funnel: the removed element still fires `vf-close` (heard by\nlisteners on the element itself — it has left the tree, so nothing\nbubbles), `open` and the pinned margins reconcile so a re-append mounts it\nclosed and re-centered, and focus returns to the element that was focused\nwhen the modal opened.\n\nSubclasses supply only the frame chrome: a `render()` returning\n`<dialog",
          "name": "VfModalDialog",
          "members": [
            {
              "kind": "field",
              "name": "scale",
              "privacy": "protected",
              "readonly": true,
              "default": "new ScaleController(this)",
              "description": "Default-on display scaling (true 72dpi size); see src/scale.ts."
            },
            {
              "kind": "field",
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the modal is open. Kept in sync with the native `<dialog>`.",
              "attribute": "open",
              "reflects": true
            },
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | undefined"
              },
              "description": "Modal width in whole system px — the art's own unit, so the box holds its\nproportion to the chrome inside it at every display density.\n\n**Declare it.** A modal owns its width; it is not a shape its content\nhappens to fall into. The platform's own default is `fit-content` measured\nagainst the space *left over* beside the margins — and those margins are\nhow snapDialogToGrid pins the box and how a movable dialog is\ndragged, so an undeclared modal squeezes itself and reflows its text as it\nmoves toward an edge. Unset, it falls back to MODAL_FALLBACK_WIDTH\nand says so once in the console.",
              "attribute": "width"
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | undefined"
              },
              "description": "Modal height in whole system px.\n\n**Declare it,** with width. A modal is a fixed box in both axes —\nthe same rule `vf-window` follows, for the same reason: the classic shells\ncarried both dimensions in the resource, and a box that grows with its body\nis one whose buttons move under the pointer as its text changes. Unset, the\nbody sizes to its content — the old behavior — and it says so once in the\nconsole.\n\nContent taller than the declared box is clipped at the frame.",
              "attribute": "height"
            },
            {
              "kind": "field",
              "name": "_dialog",
              "type": {
                "text": "HTMLDialogElement"
              },
              "privacy": "protected"
            },
            {
              "kind": "field",
              "name": "dialogSize",
              "type": {
                "text": "Record<string, string | null>"
              },
              "privacy": "protected",
              "description": "The declared box as inline styles for the subclass's `<dialog>`:\n`style=${styleMap(this.dialogSize)}`. Width always resolves to something\ndefinite — the declaration or the fallback — because that is what keeps a\ndrag from squeezing the box.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "#warnedNoSize",
              "privacy": "private",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "One warning per element, on the open that first shows an unsized modal."
            },
            {
              "kind": "method",
              "name": "#warnIfUnsized",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Say it once, when an unsized modal is first shown — a hidden one is not\nyet anybody's problem, and the name in the message is what makes it\nfindable in a page with several.\n\nBoth dimensions are required, as on `vf-window`, but they fail differently:\nan undeclared width takes a definite fallback (a squeezing box is worse\nthan a wrong-but-stable one), while an undeclared height just sizes to the\ncontent and says so."
            },
            {
              "kind": "field",
              "name": "#closeReason",
              "privacy": "private",
              "type": {
                "text": "VfCloseReason | null"
              },
              "default": "null",
              "description": "Close reason pending for the next native `close` event."
            },
            {
              "kind": "field",
              "name": "#invoker",
              "privacy": "private",
              "type": {
                "text": "Element | null"
              },
              "default": "null",
              "description": "The element that was focused when the modal opened. The normal close path\nrestores focus natively, but the removal path (see the class doc) skips\nthe close algorithm, and by the time `disconnectedCallback` runs, focus\nhas already fallen to `<body>` — so we keep our own record."
            },
            {
              "kind": "method",
              "name": "show",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Open the modal (native `showModal()`), pinned onto the device grid."
            },
            {
              "kind": "method",
              "name": "close",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Close the modal. Fires `vf-close` with `{ reason: 'close' }`."
            },
            {
              "kind": "method",
              "name": "#syncDialog",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Reconcile the native `<dialog>` with `open`. Opening pins the UA's\nauto-centering onto the device-pixel grid (half-pixel offsets fringe the\n1-bit chrome — see scale.ts). Closing just calls `dialog.close()`, routing\nteardown through the native `close` event so _onNativeClose is the\none place margins are cleared and `vf-close` is fired."
            },
            {
              "kind": "field",
              "name": "#repin",
              "privacy": "private",
              "description": "Re-derive the pin rather than keep the latch: clear the pinned margins\n(restoring the UA's `margin: auto` centering) and re-pin from the live,\nre-centered rect. Without this the margins latched at open time forever —\nand the open-time rect can be wrong by the next frame: slotted content\nupgrading after `showModal()` grows the box (the pin then holds a\nviewport-tall dialog at the offset its small first render centered at,\nstranding its bottom off-screen), and a browser zoom rescales every\nmetric while the old margins hold the stale origin. A dragged position is\ntraded for re-centering on these signals — recoverable, where a stranded\nmodal with no drag handle (frame=\"plain\") is not."
            },
            {
              "kind": "field",
              "name": "#resizeObserver",
              "privacy": "private",
              "type": {
                "text": "ResizeObserver | undefined"
              }
            },
            {
              "kind": "method",
              "name": "#watchGeometry",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "parameters": [
                {
                  "name": "dialog",
                  "type": {
                    "text": "HTMLDialogElement"
                  }
                }
              ],
              "description": "While open, watch the two signals that invalidate the pin: the dialog's\nown box changing (content upgrade/growth, `--vf-scale` moving under a\nzoom or density change — every metric resizes with it) and the viewport\nresizing. Undone in _onNativeClose / `disconnectedCallback`."
            },
            {
              "kind": "method",
              "name": "#unwatchGeometry",
              "privacy": "private",
              "return": {
                "type": {
                  "text": "void"
                }
              }
            },
            {
              "kind": "method",
              "name": "_onNativeCancel",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Native `cancel` (Escape): remember the reason; `close` follows."
            },
            {
              "kind": "method",
              "name": "_onNativeClose",
              "privacy": "protected",
              "return": {
                "type": {
                  "text": "void"
                }
              },
              "description": "Native `close` — the single teardown funnel for every close path. Clears\nthe grid-pinned margins so the next open re-centers, syncs `open`, and\nfires `vf-close` with the reason."
            }
          ],
          "events": [
            {
              "description": "The modal closed. `detail: { reason: 'escape' | 'close' }`.",
              "name": "vf-close"
            }
          ],
          "attributes": [
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the modal is open. Kept in sync with the native `<dialog>`.",
              "fieldName": "open"
            },
            {
              "name": "width",
              "type": {
                "text": "number | undefined"
              },
              "description": "Modal width in whole system px — the art's own unit, so the box holds its\nproportion to the chrome inside it at every display density.\n\n**Declare it.** A modal owns its width; it is not a shape its content\nhappens to fall into. The platform's own default is `fit-content` measured\nagainst the space *left over* beside the margins — and those margins are\nhow snapDialogToGrid pins the box and how a movable dialog is\ndragged, so an undeclared modal squeezes itself and reflows its text as it\nmoves toward an edge. Unset, it falls back to MODAL_FALLBACK_WIDTH\nand says so once in the console.",
              "fieldName": "width"
            },
            {
              "name": "height",
              "type": {
                "text": "number | undefined"
              },
              "description": "Modal height in whole system px.\n\n**Declare it,** with width. A modal is a fixed box in both axes —\nthe same rule `vf-window` follows, for the same reason: the classic shells\ncarried both dimensions in the resource, and a box that grows with its body\nis one whose buttons move under the pointer as its text changes. Unset, the\nbody sizes to its content — the old behavior — and it says so once in the\nconsole.\n\nContent taller than the declared box is clipped at the frame.",
              "fieldName": "height"
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "MODAL_FALLBACK_WIDTH",
          "declaration": {
            "name": "MODAL_FALLBACK_WIDTH",
            "module": "src/modal-dialog.ts"
          }
        },
        {
          "kind": "js",
          "name": "modalDialogStyles",
          "declaration": {
            "name": "modalDialogStyles",
            "module": "src/modal-dialog.ts"
          }
        },
        {
          "kind": "js",
          "name": "VfModalDialog",
          "declaration": {
            "name": "VfModalDialog",
            "module": "src/modal-dialog.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/position.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "The mixin's surface, as a `declare class` for the same TS4094 reason\nVfToggleControlInterface states: TypeScript cannot name the\nanonymous class a mixin returns when emitting declarations. Keep it in sync\nwith the implementation below.",
          "name": "VfPositionedInterface",
          "members": [
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              }
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              }
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "customElement": true
        },
        {
          "kind": "mixin",
          "description": "Explicit placement — `top`/`left` in whole system px, on (nearly) every\ncomponent.\n\nA DITL resource laid a dialog out as a list of items, each with a rectangle\nin the window's own coordinates; arranging controls by stating where they go\nis as native to System 7 as stacking them. This mixin is that mechanism:\ndeclaring `top` or `left` takes the element out of normal flow and\nabsolutely positions it within its parent, both coordinates in the art's own\nunit so the position scales with the display like every other metric. A\nwindow or dialog body can be laid out either way — a tree of `vf-stack`s, or\npositioned children — with no stylesheet in either case.\n\nMechanics, mirroring how `vf-stack` writes its declared size:\n\n- Setting either property writes `position: absolute` and both offsets onto\n  the host's inline style as `calc(var(--vf-scale, 1) * Npx)` — live against\n  the display, resolved at paint time. The coordinate left unset is 0.\n- `right`/`bottom` are released to `auto` and `margin` zeroed, so the stated\n  offsets are the whole story — an auto-width box with both edges set would\n  stretch rather than sit (the `vf-icon` seed logic, generalized).\n- Unsetting both returns the element to normal flow: every inline\n  declaration this wrote is removed, and stylesheet values resume.\n\nThe anchor is CSS's own: the nearest positioned ancestor's padding box. The\nkit's containers are all deliberate anchors — a desktop's raster, a window's\ncontent region (the frame's inner edge, below the title bar — the 12px body\ninset governs flow content only, exactly the DITL convention), a dialog's\ncontent area, a stack's box, a fieldset's border box, a scroll area's\nscrolled plane. In a non-kit parent, give the parent `position: relative`,\nthe one line of CSS this feature can't write for you.\n\nThe writing rides a ReactiveController (`hostUpdated`) rather than\nan `updated()` override, for a lifecycle reason worth keeping in the source:\nthe mixin sits *under* each component class, and most components override\n`updated()` without calling `super.updated()` — Lit's base is a no-op, so\nnothing ever forced the call — which would silently shadow a mixin-level\noverride. A controller is invoked by ReactiveElement itself, after every\nupdate, no matter what the subclass does.\n\nIt re-applies **only when the property values changed** — the `vf-window`\nwidth/height rule. `vf-window` drag and `vf-icon` move write resolved px\ninto the same inline `left`/`top` without touching these properties (both\nre-seed from *computed* style, so they read a live calc correctly); if some\nunrelated update re-asserted the authored coordinates, activating a window\nwould snap it back to where its markup put it. Setting a property again is\nthe deliberate way to re-place a moved element — and, as with any reactive\nproperty, setting it to the value it already holds is a no-op.",
          "name": "VfPositioned",
          "members": [
            {
              "kind": "field",
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "attribute": "top"
            },
            {
              "kind": "field",
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "attribute": "left"
            }
          ],
          "attributes": [
            {
              "name": "top",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the top of the positioning parent, in whole system px.\nSetting this (or `left`) absolutely positions the element within its\nparent; the coordinate left unset is 0. Remove both to return the\nelement to normal flow.",
              "fieldName": "top"
            },
            {
              "name": "left",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Offset from the left of the positioning parent, in whole system px.\nSee top.",
              "fieldName": "left"
            }
          ],
          "parameters": [
            {
              "name": "Base",
              "type": {
                "text": "T"
              }
            }
          ]
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfPositionedInterface",
          "declaration": {
            "name": "VfPositionedInterface",
            "module": "src/position.ts"
          }
        },
        {
          "kind": "js",
          "name": "VfPositioned",
          "declaration": {
            "name": "VfPositioned",
            "module": "src/position.ts"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/size.ts",
      "declarations": [
        {
          "kind": "class",
          "description": "The mixin's surface, as a `declare class` for the same TS4094 reason\nVfPositionedInterface states: TypeScript cannot name the\nanonymous class a mixin returns when emitting declarations. Keep it in sync\nwith the implementation below.",
          "name": "VfSizedInterface",
          "members": [
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              }
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              }
            }
          ],
          "superclass": {
            "name": "LitElement",
            "package": "lit"
          },
          "customElement": true
        },
        {
          "kind": "mixin",
          "description": "A declared box — `width`/`height` in whole system px, the size half of\nVfPositioned's DITL story: a resource stated each item's rectangle,\nposition *and* extent, in the window's own coordinates.\n\nIt goes only on the components whose box is *stated* rather than governed\nby content: the two static-text elements (`vf-paragraph`, where width is\nthe measure the copy wraps to; `vf-label`, a caption column's shared\nwidth), `vf-stack` (a panel's declared box) and `vf-window` (the WIND\nrect). The rest of the kit is content-governed on purpose — a push button\nis as wide as its label, a popup hugs its widest option (README, \"the\ncontent governs the box\") — and the components that *transform* a declared\nsize keep their own properties: `vf-desktop` adds the bezel around it,\n`vf-dialog` sizes the top-layer box, `vf-img` negotiates with the art's\nnatural size.\n\nMechanics, mirroring the position mixin: each declared dimension goes on\nthe host's own inline style as `calc(var(--vf-scale, 1) * Npx)` — live\nagainst the display, resolved at paint time — and a whole number keeps the\nbox on the device-pixel grid by construction (the size half of layout\ncontract rule 3, the one grid snapping deliberately doesn't cover).\nUnsetting a dimension removes the declaration and that axis returns to\nlayout.\n\nThe writing rides a ReactiveController for the lifecycle reason\nposition.ts records (components override `updated()` without calling\n`super.updated()`, which would shadow a mixin-level override), and it\nre-applies **only when the property values changed** — the `vf-window`\ngrow-box rule: a resize writes resolved px into the same inline\n`width`/`height` without touching these properties, and re-asserting the\nauthored size on an unrelated render would snap a resized window back.\nSetting the property again is the deliberate way to re-size it.",
          "name": "VfSized",
          "members": [
            {
              "kind": "field",
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "attribute": "width"
            },
            {
              "kind": "field",
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "attribute": "height"
            }
          ],
          "attributes": [
            {
              "name": "width",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Width in whole system px. What the number means is the component's own\naffair — the measure a paragraph wraps to, a caption column, a window's\nbox — but the mechanics are shared: the length lands on the host as a\nlive `calc(var(--vf-scale, 1) * Npx)`, so the box scales with the\ndisplay and sits on the device-pixel grid by construction. Remove it\nand the width returns to layout.",
              "fieldName": "width"
            },
            {
              "name": "height",
              "type": {
                "text": "number | null | undefined"
              },
              "description": "Height in whole system px; see width. Content that outgrows a\ndeclared height overflows the box rather than growing it — the number\nis the layout, and content that doesn't fit is a number to raise.",
              "fieldName": "height"
            }
          ],
          "parameters": [
            {
              "name": "Base",
              "type": {
                "text": "T"
              }
            }
          ]
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "VfSizedInterface",
          "declaration": {
            "name": "VfSizedInterface",
            "module": "src/size.ts"
          }
        },
        {
          "kind": "js",
          "name": "VfSized",
          "declaration": {
            "name": "VfSized",
            "module": "src/size.ts"
          }
        }
      ]
    }
  ]
}
